# travel hub — 전달 문서 모음 (2026-09-11, 10차 재검토)

이 파일 하나에 RELEASE_STATUS·BUSINESS_DECISIONS·MARKETING_LAUNCH_PLAN·OPERATIONS_SETUP
네 문서를 전부 이어 붙였다(다운로드 편의 + ChatGPT가 한 번에 읽을 수 있게).
앞으로도 문서 전달은 이 방식(파일 하나)으로 한다.

## 차례

- 1. 출시 상태 (RELEASE_STATUS)
- 2. 사업 결정 정리 (travel hub) — ChatGPT 판단용
- 3. 사전 출시 마케팅 계획 (travel hub)
- 4. 운영 서버 설정 순서 (OPERATIONS_SETUP)

---

## 1. 출시 상태 (RELEASE_STATUS)

## 0. 9차 재검토 갱신 요약(가장 중요 — 여기부터 읽기)

claude_round9_handoff.md의 지시대로 앞부분 재현된 오류(1~4)부터
고치고, 그다음 확정 사업 방향·추가 요청(6-1~6-4)을 순서대로 구현했다.
아래 표에 항목별 수정 위치·재현 결과·완료 여부·실제 연결 미검증
여부·사용자가 다음에 할 일을 정리한다. **"완료"는 코드가 실제로
동작하고 합성 데이터로 검증됐다는 뜻이지, 실제 Google/토스/Resend
키·실제 iPhone 기기·실제 배포로 검증됐다는 뜻이 아니다** — 8절 참고.

| 항목 | 수정 위치 | 재현 결과 | 상태 | 실연결 미검증 | 다음 행동(사용자) |
|---|---|---|---|---|---|
| R9-1 동기화 삭제충돌 우회 | `src/design/spots.js`(daSyncPush) | 두 기기 동시 삭제·수정 시나리오를 실제 재현 — 예전엔 삭제 큐가 서버의 새 버전으로 몰래 재무장돼 최신 내용이 사라짐. 고친 뒤엔 삭제를 영구 취소하고 최신 내용을 복원함을 확인(`scripts/test-sync-conflict-devices.mjs` 4번) | 완료 | 없음(순수 클라이언트 로직) | 없음 |
| R9-2 같은 필드 충돌 보존 | `src/design/spots.js`(daRemergePlaceConflict) | 두 기기가 같은 필드를 다르게 고치는 시나리오 재현 — 예전엔 서버 값이 조용히 사라짐. 고친 뒤엔 `_fieldConflicts`로 양쪽 값을 보존하고 화면에서 선택하게 함을 확인(같은 테스트 5번) | 완료 | 없음 | 없음 |
| R9-3 courses/trips 충돌 무시 | `src/design/spots.js` | 여행 정보 동시 수정 재현 — 서버의 반려 응답이 로컬 최신 수정을 덮어씀을 확인하고 수정(같은 테스트 6번). visits는 이미 안전했음(점검만) | 완료 | 없음 | 없음 |
| R9-4 장소확인 예약 멱등성 | `server/entitlement-usage.mjs` | 이중 확정·스윕 후 뒤늦은 응답 두 시나리오를 실제로 재현해 확인(`server/test/place-lookup-reservation-idempotency.test.mjs`, 17개) | 완료 | 없음(로직 검증 — 실제 Google Places 키 연동 자체는 별도 항목) | 없음 |
| R9-5 한도소진 후 비용 노출 | `server/entitlement-usage.mjs`, `server/config.mjs` | 한도 초과 후 새 검색 반복 시나리오 재현 — 계정당 하루 20회(제안값)로 상한(`server/test/entitlement-overlimit-verify-cap.test.mjs`, 9개) | 완료 | 상한값(20회) 자체는 확정 아님, 실사용 데이터로 조정 필요 | 실사용 데이터를 보고 상한값 조정 여부 결정 |
| R9-6 날씨 이중소비+착장 날짜혼선 | `server/routes/weather.mjs`, `src/design/weather-card.js` | 동시 요청 병합 시 캡 이중 소비, 선택 날짜와 현재 날씨 혼용 두 버그 모두 실제 재현(`server/test/weather-merge-cap.test.mjs` 4개, `scripts/test-weather-outfit-note-date-scope.mjs` 7개) | 완료 | WeatherAPI.com 무료 등급 실제 키 연동은 별도 미검증 | 없음 |
| R9-7 사업 방향 문서 갱신 | `docs/BUSINESS_DECISIONS.md` 1-2절 | 해당 없음(문서 작업) | 완료 | 문서일 뿐, 아래 남은 결정 사항은 여전히 사용자 승인 필요 | 4,900원 베타가·소규모 모집 규모 등 미결 항목 검토 |
| R9-8 최소 제휴 준비 | `server/affiliates.mjs`, `src/design/affiliates.js` | 실제 등록 0건 상태에서 화면에 안 보이는지, 합성 데이터 주입 시 뜨는지 실제 확인(`server/test/affiliates.test.mjs` 14개, `scripts/test-affiliates.mjs` 15개) | 완료(구조만 — 실제 제휴 0건) | 실제 제휴사 계약·링크 전무(의도됨) | 실제 제휴 계약 체결 여부 결정 |
| R9-9 정렬+메타데이터 분리 | `src/design/import-adapter.js`, `spots.js` | 재수입 시 firstAddedAt 유지, originalSavedAt 비파괴적 채움, 레거시 항목 정렬 시 뒤로 밀림(숨지 않음)을 실제로 확인(`scripts/test-sort-and-metadata.mjs` 22개) | 완료 | 숙소 좌표 없음(거리순 기준이 센트로이드일 뿐 GPS 아님) | GPS 옵트인 버튼 추가 여부 결정 |
| R9-10 세계 공통 상위분류+다중 태그 | `src/design/import-adapter.js`, `spots.js` | 야키토리+이자카야 동시 태그, 동의어 통합, 사용자 확정 보호, 레거시 마이그레이션 전부 실제 확인(`scripts/test-tags-and-category.mjs` 17개) | 완료 | 태그 어휘 21개는 시작 세트일 뿐, 확정 목록 아님 | 태그 어휘 추가 필요 시 요청 |
| R9-11 테스트 위치 모드 | `src/design/import-adapter.js`, `spots.js` | 잠금/해제 지속성, 프리셋 적용, 거리순 연동, 해제 복귀 전부 실제 확인(`scripts/test-test-location-mode.mjs` 14개) | 완료(개발자 전용 최소 구현) | 실제 계정 화이트리스트 메커니즘 없음(쿼리파라미터 방식) — 실제 GPS·로밍 검증 아님 | 아이폰 비공개 프리뷰 방법 3가지 중 택1 |
| R9-12 소규모 베타·피드백 체계 | `server/feedback.mjs`, `server/invite-codes.mjs`, `src/design/admin.html` 등 | 접수/설문/관리자 인증/초대코드 게이트(기본 꺼짐) 전부 실제 확인(`server/test/*` 38개, `scripts/test-feedback-and-survey-ui.mjs` 16개) | 완료(실제 모집은 비활성 유지) | 화면 스크린샷 첨부 미구현(의도됨), 관리자 화면 실제 배포 안 됨 | 실제 모집 시작 여부·규모 결정 |
| R9-13 거리영상 안정성 보강 | `src/design/street-video.js` | 로드 실패(onerror)·타임아웃·낡은 콜백 세 가지 모두 실제로 재현해 확인(`scripts/test-street-video.mjs` 8~10번, 27개 전체) | 완료 | 실제 방송 등록 여전히 0건(STREET_VIDEOS 빈 표) | 실제 방송 확인 후 영상 등록 |

**이번 라운드에서 절대 안 한 것(문서에 없는 사업 기능 신규 추가
금지 지시 준수)**: 실제 공개 배포, master 병합, 실과금, 유료 계약,
SNS 게시, 외부 메시지 발송, git 히스토리 재작성, 가격/사용한도 값
임의 변경, 저가 모집 로직 무단 추가.

**전체 회귀(10차 재검토 — 수치 정정)**: 이전 판에 "server/test 전체
53개"라고 썼던 건 서버 테스트와 화면(Chromium) 테스트를 합친 뒤
잘못 센 숫자였다 — ChatGPT가 실제로 파일 수를 세어 지적한 대로
정정한다. 실제로는:
- **서버 테스트(순수 Node, DB만 사용)**: `server/test/*.test.mjs`
  **34개**(신규 R9/R10 파일 포함).
- **화면 테스트(실제 Chromium 브라우저 구동)**: `scripts/test-*.mjs`
  **21개**.
- 합계 55개 파일을 이번 라운드 마지막에 한 번에 실행해 전부 종료코드
  0(전체 통과)임을 확인했다(개별 파일 결과는 각 항목의 테스트 이름으로
  위 표에 남겼다 — 이 문서에 매 항목 반복 나열하지 않는다).

---

## 0-1. 10차 재검토 갱신 요약(9차 다음으로 중요 — 여기부터 읽기)

이 메시지는 9차 결과 검토·정정, 만료 예약/코스 동기화 데이터 손실
버그 수정, 분류 체계 세계 공통화, AI 보조 분류(기본 비활성)와 원가
검증, 위치 공급 함수 통합, 영어 지원 구조를 모두 합친 단일 지시로
받았다. 아래 표에 항목별 상태를 정리한다. **표기 원칙은 9차와
동일하다 — "완료"는 코드가 실제로 동작하고 합성 데이터로 검증됐다는
뜻이지, 실제 키·실제 기기·실제 배포 검증을 뜻하지 않는다.**

| 항목 | 수정 위치 | 재현/검증 결과 | 상태 | 실연결 미검증 | 다음 행동(사용자) |
|---|---|---|---|---|---|
| R10-1 9차 보고 정정 | `docs/RELEASE_STATUS.md` | ChatGPT가 직접 파일 수를 세어 "53개"가 서버+화면 테스트를 잘못 합산한 숫자임을 지적 — 서버 34개/화면 21개로 정정 | 완료 | 없음(문서 정정) | 없음 |
| R10-2 만료 예약 늦은 성공 무료확정 | `server/entitlement-usage.mjs`(commitNewLookupUsageCharge) | ①old 예약→②만료로 되돌림→③new 예약이 old를 스윕→④new 성공 확정→⑤만료된 old가 뒤늦게 성공 응답 — 예전엔 old가 공짜로 확정(`entitlement_place_confirmed`만 늘고 `used`는 그대로). git stash로 수정 전 재현 후 수정 확인(`server/test/entitlement-expired-reservation-late-success.test.mjs` 13개, 기존 `place-lookup-reservation-idempotency.test.mjs`도 같은 원칙으로 정정) | 완료 | 없음(순수 서버 로직) | 없음 |
| R10-3 courses/trips 충돌 시 서버 최신값 소실 | `src/design/spots.js`(daRemergeGenericConflict), `src/design/import-adapter.js`(courses/trips 기준선 신규) | 원본 v1→A가 note='SERVER-NEW' 저장 v2→B가 v1 기준으로 memo만 고쳐 저장 — 예전엔 충돌 시 "버전만 바꿔 전체 재제출"해 SERVER-NEW가 통째로 사라짐. courses/trips에도 places와 같은 기준선(baseline) 추적을 새로 추가해 필드 단위 병합으로 고침. 같은 세션에서 점검하라고 지시받은 "저장 중 새 장소 추가 보존"(응답 대기 중 추가한 장소가 사라짐)과 "기준 스냅샷의 재시작 보존"(미동기화 수정이 재시작 후 충돌에서 조용히 사라짐)도 실제로 재현해 확인 후 수정 — 기준선을 localStorage에 지속시키고 로그아웃 시 계정 간 유출 방지까지 처리. git stash로 세 건 모두 수정 전 재현 확인(`scripts/test-sync-conflict-devices.mjs` 시나리오 7~9) | 완료 | 없음(순수 클라이언트 로직) | 없음 |
| R10-4 분류 세계 공통화+태그 레지스트리 | `src/design/import-adapter.js`(TAG_REGISTRY_BUILTIN) | `daInferTags('居酒屋')`가 부분 문자열(酒屋)로 주류샵과 잘못 겹치는 문제, `daInferTags('스시 말고 라멘 먹기')`가 부정문을 실제 업종으로 오인하는 문제 둘 다 실제 재현 후 수정(문자 클래스 제외 + 부정 단서 감지). 21개 고정 정규식을 id/label/synonyms/source 분리 레지스트리로 바꾸고 사용자 태그 생성·이름수정·삭제·일괄편집 CRUD 신규 구현(place.tags는 계속 label 문자열 저장 — 비파괴적). git stash로 두 버그 모두 수정 전 재현 확인(`scripts/test-classification-registry.mjs` 22개, 기존 `test-tags-and-category.mjs`도 새 UI 구조에 맞게 갱신) | 완료 | 없음(순수 클라이언트 로직) | 새 태그 이름 수정·삭제·일괄편집은 코드는 있으나 전용 관리 화면은 없음(장소별 태그 편집 화면에서만 조작 가능) — 별도 관리 화면 필요 시 요청 |
| R10-5 자동분류 우선순위+AI 보조(기본 비활성) | `server/adapters/ai-classify.mjs`(신규), `server/routes/ai-classify.mjs`(신규), `src/design/import-adapter.js`(daApplyConfirmedTypes) | 우선순위(사용자 확정값>확인된 장소 유형>규칙>AI>미분류)를 실제 구현. 위치 확인 응답의 Google types/primaryType을 분류에 활용(분류만을 위한 새 유료 호출 없음). AI는 실제 공급자 없음(의도적 미구현) — mock 어댑터로 출력 스키마 검증(상위분류 화이트리스트, 태그 개수/길이/중복정규화, localId 위조 방지)과 비활성 기본값을 확인(`server/test/ai-classify.test.mjs` 17개, `ai-classify-mock-enabled.test.mjs` 10개, `scripts/test-place-lookup.mjs`·`test-classification-registry.mjs`에 우선순위 체인 시나리오 추가) | **구조 구현 + 모의 검증만 완료. 실제 AI 분류는 미완료**(AI 키 자체가 없음) | AI 실제 공급자·모델·단가 전부 미확정 — `/api/health` 같은 응답으로 "AI 연결됨"이라 말한 적 없음 | AI 공급자·모델 선정 여부 결정(선정 전까지 AI 보조 분류는 계속 비활성) |
| R10-6 AI 원가 검증 보고 | `docs/BUSINESS_DECISIONS.md` 3-3-6절, `scripts/analyze-classification-rate.mjs`(신규) | 실제 daInfer/daInferTags 코드를 합성 다국어 데이터로 돌려 50/160/300+3 시나리오의 ①전체 장소 수 ②규칙 해결 수 ③AI 후보 수 ④배치 요청 수를 실측(합성 데이터일 뿐 실제 고객 평균 아님을 명시). ⑤~⑦(토큰·모델·단가)은 공급자 미확정이라 미검증으로 명시, 임의 숫자 미작성. 무료체험(700원) 기준 핵심 제공량 예약 후 AI 헤드룸이 약 245원뿐임을 계산해 보고 | 완료(보고서 작성 — 실제 원가 확정은 공급자 선정 후) | 실제 AI 단가·실제 고객 분류 성공률 전부 미검증 | 3-3-6절 읽고 AI 도입 시 예산 배분 방향 결정 |
| R10-7 위치 공급 함수 통합 | `src/design/spots.js`(daLocationBasis), `server/db.mjs`(test_access 컬럼), `server/routes/entitlement.mjs`, `server/index.mjs` | 거리순·코스출발점이 공통 함수(테스트 위치>세션 GPS>평균 위치 우선순위)를 쓰도록 통합. "평균 위치"를 "지정 위치"로 잘못 부르던 표현 전부 정정. 코스 출발지의 GPS 버튼이 테스트 위치를 무시하고 항상 실제 GPS를 부르던 결함 수정(테스트 위치 켜져 있으면 그 좌표 사용, 실제 GPS 미호출을 테스트로 확인). "?testmode=1만으로 영구 잠금 해제"되던 결함을 서버 승인 모델로 교체 — accounts.test_access 컬럼 신규, 관리자 전용 API(`/api/admin/test-access`)로만 켜고 끌 수 있음, 클라이언트는 로그인마다 서버에 재확인. git stash 없이 새 서버 모델 자체를 재현 테스트로 검증(`server/test/test-access.test.mjs` 10개, `scripts/test-test-location-mode.mjs` 20개 재작성 — URL 파라미터 무효화·GPS 미호출·승인 철회 시 로컬 잔여값 무시 확인 포함) | 완료 | 실제 GPS·로밍 조건 검증 아님(테스트 위치는 좌표 치환일 뿐) | 아이폰 실기기·현지 GPS 검증은 여전히 사용자 직접 확인 필요(8절 참고) |
| R10-8 영어 지원 구조 | `src/design/import-adapter.js`(A.t/A.setLocale/A.locale) | 내부 키/표시 문구 분리 사전 조회 구조 신규(ko 사전만 존재, en 없음). 로케일 전환 전후 목적지(도쿄) 날짜·시간 완전 동일함을 재현 테스트로 확인(시간대와 로케일 무관 증명). 사전 없는 로케일 전환은 안전하게 실패 처리. 이번 라운드 신규 화면(태그 관리)만 이 구조를 실제로 씀(`scripts/test-i18n-structure.mjs` 10개) | 완료(구조만 — 번역 자체는 범위 밖) | 실제 영어 사전 없음(의도됨), 기존 화면 대부분 여전히 한국어 하드코딩 | 실제 영어 지원 착수 시점·우선순위 화면 결정 |

**이번 라운드에서 절대 안 한 것(9차와 동일 원칙 유지)**: 실제 공개
배포, master 병합, 실과금, 유료 계약(AI 포함), SNS 게시, 외부 메시지
발송, git 히스토리 재작성, 가격/사용한도 값 임의 변경, 실제 AI
공급자 연결.

**전체 회귀(10차)**: 서버 테스트 `server/test/*.test.mjs` **38개**
(9차 34개 + 신규 4개: 만료예약 재현, AI 분류 계약 2개, 테스트접근
계약), 화면 테스트 `scripts/test-*.mjs` **23개**(9차 21개 + 신규 2개:
분류 레지스트리, i18n 구조). 합계 **61개 파일**을 이번 라운드 마지막에
한 번에 실행해 전부 종료코드 0(전체 통과)임을 확인했다.

---

작성: 2026-09-10(1차) · 갱신: 2026-09-10(2차 — 확정 상품 반영) ·
갱신: 2026-09-10(3차 — 결제/이메일/경로 공급자 확정, 서버 집행 구조
재설계) · 갱신: 2026-09-10(4차 — ChatGPT가 실제로 재현한 문제 수정,
API 비용 통제 실제 구현, 경유지 상한 반영) · 갱신: 2026-09-10(5차 —
비용 SKU 단가 정확화, 비용 예약 원자성 강화, 장소조회 캐시·동시성
버그 수정, 결제·타임아웃 경계 6건, **재방문 여행자 지원 신규**,
재방문 기록 보호 동기화) · 갱신: 2026-09-10(6차 — **초기 테스트
상품(무료체험 10+1/유료 50+30) 이용권 사용량 실제 집행**, 내부 비용
안전상한, 신규판매 예산 판정에 기존 유료고객 몫 반영, **재방문
여행자 지원을 실제 화면까지 연결**, 기존 전체치환 동기화 경로
검증) · 갱신: 2026-09-10(7차 — **`/api/places` 전체치환 데이터손실을
6차에서 발견만 하고 남겨 뒀던 것을 실제로 고침**(trips/visits와 같은
버전 보호+보수적 병합), **신규 장소 확인 한도 우회 차단**(ChatGPT가
재현한 우회 — 실제 확정 결과 기준으로 재판정), **하루 비용 한도와
유료 이용권 하루 몰아쓰기 약속의 충돌 해결**, **착장 판단용 날씨 카드
신규**(WeatherAPI.com 무료 등급)) · 갱신: 2026-09-10(8차 — 7차 ZIP
검토에서 ChatGPT가 재현한 세 가지 결함을 실제로 고침: **동기화 충돌
보호 재설계**(baseVersion 기준 서버 발급 버전 — 동시 편집 데이터
손실 차단), **장소확인 빠른경로 재판정**(같은 검색조건이어도 실제
반환 장소가 바뀌면 다시 신규 판정), **날씨 시간대 버그 수정**(공급자
epoch 필드 사용 — 서버 시간대와 무관하게 항상 정확), **현지 거리·
옷차림 영상 신규**(클릭 시에만 공식 유튜브 플레이어 로드, 실제 영상
등록은 미검증)) · 갱신: 2026-09-11(9차 — ChatGPT가 재현한 동기화
삭제충돌 우회·같은 필드 충돌 손실·장소확인 예약 이중처리·비용 노출
4건 실제 수정, **사업 방향 재확정**(세계 다도시·월 100만원 목표·이용권
+재구매 모델), **최소 제휴 준비 신규**, **저장 목록 정렬+메타데이터
분리 신규**, **세계 공통 상위분류 유지+세부 다중 태그 신규**,
**테스트 위치 모드 신규**, **소규모 베타·피드백 체계 신규**(초대코드는
기본 꺼짐), **거리영상 안정성 보강**(loadIframeApi 오류·타임아웃·
낡은 콜백 정리) — 자세한 항목별 표는 위 0절 참고) · 브랜치 `design-integration`

**8차 갱신 배경**: 7차 코드를 ChatGPT가 실제로 재현·검토한 결과 세
가지 결함이 드러났다. 첫째, `account_places` 동기화가 `버전 >= 저장된
버전`이면 받아들여 두 기기가 같은 원본에서 동시에 고치면(둘 다
"버전+1"을 보내면) 뒤에 도착한 기기가 앞 기기의 수정을 조용히
지웠다 — 이제 서버만 버전을 발급하고(`baseVersion` 정확히 일치할
때만 수락), 충돌 시 3-way 재병합으로 양쪽 수정을 모두 보존한다.
둘째, "같은 로컬 슬롯+같은 검색 조건이면 빠른 경로로 신규 아님"
판정이 실제로 돌아온 장소가 바뀌었는지 전혀 대조하지 않아, 캐시 만료
후 재조회로 실제 장소가 바뀌어도 계속 비과금으로 샜다 — 이제 항상
실제 반환 식별자를 대조하고, 잠정 예약도 프로세스 종료 시 복구
가능하게 했다. 셋째, `server/adapters/weather.mjs`가 공급자의 시간대
표기 없는 "현지 시각" 문자열을 `new Date()`로 그대로 파싱해 서버가
UTC로 돌면 일본 현지 시각이 9시간 밀려 표시됐다 — 이제 epoch 필드만
쓰고 tz_id로 재지역화해 계산한다. 마지막으로 "이미 요청한" 현지
거리·옷차림 영상 기능을 범위를 제한해 신규 구현했다(클릭 시에만 공식
플레이어, 자동재생 금지, 서버 저장·중계 없음) — 단, 실제 방송 존재·
임베드 가능 여부를 이번 세션에서 검증할 방법이 없어 영상 목록은 빈
표로 남겨 뒀고, 검증된 영상이 없는 모든 도시에서 버튼이 실제로
숨겨진다(**실제 영상 등록은 사람이 직접 확인 후 별도로 진행해야
하는, 아직 안 끝난 일**). 자세한 내용은 3-3절.

**7차 갱신 배경**: 6차에서 "새로 발견했지만 미해결"로 남겨 뒀던
`/api/places` 전체치환 데이터손실 문제(아래 3절 옛 123행 참고)를 이번
세션에서 실제로 고쳤다 — trips/visits와 같은 버전 비교+보수적 병합을
`account_places`/`account_courses`에도 적용했고, 삭제는 명시적
`deletedIds`로만 이뤄지게 해 배열 누락으로 추측 삭제하지 않는다.
ChatGPT가 실제로 재현한 두 번째 우회(같은 로컬 장소 식별자에 검색어만
바꿔 보내 서로 다른 실제 장소를 무료로 확인받는 것)도 막았다 — "신규
여부"를 클라이언트 문자열이 아니라 공급자가 실제로 돌려준 장소
식별자로 재판정한다. 세 번째로, 계정별 하루 비용 한도(기본 500원)가
무료/유료 구분 없이 적용돼 유료 이용권이 약속한 "하루 안에 50곳
확인"을 11건 만에 막던 버그를 고쳤다(이용권 기간 전체 누적 한도보다
낮게 잡히지 않게 함). 마지막으로 착장 판단용 날씨 카드를 신규로
추가했다("오늘 동선" 화면 상단 — WeatherAPI.com 무료 등급, 서버가
키를 들고 소비자는 절대 키를 입력하지 않음, 로그인 없이도 볼 수
있고 이용권/비용 원장을 전혀 안 건드림). 자세한 내용은 3-2절.

- **①코드 완료** — 동작에 필요한 코드가 전부 작성됐고, 외부 서비스가
  없거나 실제 HTTP 요청으로 검증까지 끝난 기능.
- **②모의 검증** — 실제 코드 경로는 전부 진짜고, 응답을 주는 쪽만
  가짜(테스트 어댑터, `fetch` 모의 등)로 검증.
- **③실제 연결 검증** — 실제 키·실제 네트워크로 외부 서비스에 요청을
  보내 성공 응답을 실제로 받아봄.
- **④사용자 설정 필요** — 코드는 있지만 실제 키가 없거나
  (EGRESS_BLOCKED로 이 세션이 발급받지 못함), 사용자가 직접 키를
  넣고 처음 검증해야 하는 항목.
- **⑤서버 완료·화면 미연결**(5차 신규 분류) — 서버 API·DB 스키마는
  실제 HTTP 요청으로 검증까지 끝났지만, `src/design`의 실제 사용자
  화면(spots.js 등)이 아직 그 API를 부르지 않는 항목. "배치 서버
  구현"과 "사용자가 편하게 쓰는 화면"을 구분하라는 지시에 따라, 이
  상태를 ①로 부풀리지 않고 명시적으로 구분한다.

**이번에도 ③(실제 연결 검증)은 늘지 않았다** — 네트워크 제약은
4차와 동일하게 남아 있다(`docs.tosspayments.com`, `resend.com`,
`developers.google.com`, `mapsplatform.google.com` 전부 이번 세션에도
다시 시도했지만 여전히 접속 불가). 5차의 진짜 변화는 **① 코드
완료·실제 재현 테스트 통과 항목이 늘어난 것**과 **재방문 여행자
지원의 서버 쪽 전체가 새로 생긴 것(단, 화면은 아직 미연결 — ⑤)**
이다.

---

## 1. 기능별 상태

| 기능 | 상태 | 근거 |
|---|---|---|
| Google Takeout ZIP/CSV/JSON 가져오기 | **①** | `scripts/test-zip-import.mjs` 18개 |
| 재가져오기 중복 방지, 사용자 수정(유형·도시) 보존 | **①** | `scripts/design-integration-check.mjs`, `scripts/test/t49.mjs` |
| 도시별 컬렉션·검색·수동 정리 | **①** | `scripts/design-integration-check.mjs` |
| **가져오기 자체는 유료 조회를 전혀 안 함**(6-① 지시) | **①** | `server/test/reliability-and-cost.test.mjs` — 300곳을 저장(PUT /api/places)만 했을 때 `cost_ledger`에 행이 0개 늘어남을 실제로 확인 |
| **장소 조회 — 인증 필수 + 초기가져오기/재조회/일괄 3단 한도 분리** | **①** | `server/test/server-v2.test.mjs`, `server/test/reliability-and-cost.test.mjs` — 단일 조회는 `phase` 값과 무관하게 항상 requery 한도만 적용(4차 수정: 예전엔 클라이언트가 phase=import를 자기 신고해 큰 한도를 받을 수 있었다), 더 큰 배치 한도는 `/api/places/lookup-batch`로만(서버가 실제 처리 개수로 판단) |
| **장소 조회 — Places API(New) 전환, 동명 장소 지역 힌트 판별** | **①**(코드) + **②**(계약 테스트) | `server/test/provider-contracts.test.mjs` — Legacy Find Place 폐기, `v1/places:searchText` 사용, 지역 힌트와 실제로 맞는 후보를 첫 결과보다 우선(4차 신규) |
| **장소 조회 — 중복 호출 방지(캐시) + 비용 원장 기록** | **①** | `server/test/reliability-and-cost.test.mjs` — 완전히 같은 질의 반복 시 실제 유료 호출이 1건만 기록됨을 확인 |
| **장소 조회 — 계정별/전체 비용 한도(일일·월간)** | **①** | `server/cost-ledger.mjs`, `server/test/reliability-and-cost.test.mjs` — 한도 초과 시 외부 호출 자체를 안 함(503), 계정 한도가 먼저 걸리면 전체 한도를 안 건드림(순서 수정) |
| 불확실한 후보 확인(사람이 "맞아요"를 눌러야 반영) | **①** | `scripts/test-place-lookup.mjs` 11개 |
| **장소 조회 — 실제 Google Places 연결** | **④** | `mapsplatform.google.com` 접속 불가로 문서 재확인·실 키 호출 모두 못 함 |
| **개인화 코스 생성 — 서버 집행 + 저장/체험차감 원자성 + 잠금 만료·소유권 검증** | **①** | `server/test/server-v2.test.mjs`, `server/test/reliability-and-cost.test.mjs` — 체험 차감·코스 저장·결과 기록을 하나의 DB 트랜잭션으로 묶음(4차 신규), 죽은 프로세스가 남긴 오래된 잠금을 회수해 계정이 영구히 안 막힘(4차 신규, 실제 재현), 같은 멱등키에 다른 요청 본문이 오면 충돌(409) 처리(4차 신규) |
| **코스 생성 — 입력 검증(좌표 범위·장소 개수·시간 예산)** | **①** | `server/test/reliability-and-cost.test.mjs` — 위도 999, 장소 61개, 시간예산 999999분 각각 실제로 400 거부 확인(4차 신규) |
| **운영에서 경로 키 누락 시 가짜 실제경로 방지(ChatGPT 재현 버그 수정)** | **①** | `server/test/reliability-and-cost.test.mjs` — `APP_ENV=production`+키없음+ChatGPT가 쓴 것과 동일한 synthetic 좌표로 실제 `/api/course/generate`를 호출해 `routedReal:false`·체험 미차감을 확인(4차 신규, 최우선 수정) |
| **도보 경로 계산 — Google Routes(WALK), 경유지 상한(25개)·고요금 구간(11개+) 반영** | **①**(코드) + **②**(계약·재현 테스트) | `server/test/provider-contracts.test.mjs`, `server/test/routes-waypoint-limits.test.mjs`(4차 신규, 12개) — 25개 초과 시 연결된 여러 호출로 분할(경계 보존 확인), 11개 이상 구간은 고요금 SKU로 비용 기록. `server/test/cost-budget-allornothing.test.mjs`(4차 신규) — 예산이 일부만 감당 가능하면 전부 추정으로 대체(실제·추정 뒤섞임 방지) |
| **도보 경로 — 실제 Google Routes 연결** | **④** | `developers.google.com` 접속 불가 |
| 가용 시간·출발 시각 반영, 시간 초과 시 안내 | **①** | `scripts/test-course-generation.mjs` |
| 도시별 코스 분리, 여러 날짜 일정 | **①** | `scripts/test-multi-day.mjs` 28개 |
| 새 코스 생성·저장 실패 시 기존 코스 보존 | **①** | `scripts/test-course-generation.mjs`, `scripts/test-purchase-flow.mjs` |
| 저장된 코스 재열람은 새 유료 생성으로 처리 안 함 | **①** | `scripts/test-multi-day.mjs` — 날짜 탭 전환은 서버 호출 자체가 없어 `course_generated` 이벤트가 새로 안 늘어남 |
| **이메일 로그인 — 쿨다운·IP 레이트리밋·연속실패 잠금** | **①** | `server/test/server-v2.test.mjs` |
| **이메일 로그인 — 실제 Resend 발송** | **①**(코드) + **②**(계약 테스트) + **④**(실제 발송) | `server/test/provider-contracts.test.mjs` |
| 로그인 후 기존 장소·진행 중 코스 유지, 첫 코스도 로그인 필요 | **①** | `scripts/test-purchase-flow.mjs` |
| **계정별 서버 저장 — 장소·코스 동기화·병합·로그아웃 격리** | **①** | `scripts/test-account-sync.mjs` 12개 |
| **결제 — 활성 이용권 있으면 중복 구매 서버가 직접 거부** | **①** | `server/test/reliability-and-cost.test.mjs`(4차 신규) |
| **결제 — 취소 시 계정 소유자 대조(ChatGPT 재현 버그 수정)** | **①** | `server/test/reliability-and-cost.test.mjs` — 다른 계정의 주문 취소 시도가 403으로 거부되고 실제 토스 API 호출 자체가 안 나감을 확인(4차 신규, 최우선 수정) |
| **결제 — 승인 응답 유실/타임아웃 시 조회 API로 재확인(무조건 실패 확정 금지)** | **①** | `server/test/reliability-and-cost.test.mjs`(4차 신규) — 네트워크 오류로 confirm이 실패해도 조회로 실제 DONE 상태를 확인해 성공 처리, 정말 실패면 주문을 `failed`로 낙인찍지 않고 재시도 가능하게 `pending` 유지 |
| **결제 — 동시 승인/취소 요청을 주문 단위 잠금으로 방지 + 만료 회수** | **①** | `server/test/reliability-and-cost.test.mjs`(4차 신규) |
| **결제 — 과거 주문 취소·지연 웹훅이 최신 유효 이용권을 안 건드림** | **①** | `server/test/reliability-and-cost.test.mjs`(4차 신규) — `accounts.active_order_id`로 "지금 이용권을 실제로 부여한 주문"을 추적, 그 주문이 아니면 취소·웹훅이 와도 회수 안 함 |
| **결제 — 중복/지연 웹훅 멱등 처리** | **①** | `server/test/reliability-and-cost.test.mjs`(4차 신규) — 같은 성공 웹훅이 두 번 와도 부작용 없음 |
| **결제 — 부분 취소 ≠ 전액 취소(자동 이용권 회수 안 함, 정책 미확정 별도 보고)** | **①**(코드) + 정책 미확정(`docs/BUSINESS_DECISIONS.md` 5-0절) | `server/test/reliability-and-cost.test.mjs`(4차 신규) — 소비자 화면에는 부분 취소 UI 자체가 없음(완성 안 된 기능을 노출하지 않음) |
| **결제 — 서비스 전체 월간 비용 예산 소진 시 신규 결제 자체를 차단** | **①** | `server/adapters/payment-toss.mjs`의 `isServiceCostBudgetExhausted`(4차 신규 — 유료 고객이 결제만 하고 코스를 못 만드는 상황 예방) |
| 결제 실패 후 작업 화면 복귀, 중복 클릭 방지 | **①** | `scripts/test-purchase-flow.mjs` |
| **운영(production) 환경 — 테스트 기능 완전 차단** | **①** | `server/test/production-boot.test.mjs` 8개 |
| "키 있음"과 "실제 연결 확인됨" 구분 표시 | **①** | `/api/health`의 `services` vs `verified` |
| 서비스별 독립 연결 상태 | **①** | `server/test/config.test.mjs` 18개 |
| 측정 이벤트(유입~구매 퍼널) | **①** | `server/test/server.test.mjs` |
| 한글/가나 완성형 폰트, 전송량 최적화 | **①** | `scripts/test-font-coverage.mjs` |
| cp1_→cs1_ 저장소 마이그레이션 | **①** | `scripts/test-storage-migration.mjs` |
| 소개 페이지 + 사전 신청 | **①** | `scripts/test-landing-page.mjs` 11개 |
| **비용 SKU 단가 — 공식 등급·달러 단가·계산용 환율·안전 여유 분리** | **①** | `server/config.mjs`의 `buildCostEstimateMicros`, `server/test/config.test.mjs` — Text Search **Pro**($32/1000)·Routes Compute Essentials($5/1000)/Pro($10/1000) 등급을 실제 사용 필드 기준으로 반영(5차), FX는 계산용 가정치(1,400원/$)로 명시 분리, 안전 여유(`costSafetyMarginRatio`)는 예산 상한에만 적용하고 단가 자체엔 안 섞음 |
| **비용 예약 — 세그먼트 전체를 하나의 트랜잭션으로 all-or-nothing 확정** | **①** | `server/cost-ledger.mjs`의 `chargeCostBatch`(`BEGIN IMMEDIATE`), `server/test/cost-budget-allornothing.test.mjs` — "첫 세그먼트는 예산 안에 들어도 전체 합계가 넘으면 그 무엇도 기록 안 함"을 실제로 재현·확인(5차, ChatGPT가 재현한 부분기록 버그 수정) |
| **계정별 월간 비용 한도 신설** | **①** | `server/config.mjs`의 `costBudget.perAccountMonthlyMicros` |
| **장소 조회 — 캐시 키에 지역 힌트 포함(동명 장소 캐시 오염 방지)** | **①** | `server/routes/places.mjs`, `server/test/reliability-and-cost.test.mjs` §6 — Tokyo/Kyoto 같은 질의·다른 지역 힌트가 서로 결과를 오염시키지 않고 각자 재조회됨을 확인(5차) |
| **장소 조회 — 동시 중복 요청 in-flight 병합** | **①** | 〃 — 완전히 같은 조건의 동시 요청 2건이 실제 외부 호출은 1건만 나감을 확인(5차) |
| **장소 조회 — 실패/설정미비 응답은 오래 캐시하지 않음** | **①** | `server/routes/places.mjs`의 `shouldCache` — `ok:true`·`not-found`만 캐시, `network-error`/`no-api-key-configured` 등은 캐시 안 함(5차) |
| **장소 조회 일괄 확인 화면 연결(`/api/places/lookup-batch`)** | **①** | `src/design/spots.js`의 `daBatchLookupFlow` — "오늘 동선"에서 좌표 미확인 장소가 있을 때만 노출, 가져오기·목록열람만으로는 절대 안 뜸(5차 신규 — 배치 서버와 화면을 실제로 연결) |
| **결제 — `paymentMatchesOrder` 필드 누락 시 무조건 통과하던 버그 수정 + paymentKey 대조 추가** | **①** | `server/adapters/payment-toss.mjs`, `server/test/reliability-and-cost.test.mjs`(a) — 주문번호·금액·통화·결제키 중 하나라도 없으면 불일치로 거부(5차) |
| **결제 — 여러 pending 주문으로 중복구매 차단 우회 방지(confirm-time 검사)** | **①** | 〃(b) — 두 주문이 경쟁적으로 만들어져도, 이미 활성 이용권을 부여한 다른 주문이 있으면 confirm 단계에서 토스 호출 전에 거부 |
| **결제 — 부분취소 판정을 공급자 응답(상태·잔액·누적취소액) 기준으로** | **①** | 〃(c) — 클라이언트가 부분 취소를 요청해도 공급자 응답이 잔액 0이면 전액으로 정확히 기록 |
| **결제 — 결제 가능 여부 판정 확장(일일예산·최소생성비·라우팅불가)** | **①** | `server/adapters/payment-toss.mjs`의 `isServiceUnavailableForNewSales` — 월간 한도 완전 소진뿐 아니라 일일 한도·최소 코스 생성비 부족·운영에서 경로 키 없음까지 신규 판매 차단 사유로 봄, 새 주문만 막고 진행 중인 confirm은 안 건드림 |
| **결제 — 이미 승인된 결제의 복구는 신규판매 차단과 분리** | **①** | 〃(e) — 같은 주문의 재확인(복구)은 차단 로직에 안 걸림을 확인 |
| **결제 — `createOrderRoute`가 실패 응답을 200으로 덮어쓰던 버그 수정** | **①** | `server/routes/payment.mjs`(f) |
| **타임아웃 — 응답 본문 읽기 단계까지 보호(헤더만 받고 끝나던 버그 수정)** | **①** | `server/net.mjs`의 `fetchWithTimeout`, `server/test/reliability-and-cost.test.mjs` §8(g) — 헤더 수신 후 본문 스트림이 영원히 멈추는 응답도 결국 타임아웃으로 정리됨을 재현 확인 |
| **초기 테스트 상품 — 무료체험 위치확인 최대 10곳+코스 1회, 유료 이용권 위치확인 최대 50곳+코스 30회(30일)** | **①**(6차 신규) | `server/entitlement-usage.mjs`, `server/config.mjs`의 `entitlementUsage` — 성공한 결과에만 차감(실패·추정 결과는 미차감), 이미 위치가 확인된 장소를 재조회해도 새 장소 한도를 안 깎음(영구 dedupe), `server/test/entitlement-usage.test.mjs`(14개)·`server/test/entitlement-course-limit.test.mjs`(6개)에서 확인. **이 숫자는 출시 전 검증된 시장가격이 아니라 초기 테스트 상품 스펙**(`docs/BUSINESS_DECISIONS.md` 참고) |
| **초기 테스트 상품 — 30일 유료 이용권은 주문(order_id) 단위로 집계, 달력월 경계와 무관** | **①**(6차 신규) | `server/entitlement-usage.mjs`의 `currentPeriod` — 사용량이 `accounts.active_order_id`에 고정된 버킷에 쌓여 달력월이 바뀌어도 초기화 안 됨, `server/test/entitlement-period-month-boundary.test.mjs`(8개)에서 1월→2월 경계를 직접 재현해 확인 |
| **내부 비용 안전상한 — 무료체험 계정 700원/유료 이용권 3,500원(고객 잔액 표시 아님)** | **①**(6차 신규) | `server/config.mjs`의 `costSafetyCap`, `server/cost-ledger.mjs`의 `chargeCostBatch` — 실제 원가가 이 상한을 넘으려는 순간 그 이상의 실제 유료 호출 자체를 막음, `server/test/cost-safety-cap.test.mjs`(3개)에서 확인. 현재 단가 기준 계산상 무료체험 최악 사용량은 약 455원, 유료 최악은 약 2,450원으로 상한 안에 들어오지만(`docs/BUSINESS_DECISIONS.md` 참고), 실제 공급자 단가가 바뀌면 이 여유가 줄어들 수 있어 계속 지켜봐야 함 |
| **신규 판매 가능 여부 — 전체 예산에서 기존 유료 고객에게 약속된 잔여 몫까지 먼저 뺀 뒤 판단** | **①**(6차 신규) | `server/adapters/payment-toss.mjs`의 `totalCommittedRemainingMicros`/`isServiceUnavailableForNewSales` — 활성 유료 고객이 늘어 약속된 몫 합계가 예산을 압박하면 새 결제를 거부(이미 쓴 것은 계산에 안 들어감, 이용권 만료는 계산에서 빠짐), `server/test/new-sale-committed-budget.test.mjs`(3개)에서 확인 |
| **구매 화면 — 가격·기간·자동갱신 여부·포함 사용량 표시** | **①**(6차 신규) | `src/design/spots.js`의 `showPaywallSheet`, `server/routes/entitlement.mjs`의 `priceWithIncludedUsage` — 서버 설정값을 그대로 실어 보내 화면에 하드코딩된 숫자가 없음, `scripts/test-purchase-flow.mjs`에서 실제 화면 텍스트로 확인 |
| **계정(프로필) 화면 — 남은 위치확인·코스생성 횟수 표시(API/SKU 등 개발 용어 없이)** | **①**(6차 신규) | `src/design/spots.js`의 `profile()`, `GET /api/account/usage` — "남은 위치 확인: N곳(전체 M곳 중)" 형태로 사람 말로 표시, `scripts/test-purchase-flow.mjs`에서 실제 서버 사용량과 화면 텍스트가 일치함을 확인 |
| **재방문 여행자 — 여행 목록·전환 화면(같은 도시 다른 여행도 카드로 구분)** | **①**(6차 — 화면 연결 완료) | `src/design/spots.js`의 `tripPickerSheet`/`chooseTrip`/`tripBlockHTML`, `scripts/test-revisit-flow.mjs` — 같은 도시에 여행이 2개 이상이면 "오늘 동선"·"오늘의 코스" 화면에 지금 여행 이름이 표시되고 "바꾸기"로 카드 목록에서 고를 수 있음을 실제 화면에서 확인. 여행이 하나뿐이면(마이그레이션된 기존 계정 포함) 예전과 완전히 같은 화면 |
| **재방문 여행자 — 새 여행 만들기(이름·날짜·숙소, 과거 여행 보존)** | **①**(6차) | `src/design/spots.js`의 `newTripFormSheet`, `scripts/test-revisit-flow.mjs` — 같은 도시로 새 여행을 만들어도 여행이 2개로 늘어나고 과거 여행의 코스가 그대로 남아 있음을 확인, `scripts/test-sync-protection-screens.mjs` (f) — 서버 오류로 만들기가 실패하면 성공한 것처럼 안 넘어가고 화면에 남아 재시도로 성공함을 확인 |
| **재방문 여행자 — 방문 표시 버튼(방문완료·취소·다시가고싶음)·방문 상태 필터·반복 방문일/메모** | **①**(6차) | `src/design/spots.js`의 `visitBlockHTML`/`visitAction`, `#visitFilters`(전체/미방문/방문함/다시가고싶음) — 장소 상세에서 코스에 담는 것과 완전히 분리된 버튼으로 확인, 방문 기록이 하나라도 생기면 목록 화면에 방문 상태 필터가 나타나 실제로 좁혀짐을 `scripts/test-revisit-flow.mjs`에서 확인 |
| **재방문 여행자 — 다음 여행 코스 후보 제안 화면 연결(이월, 새 화면 안 만듦)** | **①**(6차) | `src/design/spots.js`의 `carryForwardSheet`(기존 "오늘 동선" 화면에 얹음) — 같은 도시에 다른 여행이 있고 동선이 비어 있을 때만 "지난 여행에서 담아 둔 곳 이어가기" 진입점이 뜨고, 미방문 우선으로 정렬된 후보를 실제로 오늘 동선에 담을 수 있음을 확인. 사용자가 이미 고른 곳은 이 목록과 무관하게 항상 코스에 반영됨(서버가 애초에 필터링 안 함) |
| **재방문 여행자 — 코스 생성이 현재 tripId를 실어 그 여행 아래 저장** | **①**(6차) | `src/design/spots.js`의 `ensureTripForCity`/`runRealCourseGeneration` — 여행이 없는 새 도시는 조용히 하나 자동 생성(진입장벽 없음), 두 여행에서 각각 만든 코스가 서버에서 완전히 분리 저장되고 서로 안 섞임을 `GET /api/trips/:tripId/courses`로 직접 확인 |
| **재방문 여행자 — 무료/유료 경계(기록 읽기·쓰기는 항상 무료)** | **①** | `server/test/trips-and-visits.test.mjs` §4 — 여행 여러 개 생성·도시 전환·방문 기록 읽기/쓰기를 반복해도 무료체험이 전혀 안 깎이고, 유료 이용권이 없어도 방문 기록 API가 그대로 동작함을 확인 |
| **재방문 기록 보호 동기화 — 버전 확인 후 충돌 시 재병합(조용한 전체 덮어쓰기 방지) + 로그인/저장/로그아웃 실제 연결** | **①**(6차 — 화면 연결 완료) | `server/routes/trips.mjs`의 `syncTrips`, `server/routes/visits.mjs`의 `syncVisits`, `server/test/trips-visits-sync.test.mjs`(서버 5개 시나리오) — 클라이언트 쪽은 `src/design/spots.js`의 `daSyncPush`가 로그인 직후(`daSyncPullAndMerge`)와 매 로컬 저장마다(`daSyncPushSafe`) `/api/trips/sync`·`/api/visits/sync`를 실제로 부르도록 연결(예전엔 이 엔드포인트를 클라이언트가 아예 안 불렀다), 로그아웃 시 `foodMap.trips`/`foodMap.visits` 로컬 삭제. `scripts/test-sync-protection-screens.mjs` (b)(c)(e) — 서로 다른 기기의 여행 추가가 둘 다 보존됨, 방문 취소 후 오래된 기기 재연결에도 안 되살아남, 계정 전환/로그아웃 시 다른 계정 데이터가 안 새어나감을 실제 화면에서 확인 |
| **`/api/places`·`/api/courses` 전체치환 데이터손실 — 해결됨(7차)** | **①**(7차 — 6차에서 발견만 하고 남겨 뒀던 것을 실제로 고침) | `server/routes/account-data.mjs`의 `syncPlaces`/`syncCourses` — trips/visits와 같은 버전 비교+보수적 병합을 적용(뒤처진 기기의 수정은 조용히 덮어쓰지 않고 `conflicts`로 보고). 삭제는 명시적 `deletedIds`로만 이뤄지고(배열 누락으로 추측 삭제 안 함), 이미 지워진 장소는 오래된 기기가 다시 들고 나타나도 안 되살아남(무덤 표시). `server/test/places-courses-sync.test.mjs`(16개), `scripts/test-sync-protection-screens.mjs` (a) — 이제 실제로 통과함(6차엔 여기서 실패했다) |
| **신규 장소 확인 한도 우회 차단(7차)** | **①**(7차) | `server/entitlement-usage.mjs`의 예약(reserve)/확정(finalize) 2단계 — 예전엔 "신규 여부"를 클라이언트가 불러주는 로컬 장소 식별자만으로 판정해, 같은 식별자에 검색어만 바꿔 보내면 서로 다른 실제 장소를 무료로 확인받는 우회가 가능했다(ChatGPT가 실제로 재현: 한도 1로 두고 6곳을 확인받았는데 사용량은 1로만 기록). 이제 공급자가 실제로 돌려준 장소 식별자로 재판정한다(같은 로컬 id+다른 검색어→실제로 다른 장소면 각각 과금, 다른 로컬 id+같은 실제 장소→비과금). `server/test/place-lookup-entitlement-bypass.test.mjs`(16개, 재현 조건 3가지 전부) |
| **하루 비용 한도와 유료 이용권 약속의 충돌 해결(7차)** | **①**(7차) | `server/cost-ledger.mjs`의 `chargeCostBatch` — 계정별 하루 비용 한도(기본 500원)가 무료/유료 구분 없이 적용돼, 위치확인 단가(약 44.8원) 기준 11건 만에 소진돼 유료 이용권의 "하루 50곳" 약속이 애초에 불가능했다. 이제 이용권 기간 전체 누적 안전상한(무료 700원/유료 3,500원)보다 낮게 잡히지 않는다 — 이용권 몫을 하루 안에 몰아 써도 되지만 총량은 여전히 이용권 기간 누적 한도가 지킨다. `server/test/paid-daily-burst-budget.test.mjs`(11개, 출고 기본값 그대로 유료 고객 50곳+코스 1회를 하루에 마침을 확인) |
| **착장 판단용 날씨 카드(7차 신규)** | **②**(모의 검증 — WeatherAPI.com 실제 키 미연결) | `server/adapters/weather.mjs`, `server/routes/weather.mjs`(`/api/weather`), `src/design/weather-card.js` — "오늘 동선" 화면 상단에 현재 날씨·오늘 예보(최고/최저/저녁)·시간대별 강수확률/바람·규칙 기반 착장 문구를 보여준다. 로그인 불필요(이용권/비용 원장 전혀 안 건드림), 지역 단위(~1km) 캐시 공유로 도시 선택마다 새 호출 안 함, 실패 시 마지막 캐시를 시각과 함께 대체. `server/test/weather-route.test.mjs`(12개)·`weather-stale-fallback.test.mjs`(6개), `scripts/test-weather-card.mjs`(13개, 실제 Chromium) |
| **동기화 충돌 보호 재설계 — baseVersion(8차)** | **①**(8차 — ChatGPT 재현 결함 수정) | `server/routes/account-data.mjs`(`syncPlaces`/`syncCourses`)·`trips.mjs`·`visits.mjs` — 예전엔 `incomingVersion >= existing.version`이면 수락해, 같은 원본에서 동시에 편집한 두 기기가 둘 다 "버전+1"을 보내면 뒤에 도착한 기기가 앞 기기의 수정을 조용히 지웠다. 이제 서버만 새 버전을 발급하고 `baseVersion === existing.version`(정확히 일치)일 때만 수락 — 불일치는 전부 충돌로 보고하고 서버의 현재 값을 함께 돌려준다. 클라이언트(`src/design/import-adapter.js`·`spots.js`)는 로컬에서 버전을 더 이상 임의로 올리지 않고, 충돌이 나면 3-way 재병합(`daRemergePlaceConflict` — 내가 안 건드린 필드는 서버 값 채택, 내가 고친 필드는 유지)으로 양쪽 수정을 모두 보존한 뒤 자동 재시도한다. 삭제도 `{id, baseVersion}`로 같은 보호를 받는다(뒤처진 기기의 삭제 의도가 방금 확정된 좌표를 지우지 못함). `server/test/sync-conflict-protection.test.mjs`(24개), `scripts/test-sync-conflict-devices.mjs`(9개, 실제 Chromium 두 기기 — 동시 다른 필드 편집·수정vs삭제 충돌·저장 중 추가 편집 3가지 실화면 재현) |
| **장소확인 빠른경로 재판정(8차)** | **①**(8차 — ChatGPT 재현 결함 수정) | `server/entitlement-usage.mjs` — 예전엔 "같은 로컬 슬롯+같은 검색 지문"이면 빠른 경로로 곧바로 "신규 아님" 처리하고, 실제로 돌아온 장소가 바뀌었는지 전혀 대조하지 않았다(캐시 만료 후 재조회로 공급자가 실제 다른 장소를 줘도 링크만 바뀌고 사용량은 그대로였다 — ChatGPT 재현). 이제 빠른 경로라도 실제 반환 `placeId`를 매번 대조해, 바뀌었으면 신규 판정 절차를 다시 거친다. 한도가 이미 다 찬 상태에서도 reserve가 곧바로 거절하지 않고 finalize까지 판정을 미뤄, "다른 로컬 id로 이미 확인된 실제 장소"의 재사용은 한도와 무관하게 여전히 성공한다. 잠정 예약(사용량 +1)이 서버 프로세스 종료로 finalize 없이 유실돼 영구 소비되던 문제도 `entitlement_place_reservations` 표(generation_locks와 같은 만료 회수 패턴)로 고쳤다. `server/test/place-lookup-fast-path.test.mjs`(12개, 재현 조건 그대로), `server/test/place-lookup-reservation-recovery.test.mjs`(17개, 프로세스 종료 시뮬레이션 포함) |
| **날씨 시간대 버그 수정(8차)** | **①**(8차 — ChatGPT 재현 결함 수정, 파싱 로직 자체는 실제 검증됨) | `server/adapters/weather.mjs`, `server/routes/weather.mjs` — 예전엔 공급자의 `last_updated`·`hour.time`(시간대 표기 없는 "목적지 현지 시각" 문자열)을 `new Date()`로 그대로 파싱해, 시간대 표기 없는 문자열이 "서버의 로컬 시간대"로 해석되는 규격 특성상 서버가 UTC로 돌면 일본 현지 21시가 "21:00Z"로 잘못 해석되고 화면에서 다시 +9시간 밀려(다음날 새벽) 표시됐다. 이제 공급자의 epoch 필드(`last_updated_epoch`·`hour.time_epoch`·`location.localtime_epoch`)만으로 절대 시각을 만들고, "오늘인지"·"지금 몇 시인지"도 서버 시간대가 아니라 목적지의 `tz_id`로 재지역화해 계산한다. 사용자가 고른 여행 날짜의 예보와 오늘의 현재값을 서버가 분리해 내려주고(공급자가 실제 보장하는 3일 밖 날짜는 오늘 값을 그 날짜인 척 보여주지 않고 정직하게 범위 밖 표시), 위경도 범위 검증과 동일 지역 동시요청 병합도 추가했다. `server/test/weather-timezone.test.mjs`(16개 — 도쿄·방콕·뉴욕·현지 자정 경계·날짜 커버리지·동시요청 병합을 `globalThis.fetch` 모의로 실제 파싱 로직까지 실행해 검증. WeatherAPI.com 실제 키 연결 자체는 여전히 미검증) |
| **현지 거리·옷차림 영상(8차 신규, 범위 제한)** | **②**(모의 검증 — 실제 영상 미등록, 아래 참고) | `src/design/street-video.js` — 날씨 카드 아래 "현지 거리 · 옷차림 보기" 버튼. 클릭했을 때만 공식 유튜브 임베드 플레이어(`youtube.com/embed`, `autoplay=0`)를 불러오고(미리 로드 안 함), 닫으면 iframe `src`를 비워 재생을 실제로 멈춘다. 촬영 장소·제공 채널·현지 시간과 "영상 시청 시 데이터가 사용돼요" 안내를 표시하고, "현지인"이라 단정하지 않고 "현지 거리의 옷차림"으로만 표현한다. LIVE 상태를 직접 단정하는 배지는 아예 안 만든다. IFrame Player API(`onError`)로 임베드 거부·방송 종료를 실제로 감지해 원본 유튜브 링크로 대체한다. 서버가 영상을 저장·중계하지 않는다(서버 라우트 자체가 없음). **`STREET_VIDEOS`(도시별 검증된 영상 표)는 빈 표로 배포됨** — 실제 방송 존재·임베드 가능 여부·카메라 각도(옷차림이 보이는지)를 이번 세션에서 검증할 방법이 없어(가짜 videoId를 지어내지 말라는 지시) 검증된 영상이 없는 모든 도시에서 버튼이 실제로 숨겨진다. `scripts/test-street-video.mjs`(22개, 실제 Chromium) — 이 테스트가 직접 주입한 합성(명백히 테스트 전용) 픽스처로 플레이어 구조·표시 문구·닫기 동작·오류 대체 화면만 검증했다. **실제 영상 등록은 사람이 직접 방송을 확인한 뒤 별도로 진행해야 하는, 아직 안 끝난 일이다.** |

**요약**: ChatGPT가 4차·5차·6차·7차에서 지적한 문제는 모두 실제 코드
수정 + 재현 테스트 통과까지 끝났다. 이번 8차에서는 7차 ZIP을 실제로
재현·검토해 드러난 동기화 충돌 보호·장소확인 빠른경로·날씨 시간대
세 가지 결함을 전부 재현 테스트부터 작성해 고쳤고, 범위를 제한한
현지 거리·옷차림 영상 기능을 신규로 추가했다(실제 영상 등록은
미검증 — 위 표 참고). 자세한 내용은 3-3절.

**요약(7차)**: ChatGPT가 4차·5차·6차에서 지적한 문제는 모두 실제 코드 수정
+ 재현 테스트 통과까지 끝났다. 이번 7차에서는 (1) 6차에서 발견만
하고 남겨 뒀던 `/api/places`·`/api/courses` 전체치환 데이터손실을
실제로 고쳤고(trips/visits와 같은 버전 보호), (2) ChatGPT가 재현한
신규 장소 확인 한도 우회를 막았으며(공급자의 실제 확정 결과로
재판정), (3) 계정별 하루 비용 한도가 무료/유료 구분 없이 적용돼 유료
이용권의 "하루 50곳" 약속을 사실상 불가능하게 만들던 버그를
고쳤고, (4) 착장 판단용 날씨 카드를 신규로 추가했다(WeatherAPI.com
무료 등급, 서버가 키를 들고 소비자는 입력 안 함). 남은 ③(실제 키
연결) 미검증은 이전 차수와 동일하게 다섯 공급자(Toss/Resend/Google
Places/Google Routes/WeatherAPI)의 네트워크 제약 때문이며, 코드가
없어서가 아니다(2절 참고).

---

## 2. 이번 세션에서 못 하는 검증 — 사용자가 직접 확인할 최소 절차

### 2-1. 실제 키로 첫 연결 확인(가장 중요 — 3차와 동일하게 여전히 미검증)

- **상태**: Resend/토스페이먼츠/Google Places+Routes/WeatherAPI
  다섯 서비스 모두 실제 키로 검증된 적이 없다(WeatherAPI는 7차
  신규). 이번 세션도 공식 문서 사이트 전부 접속이 막혀 있었다
  (EGRESS_BLOCKED — weatherapi.com도 마찬가지로 확인 안 됨,
  `www.weatherapi.com/pricing.aspx`·`/docs/`의 요금·형식은 ChatGPT가
  확인해 전달한 내용 기준).
- **확인 절차**: `docs/BUSINESS_DECISIONS.md` 6절 표대로 키를 하나씩
  넣어 가며 `/api/health`의 `services`/`verified`와 실제 화면 동작을
  확인한다.
- **특히 4차에서 새로 생긴 확인 포인트**: 토스 승인 응답에
  `totalAmount`/`currency` 필드가 실제로 우리가 가정한 이름·형식으로
  오는지(`paymentMatchesOrder` 함수가 이 이름을 그대로 씀 —
  다르면 정상 승인도 위조 의심으로 거부될 수 있다), Places API(New)의
  `v1/places:searchText` 응답이 `places[].location.latitude/longitude`
  형식이 맞는지, Routes 경유지 25개 상한이 실제로 그 숫자가 맞는지
  (ChatGPT 확인 기준이지 이 세션이 재확인한 게 아니다).

### 2-2. 실제 Google Takeout 데이터

- **상태**: 179건 실사용 데이터셋이 이 샌드박스에 없어 재실행 못 함.

### 2-3. 모바일 Safari(iOS)

- **상태**: 실제 iPhone·Safari 미검증. 4차에서 새로 추가된 배치
  조회·비용 한도 초과 메시지가 실기기 화면에서 자연스럽게 보이는지도
  함께 확인 필요.

### 2-4. 느린 네트워크

- **상태**: 폰트 로딩만 검증됨. 4차에서 추가된 외부 요청 타임아웃
  (`EXTERNAL_REQUEST_TIMEOUT_MS`, 기본 8초)이 실제로 느린 네트워크에서
  사용자 경험에 어떤 영향을 주는지(너무 짧아 정상 요청도 끊기는 건
  아닌지)는 실기기·실네트워크 확인이 필요하다.

### 2-5. 현지 거리·옷차림 영상 — 실제 영상 등록(8차 신규, 출시 전 필수)

- **상태**: 미검증·미등록. `src/design/street-video.js`의
  `STREET_VIDEOS`가 빈 표라 지금은 **모든 도시에서 버튼 자체가
  보이지 않는다**(의도된 안전한 기본값 — 검증 안 된 영상을 지어내지
  않기 위해). 실제 방송 존재·현재 임베드 가능 여부·카메라 각도(사람
  옷차림이 실제로 잘 보이는지)를 이번 세션은 실제로 볼 방법이 없어
  검증하지 못했다.
- **해야 할 일(사람이 직접)**: 도시별로 촬영 장소가 명확하고 임베드가
  허용된 유튜브 영상을 최소 1개씩 직접 재생해서 확인한 뒤,
  `src/design/street-video.js`의 `STREET_VIDEOS` 표에 `{videoId,
  title, channel, filmingLocation, tzId, sourceUrl}`로 추가한다.
  실시간 방송이면 방송이 끝나거나 채널이 영상을 내리면 그 즉시
  깨지므로(IFrame Player API가 `onError`로 감지해 원본 링크로는
  대체하지만, "볼 수 있는 영상"이 아예 없어지는 건 막지 못한다) 정기
  점검이 필요하다는 점도 함께 남긴다.
- **9차 재검토 — 안정성 보강(재현된 버그 수정)**: `loadIframeApi`가
  스크립트 로드 실패(`onerror`)·응답 없음(타임아웃, 8초) 둘 다 처리가
  전혀 없어서, 느린 해외 연결이나 광고 차단 환경에서 패널이 "닫기"
  버튼도 못 뜬 채 영원히 빈 채로 남을 수 있었다 — 실제로 재현해 확인한
  뒤 둘 다 원본 링크 안내로 정직하게 대체되게 고쳤다. 또한 패널을 열
  때마다 세대(generation) 번호를 올려, 닫기→다른 영상 빠른 재열기
  사이에 첫 번째 열기의 응답이 뒤늦게 도착해도 그 낡은 콜백이 고정
  id를 쓰는 새 iframe에 몰래 올라타 중복 `YT.Player`를 만드는 사고를
  막았다(전부 `scripts/test-street-video.mjs`의 8~10번에서 실제
  재현으로 확인). "항상 원본 링크를 함께 제공"하라는 지시에 맞춰,
  임베드 시도 중에도 원본 유튜브 링크를 패널에 상시 노출하도록 바꿨다.

---

## 3. 이번 4차 갱신에서 고친 문제 목록(ChatGPT 재현 기준)

| # | 지적된 문제 | 고친 내용 | 재현 테스트 |
|---|---|---|---|
| 1 | 운영+경로키없음에서 `routedReal:true` 가짜 성공 생성 가능 | `services.routing==='test'`일 때만 시뮬레이터 호출, `'unavailable'`이면 절대 안 부름 | `server/test/reliability-and-cost.test.mjs` §1 |
| 2 | `/api/payment/cancel`이 계정 대조 없이 취소 | accountId로 주문 소유자 필수 대조 | 〃 §2 |
| 3 | 승인 응답 유실 시 무조건 실패 처리 | 조회 API로 재확인 후 판단, 미확정이면 `pending` 유지 | 〃 §1-2 |
| 4 | 부분 취소=전액 취소로 혼동 가능 | 상태·이용권 처리 분리, UI엔 미노출 | 〃 §2 |
| 5 | 과거 주문 취소가 최신 이용권 훼손 가능 | `active_order_id`로 주문별 권리 관리 | 〃 §2 |
| 6 | 중복/지연 웹훅으로 이중 지급·오취소 가능 | 상태 전이 멱등화 + 주문 단위 잠금 | 〃 §2 |
| 7 | 결제 승인 동시 요청 시 중복 호출 가능 | order 단위 잠금(만료 회수 포함) | 〃 §3 |
| 8 | 체험 차감·코스 저장이 트랜잭션으로 안 묶임 | 하나의 BEGIN/COMMIT/ROLLBACK으로 통합 | 〃 §4 |
| 9 | generation_locks에 만료·소유권 검증 없음 | job_id 소유권 + 시간 기반 회수 | 〃 §4 |
| 10 | 같은 멱등키·다른 요청 본문 미검출 | request_hash 비교 후 충돌(409) | 〃 §4 |
| 11 | 좌표·장소개수·시간예산 서버 미검증 | 범위·상한 검증 추가 | 〃 §4 |
| 12 | Legacy Find Place 첫 후보 그대로 확정 | Places API(New) + 지역 힌트 판별 | `server/test/provider-contracts.test.mjs` |
| 13 | phase 파라미터가 클라이언트 자기 신고 | 단일 조회는 항상 requery 한도, 큰 한도는 배치 엔드포인트 전용 | `server/test/server-v2.test.mjs` |
| 14 | 장소 조회 계정별 호출 한도 자체가 무의미(재확인) | 캐시 dedup + 계정별/전체 순서 수정 | 〃, `reliability-and-cost.test.mjs` §6 |
| 15 | API 비용을 서버가 전혀 통제 안 함 | SKU별 비용 원장 + 일일/월간 한도(chargeCost) | 〃 §7 |
| 16 | Routes 경유지 25개 상한 미반영 | 세그먼트 분할(경계 보존) + 11개 이상 고요금 SKU | `server/test/routes-waypoint-limits.test.mjs` |
| 17 | 예산 부족 시 일부만 실제·일부만 추정으로 뒤섞일 위험 | all-or-nothing(예산 부족하면 전부 추정) | `server/test/cost-budget-allornothing.test.mjs` |
| 18 | 활성 이용권 있어도 중복 결제 가능 | createOrder가 서버에서 직접 거부 | `reliability-and-cost.test.mjs` §2 |
| 19 | 서비스 전체 예산 소진 상태에서도 결제만 받을 수 있음 | 월간 예산 소진 시 신규 주문 자체를 차단 | `payment-toss.mjs`의 `isServiceCostBudgetExhausted` |

---

## 3-1. 이번 5차 갱신에서 고친 문제 목록(ChatGPT 재현 기준)

| # | 지적된 문제 | 고친 내용 | 재현 테스트 |
|---|---|---|---|
| 1 | 비용 SKU가 최저 등급으로 잘못 추정(실제는 Text Search Pro 등급) | 공식 등급·달러 단가·계산용 환율·안전 여유를 분리해 설정, 실제 요청 필드 기준으로 등급 반영 | `server/test/config.test.mjs` |
| 2 | `chargeCost`가 세그먼트별로 따로 불려 부분기록(실제 호출 0번인데 비용 1건 남음) | `chargeCostBatch`로 전체 합계를 하나의 트랜잭션(`BEGIN IMMEDIATE`)에서 확인 후 all-or-nothing 기록 | `server/test/cost-budget-allornothing.test.mjs` |
| 3 | 계정별 월간 비용 한도 없음 | `costBudget.perAccountMonthlyMicros` 신설 | `server/test/config.test.mjs` |
| 4 | 장소 조회 캐시 키가 지역을 안 담아 동명 장소 캐시 오염(Tokyo→Kyoto) | 캐시 키에 `expectedArea` 포함 | `server/test/reliability-and-cost.test.mjs` §6 |
| 5 | 동시에 같은 질의를 보내면 중복 외부 호출 발생 | in-flight 요청을 하나의 Promise로 병합 | 〃 §6 |
| 6 | 실패/설정미비 응답이 정상 결과처럼 오래 캐시됨 | `shouldCache`로 안정적인 답(`ok`/`not-found`)만 캐시 | `server/routes/places.mjs` |
| 7 | `/api/places/lookup-batch`가 서버엔 있지만 화면에서 안 불림 | `src/design/spots.js`의 `daBatchLookupFlow`로 "오늘 동선" 화면에 연결(가져오기·목록열람만으론 안 뜸) | `src/design/spots.js` |
| 8 | `paymentMatchesOrder`가 필드 누락 시 검사를 건너뛰고 통과시킴 | 누락 시 불일치로 처리, `paymentKey` 대조 추가 | `server/test/reliability-and-cost.test.mjs`(a) |
| 9 | 여러 pending 주문을 만들어 개별 confirm하면 중복구매 차단 우회 가능 | confirm-time에도 활성 이용권(다른 주문) 검사 추가 | 〃(b) |
| 10 | 부분취소 판정을 요청값(cancelAmount)만으로 결정 | 공급자 응답의 상태·잔액·누적취소액 기준으로 판정 | 〃(c) |
| 11 | 결제 가능 여부 판정이 월간 예산 완전소진만 봄 | 일일 한도·최소 생성비 부족·라우팅 서비스 불가까지 신규판매 차단 사유로 확장 | 〃(d) |
| 12 | 신규판매 차단 로직이 이미 승인된 결제 복구와 안 분리됨 | 조기 `alreadyProcessed` 반환이 (b)/(d) 검사보다 먼저 오도록 구조적으로 분리 | 〃(e) |
| 13 | `createOrderRoute`가 실패 응답의 status를 200으로 덮어씀 | 실패(`!r.ok`)면 원래 status를 그대로 반환 | 〃(f) |
| 14 | `fetchWithTimeout`이 헤더만 받으면 타이머를 꺼서 본문 읽기 단계가 무보호 | 본문 읽기(`json`/`text`/`arrayBuffer`)도 각각 새 타이머로 보호 | 〃 §8(g) |

---

## 3-2. 이번 7차 갱신에서 고친 문제 목록(ChatGPT 재현 기준)

| # | 지적된 문제 | 고친 내용 | 재현 테스트 |
|---|---|---|---|
| 1 | `/api/places`·`/api/courses`가 전체 삭제 후 재삽입 — 오래된 기기의 저장이 최신 수정을 덮어씀(6차에서 발견만 하고 미해결) | `account_places`/`account_courses`에 `version`/`deleted` 컬럼 추가, trips/visits와 같은 버전 비교+보수적 재병합(`syncPlaces`/`syncCourses`). 삭제는 명시적 `deletedIds`로만(배열 누락으로 추측 삭제 안 함) | `server/test/places-courses-sync.test.mjs`(16개), `scripts/test-sync-protection-screens.mjs` (a) — 이제 실제로 통과 |
| 2 | `daSyncPush`가 일부 응답 실패를 확인 안 하고, `daLogout`이 동기화 완료 보장 없이 로컬 데이터를 삭제 | `daSyncPush`가 4개 채널(places/courses/trips/visits) 응답을 각각 확인해 성공한 채널만 반영, `daLogout`이 로그아웃 전 마지막 동기화를 시도하고 실패 시 사용자 확인을 받음. 계정 전환 중 응답 뒤섞임 방지(세션 epoch), 겹치는 push 응답 순서 보호(push-seq) | `server/test/places-courses-sync.test.mjs`, `scripts/test-sync-protection-screens.mjs` 전체 |
| 3 | 같은 클라이언트 장소 식별자에 검색어만 바꿔 보내면 서로 다른 실제 장소를 무료로 확인받는 우회(무료 한도 1로 재현: 6곳 확인, 사용량 1로만 기록) | "신규 여부"를 클라이언트 문자열이 아니라 공급자가 실제로 돌려준 장소 식별자(real_place_id)로 재판정(예약→호출→확정 2단계). 같은 실제 장소 재사용은 계속 비과금, 다른 클라이언트 id가 같은 실제 장소로 확인돼도 비과금 | `server/test/place-lookup-entitlement-bypass.test.mjs`(16개 — 같은 id+다른 검색어, 다른 id+같은 실제 장소, 동시 성공/실패/중복 요청 전부 재현) |
| 4 | 계정별 하루 비용 한도(500원)가 무료/유료 구분 없이 적용돼, 위치확인 단가(약 44.8원) 기준 11건 만에 소진 — 유료 이용권의 "하루 50곳" 약속이 사실상 불가능 | 이용권 기간 전체 누적 안전상한(무료 700원/유료 3,500원)보다 계정별 일일/월간 한도가 낮게 잡히지 않게 함(`chargeCostBatch`). 짧은 시간창 남용 방지는 여전히 별도(rate-limit.mjs의 횟수 기반 한도)가 맡음 | `server/test/paid-daily-burst-budget.test.mjs`(11개 — 출고 기본값 그대로 유료 고객이 하루에 50곳+코스 1회를 실제로 마침을 확인) |
| 5 | 이용권 잔여는 있는데 내부 원가 안전상한에 걸릴 때 "cost-budget-exceeded"라는 뭉뚱그린 오류만 나감 | "이용권 자신의 원가 상한 도달"(`entitlement-cost-cap-reached`)과 "서비스 전체 운영상 일시 제한"(`cost-budget-exceeded`)을 구분해서 반환 | `server/test/cost-safety-cap.test.mjs` |
| 6 | 착장 판단용 날씨 기능 없음(신규 요청) | "오늘 동선" 화면 상단에 날씨 카드 추가(WeatherAPI.com 무료 등급) — 현재 날씨·오늘 예보(최고/최저/저녁)·시간대별 강수확률/바람·규칙 기반 착장 문구. 로그인 불필요, 이용권/비용 원장 전혀 안 건드림, 지역 단위 캐시 공유(도시 선택마다 새 호출 안 함), 실패 시 마지막 캐시로 정직하게 대체 | `server/test/weather-route.test.mjs`(12개)·`weather-stale-fallback.test.mjs`(6개), `scripts/test-weather-card.mjs`(13개, 실제 Chromium) |

---

## 3-3. 이번 8차 갱신에서 고친 문제 목록(ChatGPT 재현 기준)

| # | 지적된 문제 | 고친 내용 | 재현 테스트 |
|---|---|---|---|
| 1 | `account_places` 동기화가 `incomingVersion >= existing.version`이면 수락 — 같은 원본에서 시작한 두 기기가 동시에 편집(둘 다 "버전+1"을 보냄)하면 뒤에 도착한 기기가 앞 기기의 수정을 조용히 지움(`conflicts`가 빈 배열로 재현됨) | 서버만 새 버전을 발급하고 `baseVersion === existing.version`(정확히 일치)일 때만 수락. 클라이언트는 로컬에서 버전을 임의로 올리지 않고(`_stampPlaceVersions` 재설계), 충돌 시 3-way 재병합(`daRemergePlaceConflict`)으로 양쪽 수정 보존 후 자동 재시도. 같은 내용 재전송은 버전을 안 올림(no-op 감지). 삭제도 `{id, baseVersion}`로 같은 보호 적용, 코스/여행/방문 동기화도 같은 `===` 비교로 통일 | `server/test/sync-conflict-protection.test.mjs`(24개 — 정확 버전 경합, 미래 버전 사칭 거부, no-op 무증분, 삭제vs수정 충돌, 코스/여행/방문 경합), `scripts/test-sync-conflict-devices.mjs`(9개, 실제 Chromium 두 기기 — 동시 다른 필드 편집·수정vs삭제·저장 중 추가 편집) |
| 2 | "같은 로컬 슬롯+같은 검색 지문"이면 빠른 경로로 곧바로 "신규 아님" 처리 — 실제로 돌아온 장소가 바뀌었는지 전혀 대조 안 함(무료 한도 1로 재현: actual-A 확인 후 같은 조건의 다음 결과가 actual-B로 바뀌어도 링크만 바뀌고 사용량은 그대로 1) | 빠른 경로라도 실제 반환 `placeId`를 매번 대조 — 바뀌었으면 신규 판정을 처음부터 다시 거침(한도 없으면 성공 처리 안 함). 한도가 다 찬 상태에서도 reserve가 곧바로 거절 안 하고 finalize까지 판정을 미뤄 "다른 로컬 id+같은 실제 장소" 비과금 재사용이 한도와 무관하게 여전히 성공. `finalizePlaceLookupResult`가 이제 `{ok, reason}`을 반환해 이미 성공한 외부 호출도 한도초과로 뒤집어 402 응답 가능. 잠정 예약이 프로세스 종료로 유실돼 영구 소비되던 문제도 `entitlement_place_reservations` 표(만료 회수 패턴)로 고침 | `server/test/place-lookup-fast-path.test.mjs`(12개, 재현 조건 그대로), `server/test/place-lookup-reservation-recovery.test.mjs`(17개, 프로세스 종료 시뮬레이션) |
| 3 | `server/adapters/weather.mjs`가 공급자의 `last_updated`·`hour.time`(시간대 표기 없는 "현지 시각" 문자열)을 `new Date()`로 그대로 파싱 — 서버가 UTC로 돌면 일본 현지 12:00이 "12:00Z"로 잘못 해석돼 화면에서 21:00으로 표시(재현 확인) | epoch 필드(`last_updated_epoch`·`hour.time_epoch`·`location.localtime_epoch`)만으로 절대 시각 생성, "오늘인지"·"지금 몇 시인지"도 목적지 `tz_id`로 재지역화. 사용자가 고른 여행 날짜의 예보와 오늘의 현재값을 서버가 분리해 내려주고, 공급자가 보장하는 3일 밖 날짜는 정직하게 범위 밖 표시. 위경도 범위 검증과 동일 지역 동시요청 병합 추가 | `server/test/weather-timezone.test.mjs`(16개 — 도쿄·방콕·뉴욕·현지 자정 경계·날짜 커버리지·동시요청 병합, `globalThis.fetch` 모의로 실제 파싱 로직 실행) |
| 4 | 현지 거리·옷차림 영상 기능 없음(이미 요청됐던 것, 이번엔 범위 제한해 신규 구현) | 날씨 카드 아래 버튼 → 클릭 시에만 공식 유튜브 임베드 플레이어 로드(자동재생 금지, 닫으면 정지), 촬영 장소·채널·현지 시간·데이터 사용 안내 표시, "현지인" 단정 안 함, LIVE 자체 단정 안 함, 임베드 거부·방송 종료는 `onError`로 감지해 원본 링크로 대체, 서버 저장·중계 없음, 검증된 영상 없는 도시는 버튼 숨김 | `scripts/test-street-video.mjs`(22개, 실제 Chromium — **이 세션이 직접 주입한 합성 픽스처로 구조만 검증. `STREET_VIDEOS`는 빈 표로 배포돼 실제 영상은 아직 하나도 등록되지 않았다 — 사람이 방송을 직접 확인해 등록해야 하는 일이 남아 있다**) |

---

## 4. 알려진 개선 여지(출시를 막지는 않지만 남겨 둔 것)

- **부분 환불 정책이 아직 없다** — `docs/BUSINESS_DECISIONS.md` 5-0절에
  별도로 보고했다. 정책이 정해지기 전까지 소비자 화면에 부분 취소
  기능을 노출하지 않는다.
- 비용 SKU 단가(`COST_*_KRW_MICROS`)와 예산 한도는 전부 **학습 기억
  기준 추정치**다 — 실제 계약 단가로 반드시 갱신해야 한다
  (`docs/BUSINESS_DECISIONS.md` 3절).
- Routes 경유지 상한(25개)·고요금 임계값(11개)은 ChatGPT가 확인한
  값을 그대로 코드에 반영한 것으로, 이 세션이 공식 문서로 재확인한
  게 아니다 — 실제 값과 다르면 `ROUTES_MAX_INTERMEDIATES_PER_CALL`/
  `ROUTES_HIGH_VOLUME_THRESHOLD` 환경변수만 바꾸면 된다.
- 장소 조회의 "동명 장소 판별"은 지역 힌트(주소 문자열 포함 여부)
  기준의 단순 매칭이다 — 실제 서비스에서 오판별이 잦으면 더 정교한
  판별(도시 좌표 반경 등)이 필요할 수 있다.
- 재조회 사용 패턴(월 몇 %) 가정은 여전히 임의값이다 — 체험단 운영
  중 관찰 필요.
- **(해결됨, 6차)** ~~재방문 여행자 지원의 화면 연결이 아직 안
  됐다~~ — 6차에서 `src/design/spots.js`에 전부 연결했다(7절 참고).
  다만 코스 생성 요청에 `tripId`가 없을 때 예전처럼
  계정+도시+날짜 구조(`account_courses`)로 저장되는 하위 호환 경로는
  의도적으로 그대로 남겨 뒀다(마이그레이션 이전 기존 데이터·
  `tripId`를 안 보내는 외부 호출 대비).
- **(해결됨, 7차)** ~~`/api/places` 전체치환 동기화에 버전 보호가
  없다(6차 신규 발견)~~ — 7차에서 `account_places`/`account_courses`
  에 `version`/`deleted` 컬럼을 추가하고 trips/visits와 같은 버전
  비교+보수적 재병합(`server/routes/account-data.mjs`의
  `syncPlaces`/`syncCourses`)을 적용했다. 삭제는 명시적 `deletedIds`
  로만 이뤄지고, 이미 지워진 장소는 오래된 기기가 다시 들고 나타나도
  안 되살아난다. `scripts/test-sync-protection-screens.mjs` (a)가
  이제 실제로 통과한다(6차엔 여기서 의도적으로 FAIL로 남겼었다).
- **방문 기록 동기화의 충돌 병합 규칙은 이번에 새로 설계한 것**이라
  (버전 비교 + 날짜는 합집합·무덤표시, 단일 값은 최종 수정시각 기준)
  실사용 트래픽에서 검증된 적은 없다 — 지정된 5개 시나리오는 전부
  재현·통과했지만, 실제로 여러 기기를 오래 쓰는 사용자가 늘면 더
  다양한 충돌 패턴이 나올 수 있다.
- **여행 삭제 API가 없다** — 잘못 만든 여행을 지우는 기능은 이번
  범위에 포함하지 않았다(사용자 지시에 삭제 요구가 없었고, "기존
  기록을 절대 지우지 않는다"는 지시와 충돌 소지를 줄이기 위해 의도적
  으로 생략). 필요해지면 별도로 논의해야 한다.
- 기존 city+date 코스 마이그레이션(`schema_migrations`)은 **딱 한 번만
  실행**된다 — 이미 마이그레이션이 끝난 뒤에 생기는 계정 데이터는
  건드리지 않는다(정상 동작이지만, 운영 DB에 처음 배포할 때 반드시
  이 마이그레이션이 실제로 한 번 실행되는지 로그로 확인해야 한다).

---

## 5. 검증한 테스트 결과 (이번 세션에 다시 실행해 직접 확인)

```
node server/test/server.test.mjs                          # 전체 통과
node server/test/config.test.mjs                           # 18개 — 전체 통과
node server/test/server-v2.test.mjs                         # 24개 — 전체 통과(4차: phase 무력화, 배치 엔드포인트 한도 검증 추가)
node server/test/generation-trial-charging-success.test.mjs # 전체 통과
node server/test/generation-trial-charging-failure.test.mjs # 전체 통과
node server/test/production-boot.test.mjs                   # 8개 — 전체 통과
node server/test/provider-contracts.test.mjs                # 39개 — 전체 통과(4차: Places API(New) 계약 검증 12개 추가)
node server/test/reliability-and-cost.test.mjs              # 2 시나리오(routing-missing/routing-available) 합계 60여개 — 전체 통과(5차: 결제·타임아웃 경계 6건 + 캐시 재현 테스트 추가, 시나리오 분리로 재구성)
node server/test/routes-waypoint-limits.test.mjs            # 12개 — 전체 통과(4차 신규 — 경유지 분할·고요금 SKU)
node server/test/cost-budget-allornothing.test.mjs          # 5개(2 시나리오) — 전체 통과(5차: insufficient/sufficient 시나리오 분리 재작성)
node server/test/trips-and-visits.test.mjs                  # 21개 — 전체 통과(5차 신규 — 여행 분리·방문 기록·마이그레이션·무료경계)
node server/test/next-trip-suggestions.test.mjs              # 10개 — 전체 통과(5차 신규 — 미방문우선·다시가고싶음·이월·명시적선택)
node server/test/trips-visits-sync.test.mjs                  # 17개 — 전체 통과(5차 신규 — 지정된 동기화 충돌 시나리오 5건)
node server/test/entitlement-usage.test.mjs                  # 14개 — 전체 통과(6차 신규 — 위치확인 한도·재사용 미차감·실패 미차감)
node server/test/entitlement-course-limit.test.mjs           # 6개 — 전체 통과(6차 신규 — 유료 코스 생성 한도·멱등 재생성 미중복차감)
node server/test/cost-safety-cap.test.mjs                    # 3개 — 전체 통과(6차 신규 — 내부 원가 안전상한 실제 차단)
node server/test/new-sale-committed-budget.test.mjs          # 3개 — 전체 통과(6차 신규 — 기존 유료고객 약속 잔여몫 반영)
node server/test/entitlement-period-month-boundary.test.mjs  # 8개 — 전체 통과(6차 신규 — 30일 이용권 달력월 경계 면역)
node server/test/places-courses-sync.test.mjs                # 16개 — 전체 통과(7차 신규 — 버전 충돌 거부·삭제 무덤 비부활·필드 보존·코스 버전 보호)
node server/test/place-lookup-entitlement-bypass.test.mjs    # 16개 — 전체 통과(7차 신규 — 같은id+다른검색어/다른id+같은실제장소/동시요청 3가지 재현 조건)
node server/test/paid-daily-burst-budget.test.mjs            # 11개 — 전체 통과(7차 신규 — 출고 기본값으로 유료고객 하루 50곳+코스1회 실제 완료)
node server/test/weather-route.test.mjs                      # 12개 — 전체 통과(7차 신규 — 로그인 불필요·이용권 미차감·지역 캐시 공유·좌표 검증)
node server/test/weather-stale-fallback.test.mjs             # 6개 — 전체 통과(7차 신규 — 공급자 실패 시 캐시 대체)
node server/test/sync-conflict-protection.test.mjs           # 24개 — 전체 통과(8차 신규 — baseVersion 정확 일치, 미래버전 사칭 거부, no-op 무증분, 삭제vs수정 충돌, 코스/여행/방문 경합)
node server/test/place-lookup-fast-path.test.mjs             # 12개 — 전체 통과(8차 신규 — 빠른경로 실제 placeId 재대조, 한도초과 지연판정)
node server/test/place-lookup-reservation-recovery.test.mjs  # 17개 — 전체 통과(8차 신규 — 잠정예약 프로세스 종료 복구, 이중해제 방지)
node server/test/weather-timezone.test.mjs                   # 16개 — 전체 통과(8차 신규 — 도쿄/방콕/뉴욕/현지자정경계/날짜커버리지/동시요청병합, epoch 기반 파싱 실제 실행)
node scripts/test/run-all.mjs               # t1~t49, 개인용+판매용 양쪽 — 전체 통과
node scripts/design-integration-check.mjs   # 전체 통과
node scripts/test-storage-migration.mjs     # 전체 통과
node scripts/test-zip-import.mjs            # 전체 통과
node scripts/test-course-generation.mjs     # 3가지 시나리오 합계 24개 — 전체 통과
node scripts/test-purchase-flow.mjs         # 27개 — 전체 통과
node scripts/test-multi-day.mjs             # 28개 — 전체 통과
node scripts/test-place-lookup.mjs          # 11개 — 전체 통과(Places API(New) 응답 형식 반영 확인)
node scripts/test-account-sync.mjs          # 12개 — 전체 통과
node scripts/test-revisit-flow.mjs           # 23개 — 전체 통과(6차 신규 — 여행 목록/전환·새 여행·방문 버튼/필터·이월후보·다른 기기 로그인 동기화, 실제 Chromium 화면)
node scripts/test-sync-protection-screens.mjs # 16개 — 전체 통과(6차 신규, 7차: (a)도 실제로 고쳐져 이제 16/16 전부 통과 — 6차엔 (a)만 의도된 실패였다)
node scripts/test-consumer-flow-e2e.mjs      # 17개 — 전체 통과(6차 신규 — 실제 CSV 파일 300곳 가져오기→배치 위치확인→첫 코스→여행 저장→방문 표시→같은 도시 다음 여행→미방문 우선 코스→이용권 잔여횟수까지 한 번에 이어서 확인, 300곳 가져오기 시 비용 원장 0건도 실제 파일 입력으로 재확인)
node scripts/test-landing-page.mjs          # 전체 통과
node scripts/test-weather-card.mjs          # 13개 — 전체 통과(7차 신규 — 실제 Chromium: 비회원도 날씨 확인, 현재/예보 라벨 구분, 이용량 미차감, 모바일 스크린샷)
node scripts/test-sync-conflict-devices.mjs # 9개 — 전체 통과(8차 신규 — 실제 Chromium 두 기기: 동시 다른 필드 편집 3-way 재병합·수정vs삭제 충돌·저장 중 추가 편집 보존)
node scripts/test-street-video.mjs          # 27개 — 전체 통과(8차 신규, 9차 갱신 — 로드 실패(onerror)·타임아웃·낡은 콜백 정리 3건 추가 검증)
node server/test/place-lookup-reservation-idempotency.test.mjs  # 17개 — 전체 통과(9차 신규 — 이중확정·스윕후지연응답 실제 재현)
node server/test/entitlement-overlimit-verify-cap.test.mjs      # 9개 — 전체 통과(9차 신규 — 한도초과 후 검증 시도 일일 상한)
node server/test/weather-merge-cap.test.mjs                     # 4개 — 전체 통과(9차 신규 — 병합요청 호출상한 이중소비 재현·수정)
node scripts/test-weather-outfit-note-date-scope.mjs            # 7개 — 전체 통과(9차 신규 — 착장문구 날짜혼선 재현·수정)
node server/test/affiliates.test.mjs                            # 14개 — 전체 통과(9차 신규 — 최소 제휴 준비 서버측)
node scripts/test-affiliates.mjs                                # 15개 — 전체 통과(9차 신규 — 최소 제휴 준비 실브라우저)
node scripts/test-sort-and-metadata.mjs                         # 22개 — 전체 통과(9차 신규 — 정렬+메타데이터 분리)
node scripts/test-tags-and-category.mjs                         # 17개 — 전체 통과(9차 신규 — 세계 공통 상위분류+세부 다중 태그)
node scripts/test-test-location-mode.mjs                        # 14개 — 전체 통과(9차 신규 — 테스트 위치 모드)
node server/test/invite-codes.test.mjs                          # 15개 — 전체 통과(9차 신규 — 초대 코드 게이트가 켜졌을 때의 동작)
node server/test/feedback.test.mjs                              # 20개 — 전체 통과(9차 신규 — 불편함 보내기·설문·관리자 API)
node server/test/admin-not-configured.test.mjs                  # 3개 — 전체 통과(9차 신규 — ADMIN_TOKEN 미설정 시 501 안전장치)
node scripts/test-feedback-and-survey-ui.mjs                    # 16개 — 전체 통과(9차 신규 — 불편함 보내기·설문 실브라우저 + 카드 제거 순서 버그 실제 재현·수정)
node scripts/audit.mjs                      # 개인정보·하드코딩 키 잔존 검사 — 전체 통과
node scripts/verify.mjs                     # 보호 블록·저장 키 무결성 — 전체 통과
```

**우선순위 지시("전체 회귀 재실행보다 재현된 문제·실제 사용자 흐름
검증을 우선하라")에 따라**, 이번 세션은 ChatGPT가 지적한 19가지
문제(3절 표) 각각을 실제 코드로 재현하는 전용 테스트부터 작성해
통과시켰고, 그 다음 전체 회귀(위 목록)를 통째로 돌려 다른 회귀가
없는지 확인했다. `server/test/provider-contracts.test.mjs`처럼
`globalThis.fetch`만 가로채는 테스트도 요청 생성 코드(URL·헤더·바디
형태, 위조 방지 검사)는 실제로 실행된다 — ②로 표시한 항목이 가짜
테스트라는 뜻이 아니다.

**8차도 같은 원칙**: 3-3절의 4가지 결함을 먼저 재현 테스트로
확인(수정 전 코드에 돌려 실제로 실패하는 것까지 확인)한 뒤 고쳤고,
마지막에 서버 테스트 27개 파일 + e2e 스크립트 15개 파일 전체를 한
번 더 통째로 돌려 다른 회귀가 없는지 확인했다(전부 전체 통과). 현지
거리·옷차림 영상은 실제 방송 존재·임베드 가능 여부를 검증할 방법이
없어 이 세션이 직접 주입한 합성 픽스처로만 구조를 검증했다는 점을
다시 한 번 명시한다 — **"실제 키만 넣으면 완료"가 아니라 "실제 영상
목록을 사람이 직접 확인해 채워 넣어야 기능이 실제로 나타난다."**

## 6. 화면 캡처(합성 데이터)

`src/design/assets/marketing/1-collection.png`, `2-course.png` —
실제 앱 화면을 Playwright로 캡처한 것이며, 담긴 장소·사진은 전부 이
저장소에 이미 있던 샘플/디자인 예시 자산이다(실제 개인정보 없음).
`2-course.png`는 실제 키 연결이 막혀 있어(2절 참고) 성공 응답을
흉내 낸 장면이라는 점을 `src/design/landing.html`에 그대로 캡션으로
밝혀 뒀다 — 4차 갱신에서도 이 캡션 원칙은 그대로 유지한다.

**6차 신규 — 이번에 새로 화면에 연결한 것들만 `docs/screenshots/`에
따로 캡처했다**(`scripts/shot-r6-flows.mjs`, 전부 지어낸 이름·이메일만
쓴 합성 데이터 — 실제 장소명·개인정보 없음). 7장:
`after_방문표시_장소상세.png`, `after_방문상태필터_목록.png`,
`after_새여행만들기.png`, `after_여행선택.png`, `after_이월후보.png`,
`after_이용권_포함사용량.png`, `after_프로필_잔여횟수.png` — 방문
버튼·방문 상태 필터·새 여행 만들기·여행 전환·이월 후보·이용권 포함
사용량·계정 잔여 횟수가 실제 서버 응답을 반영해 화면에 표시되는
모습을 담았다. **이 스크린샷은 화면 자체가 실제 데이터로 정상
그려진다는 것만 보여준다 — 실제 iPhone Safari에서의 표시나 실제
공급자(Toss/Resend/Google) 연결을 검증하는 게 아니다**(2절과 동일한
구분 원칙).

**7차 신규 — 착장 판단용 날씨 카드**: `docs/screenshots/after_날씨카드_오늘동선.png`
(`scripts/test-weather-card.mjs`가 검증과 함께 캡처, 가상 지명·가상
장소만 사용 — 실제 개인정보 없음). "오늘 동선" 화면 상단에 현재
날씨·오늘 예보·시간대별 강수확률/바람·규칙 기반 착장 문구가 표시되고,
카드 하단에 "테스트 데이터(예시) · 실제 공급자 연결 전"이라고 스스로
밝히는 모습까지 그대로 담겼다(가짜 데이터를 진짜처럼 감추지 않음).
**이 스크린샷도 화면 자체가 정상 그려진다는 것만 보여준다 — 실제
WeatherAPI.com 연결이나 실제 iPhone Safari 표시를 검증하는 게
아니다.**

**8차 신규 — 현지 거리·옷차림 영상**: `docs/screenshots/after_현지거리영상_합성데이터.png`
(`scripts/test-street-video.mjs`가 검증과 함께 캡처). 날씨 카드
아래에 버튼이 뜨고, 클릭하면 패널이 열려 촬영 장소·제공 채널·현지
시간·"영상 시청 시 데이터가 사용돼요" 안내가 표시되는 구조를 담았다.
**이 스크린샷의 영상 자리(검은 박스)는 실제 방송이 아니다** — 이
테스트가 직접 주입한 합성 픽스처(`(합성) 예시 채널`처럼 이름 자체에
표시)이고, 실제 유튜브 네트워크 호출은 테스트에서 의도적으로 막아
뒀다. `STREET_VIDEOS`가 빈 표로 배포되므로 지금 실제 화면에서는
이 버튼 자체가 어떤 도시에서도 보이지 않는다 — 이 스크린샷은 "실제
영상을 등록하면 이런 구조로 보인다"는 것만 보여준다.

---

## 7. 재방문 여행자 화면 연결 — 6차에서 완료

5차에서 서버(스키마·API·마이그레이션·동기화)만 끝내고 ⑤(화면
미연결)로 남겨 뒀던 5가지를 이번 6차에서 `src/design/spots.js`에 전부
연결했다. 새 백엔드 설계 없이 기존 서버 API를 그대로 부르는 화면
배선·문구 작업이었다:

1. **여행 목록·전환 화면 — 완료.** `/api/trips` 목록을 `tripPickerSheet`
   로 보여주고, 같은 도시에 여행이 2개 이상일 때만 카드로 구분해
   고를 수 있게 했다(여행이 하나뿐이면 예전 화면과 동일). "오늘
   동선"·"오늘의 코스" 화면 상단에 지금 여행 이름을 표시한다
   (`tripBlockHTML`).
2. **새 여행 만들기 흐름 — 완료.** `newTripFormSheet`가 이름·시작일·
   종료일·숙소를 선택 입력으로 물어보고 `POST /api/trips`를 부른다.
   "도시 바꾸기"(장소 필터용 `chooseCity`)와는 완전히 분리된 별도
   버튼이라 오해될 소지가 없다.
3. **방문 표시 버튼 — 완료.** 장소 상세(`detail()`)에 "오늘 날짜로
   방문 완료 표시"·"마지막 방문 취소"·"다시 가고 싶어요" 버튼을
   추가했다(`visitBlockHTML`/`visitAction`). 코스 담기(`selected`)와
   완전히 분리된 상태값이라 자동 방문처리가 안 된다. 방문 기록이 하나
   라도 생기면 목록 화면에 방문 상태 필터(전체/미방문/방문함/
   다시가고싶음)가 나타난다.
4. **"다음 여행 코스 만들기" 진입점 — 완료.** 새 화면을 따로 안 만들고
   기존 "오늘 동선" 화면 안에 `carryForwardSheet`로 이월 후보 목록만
   얹었다(지시대로 "첫 화면 설정을 늘리지 않음"). 같은 도시에 다른
   여행이 있고 동선이 비어 있을 때만 진입점이 뜬다.
5. **로그인 시 동기화 전환 — 완료.** `daSyncPush`가 이제
   `/api/trips/sync`·`/api/visits/sync`를 로그인 직후와 매 로컬 저장
   때마다 실제로 부른다(예전엔 클라이언트가 이 엔드포인트를 아예 안
   불렀다). `/api/places`·`/api/courses` 전체치환 PUT은 `tripId`가
   없는 레거시 코스만 계속 담당하도록 좁혀서, trip에 딸린 코스가 그
   경로로 새어 나가 버전 보호 없이 덮어써지는 일을 막았다.

전부 `scripts/test-revisit-flow.mjs`(23개, 실제 Chromium 화면)로
검증했다. 남은 것은 4절에 적은 **`/api/places` 자체의 버전 보호
부재**(다음 라운드 후보)뿐이다.

## 2. 사업 결정 정리 (travel hub) — ChatGPT 판단용

작성: 2026-09-10(1차) · 갱신: 2026-09-10(2차 — 확정 상품 반영, 비용·
사용 한도 제안 추가) · 갱신: 2026-09-10(3차 — 결제/이메일/경로 공급자
확정 반영, 서버 집행 구조 재설계, 비용표 갱신) · 갱신: 2026-09-10(4차 —
ChatGPT가 실제로 재현한 문제(운영에서 가짜 실제경로 발생, 결제 취소
권한 미대조 등) 수정, API 비용 통제(예산 원장·한도) 실제 구현, 경유지
상한 반영, 원가표에서 미검증 낙관 결론 제거) · 갱신: 2026-09-10(5차 —
**비용 SKU 단가를 실제 요청 필드 기준(Text Search Pro 등급)으로
바로잡음**, 공식 등급·달러 단가·계산용 환율·안전 여유를 명확히 분리,
계정별 월간 한도 신설, 초기 예산 5만원 배분 재확인, **재방문
여행자(반복 구매) 손님의 비용·가격 구조 정리** 추가) · 갱신:
2026-09-10(6차 — 이용권 한도가 실제로 씌워진 뒤의 진짜 최악 원가
재계산, 신규판매 가능여부 판정) · 갱신: 2026-09-10(7차 — **계정별
하루 비용 한도가 유료 이용권의 "하루 몰아쓰기" 약속과 충돌하던
버그 수정**(3-3-2절), **신규 장소 확인 한도 우회 차단으로 매출
누수 위험 해소**(3-3-3절), **착장 판단용 날씨 카드 신규**(직접
비용 없음, 3-3-4절)) · 갱신: 2026-09-11(9차 — **사업 방향 재확정**
(세계 여러 도시 대상 재확인, 월 100만원 반복수익 목표, 이용권+재구매
기본 모델, eSIM/교통/티켓 제휴는 필요시만, 4,900원 베타가 보류,
지역팩·AI플래너·B2B광고·도시변경과금 보류 — 1-2절), **한도소진 후
유료 검색 반복으로 비용 상한이 소진되던 결함 수정**(3-3-5절), **최소
제휴 준비 구조 신규**(7절)) · 브랜치 `design-integration`

**5차 갱신 배경**: ChatGPT가 실제 Google 공식 단가 기준으로 3차/4차
문서를 검토한 뒤, `server/config.mjs`의 비용 추정이 "가장 싼 등급
($15원/건으로 가정)"이라는 학습 기억에 의존한 잘못된 전제였고, 실제
우리 코드가 요청하는 필드(`places.displayName`, `places.location`,
`places.formattedAddress`)는 **Text Search Pro 등급**(가장 싼 등급이
아님)에 해당한다고 지적했다. 공식 첫 유료 등급 단가(ChatGPT 제공,
이 세션이 직접 재확인한 것은 아님 — 5-1절 참고)는 Text Search Pro
$32/1,000, Routes Compute Essentials $5/1,000, Routes Compute Pro
$10/1,000이다. 이번 갱신은 이 단가를 실제로 코드(`server/config.mjs`)
에 반영하고, "공식 등급·달러 단가·계산용 환율·안전 여유"를 서로 다른
변수로 분리해 앞으로 등급이 바뀌어도 계산 전체를 다시 짤 필요가
없게 만들었다. 또한 "월 5만원은 초기 전체 운영 목표이지 전액 Google
예산이 아니다"라는 지적을 반영해 고정비/Google 예산 배분을 다시
확인했고, 무료 체험 사용자의 비용까지 포함해 계산한다.

이 문서는 남은 결정 사항을 정리하는 곳이다. 브랜드·가격·기능 범위는
이미 확정돼 반영했다(아래 1절) — 남은 건 실제 서비스 연결 방식과
사용 한도처럼 아직 안 정해진 것들이다. 숫자를 지어내지 않는다 —
확인 안 된 값은 "모른다/확인 필요"라고 그대로 적는다.

**3차 갱신에서 달라진 것 (요약)**: 사용자가 2차 갱신 검토 후 "무료체험이
서버가 아니라 클라이언트에서 집행되고 있었다"·"운영에서 테스트 기능이
안 막혔다"·"장소 조회가 로그인·한도 없이 열려 있었다" 등 구체적인
문제를 지적했다. 이번 갱신은 그 지적에 대한 실제 코드 수정 결과를
반영한다 — 그리고 **공급자도 확정됐다: 결제는 토스페이먼츠, 로그인
이메일은 Resend, 장소·경로는 Google Places + Routes.** 공급자 확정은
"이 API를 붙이는 코드를 작성한다"는 결정이지, 실제 유료 계약이나
과금을 지금 승인한다는 뜻이 아니다(사용자 지시 원문 그대로). 세
공급자 모두 실제로 그 API를 호출하는 코드를 작성하고 키 없이 통과하는
계약 형태 테스트까지 마쳤지만, **이 세션은 outbound 네트워크가 막혀
있어(EGRESS_BLOCKED) 공식 문서를 다시 열어보거나 실제 키로 검증하지
못했다** — 학습 시점 기억을 바탕으로 작성했고, 실제 키를 넣고 처음
써보기 전까지는 ④(실제 연결 검증 필요)로 분류한다(`docs/RELEASE_STATUS.md`
참고).

---

## 1. 확정된 사업 방향 (반영 완료)

- 모바일 중심 서버 기반 여행 웹앱, 표시명 **travel hub**.
- 후쿠오카 전용이 아니라 사용자의 저장 장소를 기준으로 여러 도시에서
  사용(도시별 분리, 여러 날짜 일정 정리까지 구현·검증 완료 —
  `docs/DESIGN_INTEGRATION_REPORT.md` 1-B-11).
- 첫 상품: **9,900원·30일 여행 이용권, 결제 승인 시 시작, 자동결제
  없음.** `server/config.mjs`의 기본값을 그대로 확정 상품으로 반영했다
  (여전히 환경변수로만 바꾸는 구조 — 코드에 숫자를 안 박는다는 원칙은
  유지). **구독(정기결제)이 아니다** — 이 문서·마케팅 문서 모두에서
  "구독"이라는 말을 쓰지 않는다. 기간이 끝나면 그냥 끝이고, 다시
  쓰려면 다시 결제해야 한다.
- 무료: 샘플, 장소 가져오기·보관·검색·수동 정리, 개인화 코스 생성
  성공 1회(로그인 필요 — 3차 갱신에서 "첫 코스도 로그인 후에만"으로
  바뀌었다, 2절 참고). 유료: 추가 코스 생성·조건 변경 후 재계산·여러
  날짜 일정 정리. 전부 구현·검증 완료.
- **⚠ 6차 갱신 — 이용권에 실제 사용량 숫자를 못 박았다: 무료체험
  위치 확인 최대 10곳+개인화 코스 성공 1회, 유료 이용권(30일) 위치
  확인 최대 50곳+코스 생성·재계산 최대 30회.** 저장 개수·방문 기록
  열람·수동 정리는 이 한도와 무관하게 계속 무료다(3-3-1절). **이
  숫자는 초기 테스트 상품의 스펙일 뿐, 공개 출시 전 실사용·편의성·
  원가를 검증해 확정한 시장가격이 아니다** — 실제 손님이 30회/50곳
  한도를 불편해하는지, 원가 계산(3-3절)이 실제 계약 단가로도
  맞는지는 초기 체험단 운영 후에 다시 봐야 한다. 새 가격 구간·유료
  부가상품은 만들지 않았다(단일 요금제 원칙 유지).
- 만료 후에도 기존 장소와 코스 열람은 유지된다(서버 게이트를 안 타는
  순수 열람 — `scripts/test-multi-day.mjs`에서 환불 뒤에도 열람되는
  것까지 확인).
- 무제한 사용은 아직 확정 안 됨 — 3-5절에 제안을 정리해 뒀다.
- 결제 **토스페이먼츠**(결제창+서버 승인 연동), 로그인 이메일
  **Resend**, 장소·경로 **Google Places + Routes**로 공급자가
  확정됐다. 이건 "이 API를 호출하는 코드를 만든다"는 결정이고, 실제
  유료 계약·과금을 지금 승인하는 게 아니다.

기존 `src/landing.html`(브랜드 "별표털기", 서버 없음, 한 번 결제)과의
충돌은 위 확정 방향대로 **(A) travel hub로 완전히 전환**하는 것으로
해소된 것으로 보고 진행한다 — `src/landing.html`은 여전히 손대지
않았고(폐기 여부는 실제 공개 배포 시점에 결정), `src/design/`
기준으로만 개발을 이어간다. 옛 마케팅 문서(`가격_판매전략.md` 등,
서버 없는 단일 파일·한 번 결제 전제)는 이제 전제 자체가 안 맞으므로
`MARKETING_LAUNCH_PLAN.md`(이번에 확정 상품 기준으로 갱신됨)를 쓴다.

## 1-2. 9차 재검토 — 확정 사업 방향 갱신(기존 문서 내용보다 이 절 우선)

사용자가 9차에서 사업 방향을 다시 확정했다. 아래 내용이 이 문서
다른 곳의 관련 서술과 다르면 **이 절을 따른다.**

- **목표 시장**: 세계 여러 도시에서 사용자 자신이 저장한 장소로 여행을
  편하게 계획하는 앱이다. 후쿠오카 한정 상품이 아니다(1절과 동일한
  원칙의 재확인). 창업자 본인의 후쿠오카 저장 장소가 160곳 이상이라는
  경험담은 홍보에 쓸 수 있지만, 이걸 "전체 국가 합계 사용자 수"나
  "검증된 시장 규모"처럼 확대해서 쓰지 않는다.
- **초기 사업 목표**: 운영비를 빼고 **월 100만 원이 남는, 관리 부담이
  낮은 반복 수익 자산**을 만드는 것. 이건 목표치이지 세후 순이익이나
  수익을 보장하는 약속이 아니다 — 마케팅 문서·소비자 화면 어디에도
  이 숫자를 성과나 약속처럼 쓰지 않는다(내부 목표로만 취급).
- **기본 수익 모델**: 앱 이용권(현재 코드값 9,900원/30일, 자동결제
  없음)과 **다음 여행 때의 재구매**. 자동 갱신되는 월 구독으로
  바꾸라는 지시가 아니다 — 지금 방식(만료 후 재결제) 그대로 유지한다.
- **추가 수익(보조)**: 필요할 때만 eSIM·교통권·입장권 등 제휴로
  보충한다. 숙박 제휴는 "아직 숙소를 예약하지 않은 사용자"에게만
  자연스럽게 노출한다(이미 숙소를 정한 사용자에게 억지로 들이밀지
  않는다). 6절의 "최소 제휴 준비 구현" 참고 — 이번엔 구조와 클릭
  이벤트만 준비하고 실제 계약·게시는 하지 않는다.
- **가격 재검토**: 4,900원 베타 가격안은 **보류**다(채택도 폐기도
  아님 — 다음 결정을 기다리는 상태). 현재 코드에 있는 9,900원은
  "지금 코드가 이렇게 동작한다"는 사실일 뿐, 검증을 마치고 확정한
  최종 판매가로 홍보하지 않는다. Claude는 가격·사용량 관련 코드
  값을 임의로 바꾸거나, 사용자를 싸게 모집하는 문구·로직을 마음대로
  추가하지 않는다(변경이 필요하면 ChatGPT/사용자의 명시적 결정을
  먼저 받는다).
- **보류(당장 하지 않음)**: 검증 안 된 유료 지역팩, 별도의 대규모
  AI 여행 플래너, 업장 광고·B2B 상품, "도시를 바꿨다"는 이유만으로
  받는 추가 과금. 이 문서에 없는 새 사업 기능을 Claude가 스스로
  만들지 않는다.
- **제휴 운영 원칙**: 제휴 수수료 때문에 추천 동선을 바꾸거나,
  수수료가 높은 업체 순서로 장소를 추천하지 않는다. 이 원칙은 코드
  구조(6절 제휴 스캐폴드)에도 그대로 반영한다 — 제휴 표시 조건이
  "이 지역에 승인된 링크가 있는가"일 뿐, 코스 생성 로직에는 전혀
  관여하지 않는다.
- **수치 사용 규율**: "고객당 추가 매출 15,000~30,000원", "마진
  100%", "제휴 업체 30개 확보" 같은 숫자를 검증된 전망으로 문서나
  마케팅 자료에 쓰지 않는다. 실제 계약·데이터가 생기기 전까지는
  전부 "미정" 또는 "가설"로만 표기한다.

## 2. 확인된 사실 (실제로 코드를 돌려서 검증함)

**4차 갱신에서 새로 고친 것(요약)**: ChatGPT가 실제로 재현·지적한
문제들이다 — 전부 재현 테스트로 확인한 뒤 고쳤다.
- **운영에서 경로 API 키가 없을 때 가짜 "실제 경로"가 나오던 버그**를
  고쳤다. `services.routing !== 'real'`이면 무조건 테스트 시뮬레이터로
  빠지던 조건문이 `'unavailable'`(운영, 키 없음)일 때도 참이 돼,
  ChatGPT가 `APP_ENV=production`+키 없음 상태에서 실제로 `routedReal:true`
  를 받아냈다 — 이제 `'test'`일 때만 시뮬레이터를 부르고, `'unavailable'`
  이면 절대 시뮬레이터로 안 빠지고 정직한 추정만 준다(설정 함수뿐
  아니라 실제 `/api/course/generate` 엔드포인트까지 재현 테스트로
  확인).
- **결제 취소가 계정 소유자를 대조하지 않던 문제**를 고쳤다 — 다른
  계정의 주문을 취소할 수 있었다. 이제 세션의 accountId로 주문
  소유자를 반드시 대조한다.
- **과거 주문 취소가 다른(더 최근) 유효 주문의 이용권을 없앨 수 있던
  구조**를 고쳤다 — 계정이 지금 어느 주문으로 이용권을 받았는지
  (`accounts.active_order_id`)를 기억해, 그 주문이 아닌 옛 주문의
  취소·지연 웹훅은 지금의 이용권을 건드리지 않는다.
- **결제 승인 응답이 유실(타임아웃 등)돼도 무조건 실패로 확정하지
  않고** 토스 조회 API로 실제 상태를 재확인한 뒤 판단한다. 확정 못
  하면 주문을 `failed`로 낙인찍지 않고 재시도 가능한 상태로 남긴다.
- **부분 취소를 전액 취소와 다르게** 처리한다 — 부분 취소는 이용권을
  자동으로 회수하지 않는다(환불 정책 미확정 — 아래 5절에 별도 보고).
- **활성 이용권이 있으면 새 결제를 서버가 직접 거부**한다(중복 구매
  방지, 만료일 안내는 클라이언트가 응답의 `expiresAt`으로 표시).
- **코스 생성의 체험 차감·결과 저장을 하나의 DB 트랜잭션으로 묶었다**
  — 저장이 실패하면 체험도 차감되지 않는다. 생성 잠금에 만료·소유권
  검증을 추가해 프로세스가 도중에 죽어도 그 계정이 영원히 막히지
  않는다. 같은 멱등키에 실제로 다른 요청 본문이 오면 충돌로 처리한다.
- **장소 조회를 Legacy Find Place에서 Places API(New)로 전환**했다 —
  첫 검색 결과를 무조건 확정하지 않고, 지역 힌트와 실제로 맞는 후보를
  우선한다(동명 장소 오확정 방지). 일괄 조회 엔드포인트를 추가해
  장소마다 반복 설정하지 않아도 되게 했다.
- **API 비용 통제를 실제로 코드로 집행**했다 — 호출 종류·SKU별 예상
  비용을 원장에 기록하고, 계정별→전체 순으로 한도를 확인해(계정
  한도로 거부될 요청이 전체 한도를 갉아먹지 않게), 한도를 넘으면
  실제 호출 자체를 하지 않는다. `phase=import`처럼 클라이언트가
  자기 신고하는 값만으로 더 큰 한도를 받을 수 없게 했다(더 큰 한도는
  서버가 실제로 처리하는 배치 크기로만 정해진다). 여러 세그먼트로
  나뉘는 코스는 예산이 일부만 감당 가능하면 전부 추정으로 대체한다
  (일부만 실제·일부만 추정으로 뒤섞이지 않는다).
- **Google Routes 중간 경유지 상한(25개)을 지킨다** — 넘으면 연결된
  여러 번의 호출로 나누고(연결 구간·실제 호출 수 보존), 11개 이상인
  구간은 더 비싼 요금(SKU)으로 비용을 기록한다.
- 서비스 전체 이번 달 비용 한도가 이미 꽉 찼으면 새 결제 자체를
  막는다(유료 고객이 결제만 하고 정작 코스를 못 만드는 상황 예방).

**2차 갱신(이전) 이전에는 이 절이 설명하는 "무료체험 확인"·"장소
조회"가 실제로는 클라이언트가 먼저 계산하고 서버에는 결과만 뒤늦게
알리는 구조였다 — 사용자가 직접 테스트해서 찾아낸 문제였다(동시
요청으로 무료체험 우회 가능, 실패한 추정 결과도 체험이 차감됨,
장소 조회가 로그인·한도 없이 열려 있음 등). 아래는 그 구조를
서버 집행 방식으로 다시 짠 뒤 실제로 검증한 내용이다.**

- 구글맵 저장 목록 내보내기(Google Takeout) ZIP/CSV/JSON을 실제로
  읽어 장소를 가져온다.
- 가져온 장소를 도시별로 나누고, 유형을 짐작하되 사람이 직접 고치면
  그 선택을 유지한다.
- **장소 조회(`/api/places/lookup`)는 이제 로그인 없이는 아예 호출할
  수 없다**(인증 필수로 막음). 초기 가져오기(160~300곳처럼 한 번에
  많이 확인하는 상황)와 이후 재조회는 **서로 다른, 환경변수로 조절
  가능한 일일 한도**를 따로 둔다(`PLACE_LOOKUP_IMPORT_DAILY_LIMIT`/
  `PLACE_LOOKUP_REQUERY_DAILY_LIMIT`, 계정별+서비스 전체 상한
  `PLACE_LOOKUP_GLOBAL_DAILY_CAP`도 별도) — "위치 조회 하루 30회"처럼
  두 상황을 하나의 값으로 묶는 안은 사용자가 명시적으로 반려했다(첫
  가져오기부터 막힐 수 있다는 이유). 지금은 테스트 어댑터(가짜
  좌표·결정론적) 기준으로 인증·한도 분리 동작을 실제 HTTP 요청으로
  검증했다 — 실 Google Places 키 연결은 아직 미검증(④).
- **개인화 코스 생성이 전부 서버 집행으로 바뀌었다.** 로그인 필수(샘플
  체험만 예외), 클라이언트가 만든 멱등키로 재시도해도 실제 작업을
  중복 실행하지 않음, 계정당 동시 생성 잠금(같은 계정이 동시에 두
  요청을 보내도 하나만 진행), 시간당 시도 횟수 제한, 그리고 **무료체험은
  실제 경로 계산이 성공했을 때만(`routedReal===true`) 딱 한 번
  차감** — 추정(직선거리) 결과로 대체됐을 때는 차감하지 않아서 재시도가
  계속 무료로 가능하다. 이 과정에서 "좌표 없는 곳이 정거장 목록에
  순서·구간 없이 잘못 끼어드는" 실제 버그를 새 테스트로 찾아 고쳤다
  (`server/routes/course-generation.mjs`의 좌표 필터링).
- 걸어서 도는 코스는 이제 **Google Routes(WALK) 서버측 어댑터**로
  실제 경로를 계산한다(기존 공개 OSRM 데모 URL 방식은 폐기 —
  실서비스 트래픽용이 아니었고, `/trip` 파라미터 위험도 있었다).
  방문 순서는 비용 없는 최근접 이웃 방식으로 우리가 먼저 정하고,
  Google Routes에는 그 순서 그대로 딱 한 번만 요청해 실제 구간
  거리·시간을 받는다(정렬 자체를 API에 맡기지 않음 — 도보 모드에서
  지원 여부·과금 등급이 불확실했기 때문). 말이 안 되는 속도의 응답은
  실제 경로로 인정하지 않고 추정으로 대체한다. **이 세션은
  `developers.google.com`으로 나가는 네트워크가 막혀 있어(EGRESS_BLOCKED)
  공식 문서를 다시 확인하거나 실제 키로 성공 응답을 받아보지
  못했다** — 요청 형태는 계약 테스트(`server/test/provider-contracts.test.mjs`)
  로 검증했지만 실제 연결은 미검증(④).
- 로그인 코드 이메일은 **Resend** 어댑터로 실제 API를 호출하는 코드를
  작성했다(`POST https://api.resend.com/emails`). 발송 실패 시
  방금 만든 로그인 코드를 되돌려 재사용 못 하게 하고, 쿨다운·IP
  레이트리밋·연속 실패 잠금까지 갖췄다. 실제 키로 진짜 이메일이
  도착하는지는 미검증(④, 같은 이유로 `resend.com` 문서도 이 세션에서
  못 열어봤다).
- 결제는 **토스페이먼츠** 결제창+서버 승인 연동 코드를 작성했다 —
  서버가 먼저 주문(금액·주문번호)을 만들고, 결제창은 그 주문으로만
  진행되며, 승인 API 호출 전에 서버가 직접 주문 소유자·금액을 다시
  확인한다(클라이언트가 준 금액을 그대로 믿지 않음). 웹훅은 페이로드를
  그대로 믿지 않고 토스 조회 API로 다시 확인한 뒤에만 반영한다. **이
  세션은 `docs.tosspayments.com`으로 나가는 네트워크가 막혀 있어
  공식 스펙(인증 방식·쿼리·이벤트 처리)을 다시 확인하지 못했다** —
  학습 시점 기억으로 작성했고, 실제 클라이언트 키·시크릿으로 결제창이
  뜨는지는 미검증(④).
- **운영(production) 환경에서는 테스트/모의 기능이 확실히 차단된다.**
  결제·이메일에 필요한 시크릿이 없으면 서버 자체가 부팅을 거부한다
  (`assertBootReady` — 실제로 자식 프로세스를 띄워 종료코드 1을
  확인했다). 장소조회·경로 키가 없으면 부팅은 막지 않되 그 기능만
  정직하게 "unavailable"로 표시한다(가짜 응답으로 대체하지 않음).
  개발용 결제 시뮬레이션 엔드포인트는 production에서 라우트 자체가
  등록되지 않는다(호출 자체가 불가능 — 내부 조건문으로만 막는 게
  아니라 아예 없음). "키가 있다"와 "실제로 연결이 확인됐다"도
  `/api/health`에서 서로 다른 필드(`services` vs `verified`)로
  구분해 보여준다.
- 계정별 장소 보관함·날짜별 일정을 서버에 저장하고(`/api/places`,
  `/api/courses`), 로그인 시 이 기기의 손님 데이터와 다른 기기에
  이미 있던 데이터를 둘 다 잃지 않게 병합하며, 로그아웃 시 이
  기기에서 로컬 데이터를 지워 다음에 로그인하는 계정과 섞이지 않게
  한다 — 전부 실제 브라우저(Chromium)로 로그인→병합→로그아웃→다른
  계정 로그인→데이터 미노출→재로그인→복원까지 종단 검증했다
  (`scripts/test-account-sync.mjs`).

## 3. 비용 — 무엇이 얼마나 드는가

### 3-0. 이 절을 읽기 전에 — 숫자의 신뢰 수준

공급자가 확정돼(토스페이먼츠·Resend·Google Places+Routes) 이번에는
"확인 필요"라고만 적지 않고 실제로 계산까지 해 본다. 하지만 정직하게
밝혀야 할 제약이 있다: **이 세션은 outbound 네트워크가 막혀 있어
(EGRESS_BLOCKED) 세 공급자의 공식 가격표 페이지를 이번에도 열어보지
못했다**(`mapsplatform.google.com`, `resend.com`,
`tosspayments.com` 모두 시도했고 전부 차단됨). 아래 단가는 **학습
시점 기억에 남아 있는 대략적인 자릿수**이지, 지금 이 순간의 공식
고시 가격이 아니다 — 실제 계약 전 반드시 각 공식 페이지에서
재확인해야 한다. 이 제약을 감안해 아래 표는:

1. **단가를 변수로 두고 사용량(요청 건수)을 먼저 정확히 계산**한다
   (이 부분은 지금 코드의 실제 설계로부터 나온 확정값이다 — 지어낸
   게 아니다).
2. 학습 기억 기준 **자릿수 추정 단가**를 참고용으로 병기하되, "약
   얼마 이하일 가능성이 높다" 수준의 범위로만 제시하고 "정확히
   얼마"라고 단정하지 않는다.
3. 실제 계약 시점에 확인한 진짜 단가를 이 표의 빈칸에 넣기만 하면
   바로 실제 비용이 나오게 구조를 짜 뒀다.

### 3-1. 요청 건수 — 실제 코드 설계에서 나온 확정값

지금 코드(`server/rate-limit.mjs`, `server/routes/course-generation.mjs`,
`server/adapters/routing.mjs`)의 실제 동작 기준:

| 사용자 행동 | 실제로 나가는 외부 API 호출 |
|---|---|
| 저장 장소 최초 가져오기(위치 불확실 후보 확인) | 장소 1곳당 Google Places 요청 **최대 1건**(위치가 이미 확인된 곳은 0건 — 짐작 유형·주소만으로 이미 확정된 곳은 다시 안 부른다) |
| 이후 개별 재조회("서버로 위치 후보 찾아보기" 버튼) | 누른 횟수만큼 Google Places 요청 1건씩(10분 캐시 안에 같은 질의면 0건) |
| 코스 생성·재계산 1회 | Google Routes(`computeRoutes`) 요청 **보통 1건**(방문 순서는 비용 없는 로컬 계산으로 먼저 정하고, 실제 구간 거리·시간만 API에 묻는 설계 — 정거장이 **25곳을 넘으면** 경유지 상한 때문에 여러 번의 연결된 호출로 나뉜다: `ceil((정거장 수 − 1) / 25)`건. 하루 코스는 보통 한 자리~십수 곳이라 대부분 1건이지만, 여러 날짜를 한 코스로 묶는 등 정거장이 많은 경우는 늘어날 수 있다 — `server/adapters/routing.mjs`의 세그먼트 분할 로직, `docs/RELEASE_STATUS.md` 참고) |
| 로그인 1회(코드 요청) | Resend 이메일 발송 1건 |
| 결제 승인 1건 | 토스페이먼츠 거래 수수료 1건 |

**사용량 상한(이미 코드로 구현·검증됨 — 예전엔 계정별 호출 횟수 제한이
전혀 없었다는 지적이 이번에 해소됐다)**: 초기 가져오기 일일 한도(`PLACE_LOOKUP_IMPORT_DAILY_LIMIT`,
기본 400)와 재조회 일일 한도(`PLACE_LOOKUP_REQUERY_DAILY_LIMIT`,
기본 60)를 분리했고, 코스 생성도 시간당 한도(`GENERATION_RATE_LIMIT_PER_HOUR`,
기본 10)가 있다. 전부 환경변수라 계약 후 실제 단가를 보고 숫자를
조정할 수 있다.

### 3-2. 사용자가 지정한 세 시나리오(저장 50/160/300곳 × 월 코스
생성 10/30/50회)의 요청 건수 계산

**가정(명시)**: 최초 가져오기 시 장소 전부가 위치 확인이 안 된
상태라고(최악의 경우로) 가정한다 — 실제로는 링크에 좌표가 이미 있는
곳은 0건이라 실제 값은 이보다 작거나 같다. 재조회는 저장 장소 수의
10%를 한 달에 한 번씩 다시 확인한다고 가정한다(이 숫자는 실제 사용
패턴 관찰 전까지의 임의 가정 — 4절 "확인 안 된 가설"에도 동일하게
표시).

| 저장 장소 수 | 최초 가져오기 Places 요청(1회성) | 월 재조회 Places 요청(가정) |
|---|---|---|
| 50곳 | 최대 50건 | 약 5건/월 |
| 160곳(제작자 실제 사례) | 최대 160건 | 약 16건/월 |
| 300곳 | 최대 300건 | 약 30건/월 |

| 월 코스 생성 횟수 | 월 Google Routes 요청 |
|---|---|
| 10회 | 10건 |
| 30회 | 30건 |
| 50회 | 50건 |

무료체험(가입 후 첫 실제 경로 성공 1회)은 위 "월 코스 생성" 중 1건을
대체할 뿐 추가 요청을 만들지 않는다 — 실패(추정) 결과는 체험을 안
쓰므로 사용자가 재시도하면 그만큼 Routes 요청이 추가될 수 있다는
점만 유의(성공할 때까지 재시도 횟수는 예측 불가 — 지어내지 않는다).

### 3-3. 단가(5차 갱신 — 실제 요청 필드 기준 등급으로 바로잡음)와
9,900원 이용권 1건의 원가 그림

**5차 갱신 핵심**: 3차/4차 문서는 Google Places 단가를 "가장 싼
등급"이라는 학습 기억으로 약 15원/건으로 추정했다. ChatGPT가 지적한
대로, 우리 코드가 실제로 요청하는 필드마스크(`places.id`,
`places.displayName`, `places.location`, `places.formattedAddress`)는
**Text Search Pro 등급**에 해당하고 가장 싼 등급이 아니다. 아래
표는 ChatGPT가 제시한 공식 첫 유료 등급 단가(이 세션이 직접
`mapsplatform.google.com`에 접속해 재확인한 것은 아니다 — 여전히
EGRESS_BLOCKED, 5-1절 참고)를 그대로 코드(`server/config.mjs`의
`buildCostEstimateMicros`)에 반영한 결과다. **공식 등급·달러 단가·
계산용 환율·안전 여유를 서로 다른 값으로 명확히 나눴다** — 안전
여유(15%)는 예산 상한에만 적용하고, 실제 원가 계산(이 표)에는
안전 여유를 안 섞은 순수 단가만 쓴다(부풀려진 숫자로 원가를 낮게
착각하지 않기 위해).

| SKU | 공식 첫 유료 등급 단가(ChatGPT 제공, 이 세션 미재확인) | 계산용 환율(가정, 실시간 아님) | 원화 환산 단가 |
|---|---|---|---|
| Places Text Search, **Pro** 등급 | $32 / 1,000건 | 1,400원/$ | **약 44.8원/건** |
| Routes computeRoutes, **Essentials** 등급(경유지 11개 미만) | $5 / 1,000건 | 1,400원/$ | **약 7원/건** |
| Routes computeRoutes, **Pro** 등급(경유지 11개 이상 — `ROUTES_HIGH_VOLUME_THRESHOLD`) | $10 / 1,000건 | 1,400원/$ | **약 14원/건** |

| 항목 | 확인해야 할 곳 |
|---|---|
| 위 세 SKU의 실제 등급명·단가 | Google Maps Platform 공식 가격표 — 이 세션은 여전히 접속 불가, 계약 전 반드시 사람이 직접 재확인 |
| Google Maps Platform 월 무료 사용량 | 과거 월 정액 크레딧에서 SKU별 무료 사용량 방식으로 바뀌었을 가능성 — "0원일 수도 있다" 이상으로 단정하지 않는다 |
| 토스페이먼츠 카드 결제 수수료 | 일반(미계약) 요율 기준 9,900원의 몇 퍼센트대로 기억(정확한 요율 불확실) — 토스페이먼츠 가맹 계약 시 실제 고지 요율 |
| Resend 이메일 발송 | 소규모 발송량은 무료 등급 가능성 — 정확한 일일/월 한도는 Resend 공식 가격표에서 확인 |

**9,900원 이용권 1건의 원가 계산(포함/제외 항목을 명시한다)**:

```
원가 = (토스 결제 수수료 1건)                              [포함 — 위 표의 추정 요율]
     + (Resend 발송 단가 × 이 손님의 30일간 로그인 횟수)     [포함 — 추정 단가]
     + (Places Text Search Pro 단가 × 최초 가져오기 건수 + 재조회 건수) [포함 — 44.8원/건]
     + (Routes 단가 × 30일간 실제 생성·재계산 횟수, 경유지 수에 따라 7원 또는 14원/건) [포함]

제외(이 계산에 안 들어감, 별도 항목): 서버·DB 호스팅비(계정 수와
무관하게 나가는 고정비라 "이용권 1건당 원가"에 나누어 넣지 않았다 —
6절에서 별도로 다룬다), 환불 처리 자체의 운영 비용(사람이 처리하는
시간 등 — 지금은 0으로 가정), Google Maps Platform의 무료 사용량
크레딧(있다면 원가를 낮추는 방향이지만, 정책을 확인하지 못해 0원으로
가정 — 보수적 가정).
```

**세 시나리오(3-2절 건수 × 위 정확화된 단가)의 실제 계산 — 단가
자체는 아직 이 세션이 공식 문서로 재확인 못 했지만("확인해야 할 곳"
표 참고), 최소한 "가장 싼 등급이 아니라 Pro 등급"이라는 등급 자체는
반영된 계산이다(4차의 "약 15원/건" 추정보다 훨씬 근거 있는 수치)**:

**⚠ 6차 갱신 — 아래 표는 "이용권 사용량에 한도가 없다고 가정했을 때"의
낡은 계산이다.** 5차 시점엔 코스 생성·위치 조회 횟수 자체에 상한이
없어서 "저장 300곳·월 50회 재계산"이 실제로 가능한 사용 패턴이었다.
**6차에서 유료 이용권에 실제 사용량 한도(위치 확인 최대 50곳+코스
생성 최대 30회, 30일 기준 — `server/entitlement-usage.mjs`)를
집행했으므로, 아래 "300곳·월 50회"(=330건 조회) 시나리오는 이제
한 이용권 기간 안에서 물리적으로 불가능하다**(50건을 넘는 순간
서버가 그 자리에서 위치 확인을 거부한다 — 5절 참고). 이 표는 "한도를
두기 전에는 이런 헤비유저가 존재할 수 있었다"는 **역사적 근거**로만
남겨 두고, 실제로 지금 가능한 최악의 경우는 바로 아래 3-3-1절의
새 표를 봐야 한다. **또한 아래 "9,900원 대비" 칸의 표현을 6차에서
고쳤다** — 이 계산은 결제 수수료·장소/경로 API 원가만 뺀 것이지
서버 호스팅비·이메일·무료 체험 손님 비용·환불·세금을 전혀 안 뺀
숫자라, "흑자/적자"라는 말은 실제 손익을 뜻하는 것처럼 오해를 줄 수
있어 쓰지 않는다 — 대신 "매출 − 표에 포함된 변동비"라는 좁은
의미의 차액으로만 표시한다:

| 시나리오(한도 도입 전 가정 — 이제 실제로는 불가능할 수 있음) | Places 조회 원가(건수×44.8원) | Routes 계산 원가(건수×7원, Essentials 가정) | 토스 수수료(3% 가정) | Resend(무료 가정) | **표에 포함된 변동비 합계** | 매출(9,900원) − 위 변동비 (이윤이 아님 — 아래 주석) |
|---|---|---|---|---|---|---|
| 50곳 · 월 10회 | 55건 = 약 2,464원 | 10건 = 약 70원 | 약 297원 | 약 0원 | **약 2,831원** | +약 7,069원 |
| 160곳(제작자 실사례) · 월 30회 | 176건 = 약 7,885원 | 30건 = 약 210원 | 약 297원 | 약 0원 | **약 8,392원** | +약 1,508원 |
| 300곳 · 월 50회(6차 이전 가정 — 지금은 이용권 한도로 물리적으로 불가능할 수 있음) | 330건 = 약 14,784원 | 50건 = 약 350원 | 약 297원 | 약 0원 | **약 15,431원** | −약 5,531원(변동비가 매출 초과) |

**"매출 − 변동비"는 이윤·순이익·흑자가 아니다** — 이 표에 포함된
항목은 결제수수료·Places/Routes API 원가뿐이다. **서버 호스팅비,
이메일(Resend), 결제 안 하는 무료 체험 손님의 비용, 환불, 세금은 이
표 어디에도 포함돼 있지 않다**(각 항목이 포함/제외됐는지는 매 계산마다
명시한다는 원칙 — 6절·3-3-1절 참고). 이 넷을 반영하면 실제 순이익은
이 표의 숫자보다 항상 더 낮다.

**참고 — 이제는 역사적 배경으로만 남긴 4차 문서의 낙관 오류**: 4차
문서는 Places 단가를 실제보다 약 3분의 1 수준으로 낮게 잡아 "가장
비싼 시나리오도 원가 5,650원으로 남는 장사"라고 적었었다. 5차에서
정확한 단가(44.8원/건)로 다시 계산하자 300곳·월 50회 시나리오가
매출을 초과한다는 게 드러났고, 6차에서는 애초에 그런 사용량 자체를
이용권 한도로 못 만들게 막았다(아래 3-3-1절).

이 표는 **"장소 조회 원가가 이용권 원가의 대부분을 차지한다"는 구조를
보여주기 위한 것**이지, 확정 원가표가 아니다 — 위 계산의 전제(Places·
Routes 등급별 정확한 실제 단가, 토스 수수료율, Google 무료 크레딧
유무)가 전부 미검증이라는 걸 다시 강조한다. **확정된 것은 3-2절의
"건수"뿐이고, 3-3절의 "단가"는
전부 추정**이다 — 이 둘을 섞어 계산한 위 표의 "원가"도 당연히
추정치다. 실제 계약 후 단가만 갱신하면 이 표 구조 그대로 진짜 원가가
나온다.

### 3-3-1. 6차 갱신 — 이용권 한도가 실제로 씌워진 뒤의 진짜 최악 원가

**핵심: "저장 개수"·"실제 유료 조회 건수"·"경로 요청 건수"·
"실패·재시도 건수"는 서로 다른 변수이고, 각각 따로 계산해야 한다**
(지시 5절 반영 — 이 넷을 하나로 뭉뚱그려 "300곳 저장 = 300건 조회
원가"처럼 계산하던 5차까지의 방식이 오해를 만들었다):

- **저장 개수(storage count)** — `PUT /api/places`에 들어 있는
  장소 배열 길이. **몇 곳을 저장하든 그 자체는 0원이다**(가져오기
  자체는 유료 API를 안 부른다 — `server/test/reliability-and-cost.test.mjs`
  로 이미 확인된 사실, 아래 3-2절 첫 행). 300곳을 저장한 손님도
  실제로 위치 확인을 한 번도 안 누르면 원가는 0원이다.
- **실제 유료 위치 조회 건수(billable lookup count)** — 저장 개수와
  무관하게, **엔타이틀먼트 한도가 실제로 씌우는 값**이다:
  무료체험 계정은 평생 최대 10곳, 유료 이용권은 30일 기간당 최대
  50곳(`config.entitlementUsage`). 이미 위치가 확인된 장소를 다시
  조회해도(재확인·재수입 등) 이 한도를 또 깎지 않는다(영구
  dedupe — `entitlement_place_confirmed`).
- **경로 요청 건수(route request count)** — 코스 생성 성공 횟수와도
  다르다: 정거장이 25곳을 넘으면 한 번의 코스 생성이 여러 건의 Routes
  호출로 나뉜다(3-1절). 코스 생성 성공 자체는 무료체험 평생 1회,
  유료 30일 기간당 최대 30회로 한도가 씌워진다.
  (`config.entitlementUsage.paidCourseLimit`).
- **실패·재시도 건수(retry count)** — **이용권 한도의 보호를 안
  받는 유일한 변수다.** 실패(추정 결과)·재조회·타임아웃 재시도는
  "성공"이 아니라서 고객에게 약속한 사용 횟수는 안 깎지만, 서버가
  실제로 외부 API를 부르는 건 맞아서 **진짜 회사 비용은 그대로
  든다.** 이 변수만은 고객 약속(엔타이틀먼트)이 아니라 **내부 비용
  안전상한**(`config.costSafetyCap`, 6차 신규)이 유일한 방어선이다 —
  무료체험 계정 누적 700원, 유료 이용권 하나당 누적 3,500원을 넘어서는
  순간 그 이상의 실제 유료 호출 자체를 막는다(`server/cost-ledger.mjs`의
  `chargeCostBatch`, `server/test/cost-safety-cap.test.mjs`).

**이 넷을 분리해서 계산한 5가지 시나리오**(요청 5절이 지정한 시나리오
그대로):

| 시나리오 | 저장 개수 | 실제 유료 조회 건수 | 코스 생성 성공 건수 | 계산(원가) | 비고 |
|---|---|---|---|---|---|
| 300곳을 저장했지만 실제 위치 확인은 10건만 함 | 300곳 | 10건 | 0회(위치 확인만, 코스 미생성) | 10 × 44.8원 = **약 448원** | 저장 개수는 원가에 전혀 영향 없음을 보여줌 — 5차까지의 "300곳=14,784원" 계산은 "300곳을 전부 조회했다"는 가정이 만든 착시였다 |
| 유료 이용권 하나가 한도(위치확인 50+코스 30)를 전부 소진 | 무관(50곳 이상) | 50건(한도 최대치) | 30회(한도 최대치) | 50×44.8 + 30×7 = 2,240 + 210 = **약 2,450원** | 이게 **엔타이틀먼트 한도만으로 정해지는, 실패·재시도가 전혀 없을 때의 이론적 최댓값**이다. 9,900원 매출 대비 약 7,450원의 표-포함-변동비 차액(흑자 아님, 위 주석과 동일 원칙) |
| 재방문 손님이 이미 확인된 장소를 재사용(신규 조회 0건) | 저장 300곳(전부 기존 확인 장소) | **0건**(전부 재사용 — 영구 dedupe로 미차감) | 10회 | 10 × 7원 = **약 70원** | "재방문 여행자 지원"이 실제로 원가를 낮추는 방향으로 작동함을 보여줌 — 재방문 손님은 오히려 원가가 가장 낮은 축에 든다 |
| 무료체험 가입자가 많고 유료 전환율이 낮음(예: 가입 100명 중 5명만 결제) | 계정별 상이 | 계정당 최대 10건(무료 한도) | 계정당 최대 1회(무료 한도) | 계정 1명 최악 = 10×44.8+7 = **약 455원**, 가입 100명이 전부 한도까지 다 쓰면 최악 총액 = 100×455원 = **약 45,500원**(매출 0원) | 이게 R6-4(안전상한 700원)가 실제로 계산했던 "무료체험 최악치 ≈455원"과 일치 — 700원 상한 안에 여유 있게 들어옴. 하지만 **전환율이 낮을수록 이 무료 비용의 총합이 순수 비용**이라는 점은 그대로다(4절 "확인 안 된 가설" 참고) |
| 실패·재시도가 늘어나는 경우(경로 분할 포함) | 무관 | 성공 50건(한도) + 실패/재시도 N건 | 30회(한도, 실패는 미차감) | 최소 2,450원(위 이론적 최댓값) + 실패·재시도 비용, 단 **내부 안전상한(3,500원)에서 그 이상의 실제 호출을 강제로 막음** | 실패·재시도가 아무리 늘어나도 이 이용권 하나의 실제 비용은 **3,500원을 넘을 수 없다**(구조적 상한 — `server/test/cost-safety-cap.test.mjs`로 확인) |

**결론 — 6차 이전과 달라진 것**: 5차까지는 "이용권 사용량에 실질적
상한이 없어서, 헤비유저 한 명이 원가를 매출보다 크게 만들 수 있다"는
게 실제 위험이었다. 6차에서 이용권 사용량 한도(엔타이틀먼트)와 내부
비용 안전상한을 함께 도입한 결과, **유료 이용권 하나의 실제 원가는
이제 이론적으로도(엔타이틀먼트 기준) 약 2,450원, 실패·재시도까지
감안한 절대 상한(안전상한 기준)으로도 3,500원을 넘지 않는다** — 이
숫자와 9,900원 매출의 차액(표에 포함된 항목 기준 약 6,400~7,450원)을
"흑자"라고 부르지 않는 이유는 위 주석과 같다(서버 호스팅비·이메일·
무료 체험 손님 비용·환불·세금 전부 미포함). **다만 안전상한 자체가
충분한지는 별개 질문이다** — 3,500원 상한이 30회 코스 생성을
정상적으로 다 제공하기에 부족하면(즉 정상 사용만으로 상한에 먼저
닿으면) 그건 "손님이 약속받은 걸 다 못 받는" 문제이므로, 위 표의
"이론적 최댓값 2,450원 < 안전상한 3,500원"이라는 여유(약 1,050원)가
계속 유효한지 실제 단가 확정 후 다시 계산해 확인해야 한다.

**무료 사용자(체험 1회만 쓰고 결제 안 함)의 비용 — 6차 갱신, 이용권
한도 반영 후 다시 계산**: 5차까지는 무료 체험 손님이 저장한 장소
수만큼 전부 위치 확인을 할 수 있다고 가정해 "300곳 저장 = 약
13,447원"이라는 표를 실었다. **6차에서 무료체험 계정에도 위치 확인
한도(평생 최대 10곳, `config.entitlementUsage.freePlaceLookupLimit`)를
실제로 씌웠으므로, 저장 개수와 무관하게 무료 체험 손님 한 명의 실제
유료 조회 원가는 이제 "최대 10건"으로 고정된 상한이 있다**:

| 항목 | 계산 | 비고 |
|---|---|---|
| 무료체험 1명 최악 원가(위치확인 10건 + 코스 1회) | 10×44.8원 + 7원 = **약 455원** | 저장 개수(50곳이든 300곳이든)와 무관 — 3-3-1절 시나리오 4와 동일 계산. 내부 비용 안전상한(700원)보다 낮아 상한이 정상적으로 여유를 두고 있음 |
| 가입 100명, 전부 한도까지 다 씀(현실적 상한 없음, 최악 가정) | 100×455원 = **약 45,500원**(매출 0원) | 무료→유료 전환율이 낮을수록 이 비용의 총합이 순수 회사 부담이라는 점은 그대로다 — 정확한 전환율은 4절대로 검증 전 |

**즉 6차부터는 "저장 장소가 많은 무료 체험자"라는 이유만으로 원가가
치솟지 않는다** — 위치 확인 한도가 저장 개수와 완전히 분리돼 있기
때문이다(지시 1절 "저장 개수와 실제 조회 횟수를 분리하라"를 그대로
반영한 구조). 남는 진짜 위험은 "가입자 수 × 낮은 전환율"이지 "가입자
1명이 저장을 얼마나 많이 했는가"가 아니다 — **이 비용 구조 때문에
무료체험 한도(`ENTITLEMENT_FREE_PLACE_LOOKUP_LIMIT` 등)와 내부 비용
안전상한(`COST_SAFETY_CAP_FREE_KRW_MICROS`, 6차 신규)이 단순한 남용
방지 그 이상으로 실제 예산 방어선 역할을 한다**는 점을 분명히 해 둔다.

**무료 구간 적용 전/후 구분**: Google Maps Platform에 월 무료
사용량이 있다면(3-3 표 참고, 정책 자체가 불확실) 그 구간 안에서는
위 표의 Places·Routes 원가가 0원에 가깝고, 그 구간을 넘긴 뒤부터
표의 단가가 실제로 적용된다. 이 문서는 **무료 구간이 없다고
가정한 보수적(최악) 시나리오**로 위 표를 계산했다 — 실제로는 이보다
쌀 가능성이 있지만, 비쌀 가능성은 낮다(정책이 지금보다 더 나빠지는
쪽으로 바뀔 근거는 없다).

### 3-3-2. 7차 갱신 — 계정별 하루 비용 한도가 위 이론적 최댓값과 충돌하던 버그를 고침

**6차까지는 위 3-3-1절의 "이론적 최댓값 2,450원"이 실제로는 하루 안에
받을 수 없는 값이었다** — 계정별 하루 비용 한도(`costBudget.
perAccountDailyMicros`, 기본 500원)가 무료/유료 구분 없이 똑같이
적용됐기 때문에, 위치확인 단가(약 44.8원) 기준 하루에 약 11건만
쓰면 그날은 더 이상 위치 확인이 안 됐다. 유료 손님이 "하루 안에
50곳을 몰아서 확인"하려 하면(예: 여행 출발 전날 한꺼번에 정리하는
흔한 사용 패턴) 실제로는 여러 날에 나눠 써야만 하는 결과가 됐다 —
**이건 의도한 정책이 아니라, 무료체험 크기(10곳+코스1회 ≈ 500원
이내)에 맞춰 정한 값을 유료에도 그대로 재사용한 버그였다.**

7차에서 계정별 일일/월간 비용 한도가 이 계정의 이용권 기간 전체
누적 안전상한(무료 700원/유료 3,500원)보다 낮게 잡히지 않도록
고쳤다(`server/cost-ledger.mjs`). 그 결과 위 3-3-1절의 "이론적
최댓값(약 2,450원, 실패·재시도까지 감안한 절대 상한 3,500원)"이
**실제로 하루 안에도 받을 수 있는 값**이 됐다 — 다만 **이용권 기간
(30일) 전체 누적 총량은 그대로 3,500원 이하로 유지**된다(하루 안에
몰아 쓸 수 있을 뿐, 매일 3,500원씩 새로 받는 게 아니다).

**서비스 전체 하루/월간 비용 한도(`COST_GLOBAL_DAILY_KRW_MICROS`
기본 3,000원, `COST_GLOBAL_MONTHLY_KRW_MICROS` 기본 27,000원)는
이번 수정에서 건드리지 않았다** — 이건 개별 계정 문제가 아니라
회사 전체 예산 상한이라 ChatGPT가 판단할 사업 결정 영역이라고
보기 때문이다. 다만 계산상 참고할 점: 유료 손님 한 명이 하루 안에
전체 이용권 몫(약 2,450원)을 몰아 쓰면, **서비스 전체 하루 한도
(3,000원) 안에서는 그런 손님을 하루에 1명 정도만 감당**한다(그
이상 동시에 몰리면 전체 하루 한도에 먼저 걸려 그 이후 요청은
거부된다 — 개별 손님 잘못이 아니라 순전히 트래픽이 몰린 결과다).
**실제 유료 손님이 여러 명 생기고 이런 "몰아 쓰기"가 흔해지면, 이
전체 한도를 예상 동시 손님 수에 맞춰 올려야 한다** — 코드는 이미
환경변수로 조정 가능하게 돼 있고(`COST_GLOBAL_DAILY_KRW_MICROS`
등), 실제 숫자를 얼마로 올릴지는 실사용 데이터를 보고 ChatGPT와
함께 정할 사업 결정이다.

### 3-3-3. 7차 갱신 — 신규 장소 확인 한도 우회 차단(매출 누수 위험 해소)

ChatGPT가 실제로 재현한 우회: 무료 위치확인 한도를 1로 두고, 같은
클라이언트 장소 식별자에 검색어만 바꿔 보내면 서로 다른 실제 장소
6곳을 확인받고도 서버에 기록된 사용량은 1로만 남았다 — 즉 **이론상
무료체험 손님이 위치확인 한도를 무제한으로 우회해 유료 손님과 같은
가치를 공짜로 받을 수 있었다**(직접적인 매출 누수 위험). 7차에서
"신규 확인 여부"를 클라이언트가 불러주는 문자열이 아니라 공급자가
실제로 돌려준 장소 식별자로 재판정하도록 고쳤다(`server/entitlement-
usage.mjs`) — 같은 실제 장소를 재사용하는 정상적인 경우는 계속
비과금이지만, 실제로 다른 장소를 확인할 때마다 정직하게 한도가
깎인다. 사업적으로는 "무료체험이 무제한 위치확인 도구로 오용될 수
있었던 구멍을 막았다"는 뜻이다 — 광고 문구·판매 페이지에 별도로
반영할 내용은 없다(소비자에게 보이는 약속·가격은 그대로다).

### 3-3-4. 7차 신규 — 착장 판단용 날씨 카드(직접 비용 없음)

WeatherAPI.com 무료 등급(상업적 사용 허용, 월 100,000회, 공식
가격표 https://www.weatherapi.com/pricing.aspx 확인)으로 "오늘
동선" 화면에 날씨 카드를 추가했다. **이 기능 자체는 유료 계약이
없고 직접 비용이 0원이다**(무료 등급 안에서 동작). 서버가 지역
단위(~1km)로 캐시를 공유해 같은 지역을 여러 사용자가 봐도 실제
호출은 드물게 나가고, 서비스 전체 하루 호출 상한(`WEATHER_GLOBAL_
DAILY_CAP`, 기본 2,000회/일)도 걸어 뒀다 — 월 100,000회 한도를
지키기 위한 안전판이다. 이 한도가 실제 트래픽에 비해 너무 낮거나
높은지는 실사용 데이터로 나중에 조정하면 된다(코드 수정 없이
환경변수만 바꾸면 됨).

**가격 정책에 영향 없음** — 날씨는 무료(비회원) 사용자도 볼 수
있고, 위치확인·코스생성 이용권/비용 원장을 전혀 안 건드린다. 즉
이 기능을 판매 상품의 유료 항목으로 넣거나 가격에 반영할 필요가
없다 — 무료로 계속 제공해도 회사에 실질적 비용 부담이 없는
구조다.

### 3-3-5. 9차 신규 — 한도 소진 후 유료 검색 반복으로 인한 비용 상한 소진 위험 해소

ChatGPT가 지적한 위험: 이용권 한도를 다 쓴 계정이 새 검색어를 계속
보내면(재사용인지 신규인지는 실제 외부 호출 없이는 알 수 없으므로)
매번 실제 유료 Google Places 호출이 나가 비용 상한을 소진할 수
있었다 — "이미 확인된 실제 장소 재사용은 한도와 무관하게 공짜"라는
약속을 지키려다, 그 판정 자체에 필요한 외부 호출을 무제한 허용해
버린 것이다.

**해결**: 계정당 하루 "한도 초과 상태에서 시도해 볼 수 있는 횟수"에
작은 안전판을 뒀다(`entitlementOverLimitVerificationDailyLimit`, 기본
**20회/계정/일**). 이미 확인된 실제 장소 재사용(같은 로컬 슬롯+같은
검색 조건으로 이미 링크된 경우)은 이 안전판을 아예 거치지 않으므로
여전히 무제한 공짜다 — 이 안전판은 오직 "신규일 수도 있는 새 검색
시도" 횟수만 하루 단위로 제한한다.

**비용 영향(추정)**: 위치확인 단가 약 44.8원(3-3절) 기준, 계정당 하루
최대 약 20 × 44.8원 = **약 896원**의 추가 노출이 이론상 가능하다 —
단, 이건 "이용권 한도를 이미 다 쓴 계정이 매일 20건씩 새 검색을
반복하는 최악의 경우"이고, 실제로는 대부분 이용권 자체의 원가
안전상한(무료 700원/유료 3,500원 누적, 3-3절)이 먼저 걸린다. 이
20이라는 숫자는 확정 수치가 아니라 제안값이며, 실사용 데이터(정당한
재확인이 하루에 몇 건이나 필요한지)로 조정해야 한다.

**API 공급자 데이터 보관·캐시 조건**: 이 안전판·기존 로컬 링크
캐시(`entitlement_place_local_link`)가 Google Places API의 실제
"결과 보관·캐시 허용 기간" 정책과 맞는지는 이번 세션에서 공식
문서로 재확인하지 못했다(EGRESS_BLOCKED, 5-1절과 같은 제약). **이
안전판·캐시 구현이 "장소 정보를 무기한 자유롭게 저장해도 된다"는
승인은 아니다** — 실제 서비스 전 Google Places API 이용약관의 데이터
보관 조건을 사람이 직접 확인해야 한다(미검증 상태를 확인 완료로
표시하지 않는다).

### 3-3-6. 10차 신규 — AI 보조 분류 원가 검증(요청대로 ①~⑦ 항목별 보고)

**결론 먼저**: AI 보조 분류의 **실제 원가는 지금 시점에 검증할 수
없다** — AI 공급자·모델이 전혀 확정되지 않았고(사업 문서 어디에도
결정된 적 없다), 이 세션은 공식 가격 페이지 접속도 막혀 있다(3-0절과
같은 EGRESS_BLOCKED 제약). 그래서 아래 ⑤·⑥·⑦은 **미검증**으로
명시하고, 대신 ①~④(우리 코드가 실제로 어떻게 동작하는지)는 실제
코드를 합성 데이터로 돌려 정확히 계산했다.

**①~④ 산출 방법과 결과**: `scripts/analyze-classification-rate.mjs`가
`A.merge`/`A.parseCsv`(실제 daInfer/daInferTags 규칙 분류 코드, 10차
4절에서 고친 버전)로 합성 장소 이름 목록을 실제로 분류해 집계했다.
합성 데이터는 다국어·다업종(일본어·영어·한국어·이탈리아어 등, 명확한
이름 70% + "하나"·"모모" 같은 근거 부족 이름 30%로 임의 구성)을
섞었다 — **이 70/30 비율과 결과 성공률은 실제 고객의 저장 목록 분포를
안다고 주장하는 게 아니다.** 실제 고객 데이터는 확보한 적이 없고,
이 결과를 실제 평균 성공률로 일반화하면 안 된다(10차 지시 그대로).

| 시나리오 | ① 전체 장소 수 | ② 규칙으로 해결(상위분류 또는 태그 ≥1개) | ③ AI 후보(미해결) | ④ 배치 요청 수(20건/배치) |
|---|---|---|---|---|
| 최초 가져오기 50곳 | 50 | 33 | 17 | 1 |
| 최초 가져오기 160곳 | 160 | 106 | 54 | 3 |
| 최초 가져오기 300곳 | 300 | 196 | 104 | 6 |
| 이후 3곳 추가 | 3 | 3 | 0 | 0 |

("이후 3곳 추가"는 합성 표본에 우연히 근거가 뚜렷한 이름만 걸려 AI
후보가 0이 됐다 — 실제로는 애매한 이름이 섞이면 이보다 늘어날 수
있다. 10차 지시대로 "신규·변경된 항목 중 미해결 항목만" 처리하므로
기존 160곳은 재처리하지 않는다.)

**⑤ 입력·출력 토큰과 재시도 가정 — 미검증**: 배치당 항목 수(최대
20건, `config.aiClassify.maxItemsPerBatch`)와 항목당 필드(name/note/
address, 각 최대 200/300/200자로 서버가 자름 — `routes/ai-classify.mjs`
의 `sanitizeItem`)는 코드로 확정했지만, 이걸 실제 토큰 수로 환산하려면
프롬프트 형식과 모델의 토크나이저가 필요하다 — 공급자가 없어 계산할
수 없다. 재시도 횟수(타임아웃·malformed 응답 시 몇 번 다시 부를지)도
아직 정책이 없다(현재 코드는 재시도 없이 그 배치를 실패로 남기고
규칙 결과로 대체한다 — RELEASE_STATUS.md에 "재시도 없음"으로 명시).

**⑥ 모델·공식 단가·확인 날짜 — 미검증**: 없음(공급자 미확정). 확정되면
이 표의 빈칸을 채우는 구조는 이미 준비돼 있다 —
`config.mjs`의 `aiClassify.placeholderPerItemMicros`(현재 예산 코드
경로 검증용 자리표시자, 3원/건)를 실제 공식 단가로 교체하고, 이 절의
표를 그 값으로 다시 계산하면 된다.

**⑦ 분류 비용과 장소·코스 비용을 합친 추정액**: 장소조회·코스생성
비용은 위 3-3절 단가(학습 기억 기준 자릿수 추정, 역시 미검증)를 그대로
쓰고, AI 분류 비용은 **자리표시자 단가로 계산한 참고용 숫자**(실제
비용이 아님을 표 제목에 그대로 남긴다)로 병기한다.

| 시나리오 | 장소조회 비용(3-3절 단가, 미검증) | AI 분류 비용(자리표시자 3원/건, 실제 아님) | 합계(참고용, 실제 아님) |
|---|---|---|---|
| 50곳 | 약 50 × 44.8원 = 2,240원 | 17 × 3원 = 51원 | 약 2,291원 |
| 160곳 | 약 160 × 44.8원 = 7,168원 | 54 × 3원 = 162원 | 약 7,330원 |
| 300곳 | 약 300 × 44.8원 = 13,440원 | 104 × 3원 = 312원 | 약 13,752원 |

이 합계는 "AI를 실제로 켰을 때 얼마가 드는지"에 대한 답이 아니다 —
공급자·단가가 없는 상태에서 코드 구조(비용 예산 예약→차감 경로)가
정상 동작하는지 보여주는 참고 숫자일 뿐이다.

**예산 통합(요청대로 기존 비용 통제 안에 포함)**: AI 분류 비용은 별도
상한을 새로 만들지 않고, 기존 이용권 기간 원가 안전상한(무료 700원/
유료 3,500원 누적, 3-3절)에 **같은 원장(cost_ledger)의 같은
`period_id`로 합산**된다(`server/cost-ledger.mjs`의 `periodCostMicros`가
서비스·SKU와 무관하게 그 기간의 전체 합계를 본다 — 새 로직을 만들
필요가 없었다). 단, "AI가 예산을 먼저 소진해 핵심 제공량(위치확인
50곳·코스 30회)을 막으면 안 된다"는 지시에 따라, `entitlement-usage.mjs`
의 `aiClassifyBudgetHeadroomMicros`가 **남은 핵심 제공량의 예상 원가를
먼저 예약**해 두고 AI는 그 예약분을 뺀 나머지 여유만 쓸 수 있다.

무료체험(700원 상한)으로 이 계산을 실제로 해 보면: 남은 위치확인
10건(약 448원) + 남은 코스 1회(약 7원, Routes Essentials 단가) =
약 455원이 예약되고, AI에게 줄 수 있는 여유는 **약 245원뿐**이다(자리
표시자 단가 3원/건 기준 약 81건 분류 가능 — 다시 말하지만 실제 단가가
아니라 참고용 계산이다). **이 숫자가 뜻하는 바**: 무료체험 규모(700원)
안에서는 AI에게 줄 여유가 애초에 크지 않다 — 만약 실제 단가가 이
자리표시자보다 훨씬 크면(토큰 기반 과금은 대개 이보다 비쌀 가능성이
있다), 무료체험에서는 AI 보조 분류를 사실상 못 켜는 조건이 나올 수
있다. **이 경우 기존 제공량(50곳·30회)과 AI 기능을 동시에 만족시킬
수 없다면, 가격·제공량·비용상한을 임의로 바꾸지 않고 이 사실 그대로
ChatGPT/사용자에게 보고한다** — 지금 시점의 결론은 "실제 단가가
나오기 전까지는 무료체험에서 AI 보조 분류가 제한적이거나 꺼져 있을
가능성이 높다"는 것이다.

**구조 vs 실제 연결 구분(10차 지시)**: 여기까지는 전부 **구조 구현 +
모의(mock) 검증**이다. 실제 AI 공급자 연결은 없다
(`server/adapters/ai-classify.mjs`가 'real' 모드 자체를 구현하지
않는다) — `/api/health` 같은 응답으로 "AI 연결 확인됨"이라고 말한 적도
없고, 실제 키도 이 대화·ZIP·저장소 어디에도 없다. AI 키가 없으므로
**실제 AI 분류는 미완료**다. 규칙 기반 분류·수동 편집은 AI 상태와
무관하게 계속 정상 동작한다(`config.services.aiClassify==='disabled'`
여도 `daInfer`/`daInferTags`/`tagsEditSheet`는 그대로 쓸 수 있다 —
10차 5절 재현 테스트로 확인됨).

### 3-4. 초기 운영 예산(월 5만원) 배분 제안 — 확정 아님

**5차 갱신 핵심 지적 반영**: "월 5만원은 초기 전체 운영 목표이며
전액 Google 예산이 아니다. 무료 구간 소진 후 원가와 무료 체험자
비용까지 계산하고, 유료 사용자가 늘었는데 전체 예산을 고정해 서비스를
계속 막는 방식도 피하라"는 지시에 따라, 이 절은 **① 초기 비용
상한**과 **② 매출 증가 시 예산을 늘리는 기준**을 명확히 구분해서
정리한다 — 이전 문서(4차까지)는 이 둘을 구분하지 않고 "월 5만원 →
Google 예산 3만원"이라는 고정값 하나만 제시했었다.

**월 5만원 목표를 고정비와 Google 예산으로 나누는 배분(가정)**:

| 항목 | 성격 | 월 예상액 | 근거 |
|---|---|---|---|
| 서버 호스팅(작은 VM 또는 컨테이너 1대) | 고정비 — **가정치**(실제 견적 아님) | 약 5,000~15,000원 | 국내외 소규모 Node.js 호스팅사의 최저가형 요금제 자릿수 추정(정확한 사업자·요금제 미정이라 확인 안 됨) |
| DB 백업(서버가 실제로 켜지면 필요 — 6절 4번) | 고정비 — **가정치** | 약 0~3,000원 | 파일 기반 SQLite를 별도 스토리지에 주기적으로 복사하는 정도로 가정 — 실제 백업 대상(오브젝트 스토리지 등) 미정 |
| Resend 이메일 | 고정비에 가까움(소량은 무료 등급 가능성) | 약 0원(가정) | 3-3절 참고 — 무료 등급 정확한 한도는 미확인 |
| 토스페이먼츠 | 고정비 아님(거래 건당 변동비, 이미 위 원가 계산에 포함) | — | 3-3절 참고 |
| **고정비 소계(가정)** | | **약 5,000~18,000원** | |
| **Google API(Places+Routes) 예산 — 남는 금액** | | **약 32,000~45,000원** | 5만원 − 위 고정비 가정 |

**① 초기 비용 상한(코드에 실제로 반영된 값)**: `server/config.mjs`의
`costBudget.globalMonthlyMicros` 기본값은 **27,000원/월**이다 — 위
Google 예산 범위(약 32,000~45,000원)의 하단보다도 낮게, 안전 여유
15%(`costSafetyMarginRatio`)를 적용해 보수적으로 잡았다(고정비 가정이
빗나갈 여지, 예상치 못한 SKU 추가 호출 등을 대비). **이건 "서비스
전체가 한 달에 쓸 수 있는 절대 상한"이고, 이 한도에 도달하면
`isServiceUnavailableForNewSales`(R5-4 항목)가 신규 결제 자체를
막는다** — 이미 결제한 손님의 이용권은 안 건드린다.

**이 초기 상한으로 몇 명을 감당할 수 있는가 — 6차 갱신: 이용권 한도
도입 후에는 "헤비유저"의 정의 자체가 바뀐다.** 5차까지는 손님이
저장·재계산을 얼마든지 반복할 수 있어 "300곳·월 50회"급 사용자가
실제로 나올 수 있었다. 6차부터는 유료 이용권 하나가 30일 동안 실제로
쓸 수 있는 최댓값이 위치확인 50건+코스 30회로 못 박혀 있으므로,
**"한 이용권이 낼 수 있는 최악의 원가"는 이제 하나의 숫자(약
2,450원, 실패·재시도까지 넣어도 안전상한 3,500원)로 수렴한다**
(3-3-1절). 이 상한 기준으로 다시 계산하면:

| 유료 손님 유형 | 30일 원가 | 27,000원 예산으로 감당 가능한 명수 |
|---|---|---|
| 가벼운 사용(위치확인 10건+코스 5회 정도) | 약 483원 | 약 55명 |
| 이용권 한도를 전부 소진(위치확인 50건+코스 30회, 이론적 최댓값) | 약 2,450원 | 약 11명 |
| 실패·재시도까지 겹쳐 안전상한에 도달(구조적 절대 최댓값) | 약 3,500원 | 약 7명 |

**이 표가 뜻하는 것**: 이용권 한도 도입 전(5차까지)에는 "300곳·월
50회급 헤비유저 1명이 예산을 통째로 위협"할 수 있었지만, 6차부터는
**한 이용권이 낼 수 있는 최악의 원가 자체가 3,500원으로 구조적으로
막혀 있어서, 27,000원 예산으로 최소 약 7명은 "무슨 일이 있어도"
감당할 수 있다는 하한선이 생겼다.** 다만 이 계산도 Places/Routes
단가가 지금의 추정치(3-3절)와 실제로 얼마나 다른지에 따라 달라진다
— 실제 계약 단가 확정 후 반드시 다시 계산해야 한다. 계정별 일일/월간
한도(`COST_PER_ACCOUNT_DAILY_KRW_MICROS`,
`COST_PER_ACCOUNT_MONTHLY_KRW_MICROS`, 5차 신설)와 이번 6차의 내부
비용 안전상한은 서로 다른 층위의 방어선이다 — 전자는 하루/한 달
단위의 속도 제한, 후자는 "이 이용권 하나가 평생(30일 기간 동안) 낼 수
있는 절대 최댓값"이다.

**② 매출 증가 시 예산을 조정하는 기준(고정하지 않는다)**: "유료
사용자가 늘었는데 전체 예산을 고정해 서비스를 계속 막는 방식도
피해야 한다"는 지시에 따라, 이 문서는 27,000원을 **영구 고정값이
아니라 초기 안전값**으로 규정한다. 유료 손님 수가 늘면 다음 규칙으로
`COST_GLOBAL_MONTHLY_KRW_MICROS`를 올릴 것을 제안한다(확정 아님 —
실제 결정은 매출이 실제로 들어온 뒤 사업 쪽에서):

```
다음 달 Google 예산 제안 = (이번 달 실제 유료 결제 건수 × 9,900원 × 손님당
                            평균 원가 비율(3-3절 기준 약 30~150%)) × 1.2(여유)
```

즉 **"이번 달 실제로 들어온 매출에 비례해 다음 달 예산을 올린다"**는
원칙이다 — 매출과 무관하게 27,000원을 영원히 고정하면, 유료 손님이
늘어도 서비스가 계속 막히는(가장 나쁜) 상황이 된다. 반대로 매출
증가 없이 예산만 무한정 올리면 회사가 적자를 떠안는다 — 그래서
"매출에 비례한 조정"을 원칙으로 제안한다. 실제 조정 주기(매달/분기)
와 승인 절차는 사업 쪽에서 정할 결정 사항으로 남긴다(5-2절).

**경로·이메일 등 다른 외부 호출 비용도 검토했는가**: 그렇다 — 위
계산에 Resend(이메일)와 Google Routes(경로)를 전부 포함했다. Places가
가장 큰 비중을 차지하는 이유는 단가 자체보다 **건수가 압도적으로
많기 때문**이다(코스 생성은 계정당 월 수십 건이지만, 장소 조회는
저장 목록 규모(수백 건)에 비례한다).

### 3-5. 제안 — 사용 한도 (확정 아님, 검토용 제안 — 갱신)

**이 숫자들은 개발 담당이 엔지니어링 안전장치로 제안하는 값이지,
사업적으로 확정된 한도가 아니다.** 실제 값은 ChatGPT/사용자가 정할
몫이다. 2차 갱신 때의 제안("코스 생성 30일 N회", "위치 조회 하루
M회")은 사용자가 명시적으로 반려했다 — "위치 조회 하루 30회"로
묶으면 후쿠오카 160곳을 처음 정리하는 사용자부터 막힌다는 이유였다.
그래서 이번에는 **초기 가져오기와 이후 재조회를 애초에 다른 값으로
분리해 코드로 구현**했다(3-1절) — 이제 남은 결정은 "그 분리된 두
값과 코스 생성 한도를 각각 얼마로 둘지"뿐이다.

- **코스 생성·재계산**(`GENERATION_RATE_LIMIT_PER_HOUR`, 지금 기본값
  시간당 10): 지금 값은 "동시 다발적 남용을 막는 임시 안전판"이지
  사업적으로 설계된 월간 한도가 아니다. 30일 단위로 다시 볼지, 지금의
  시간당 한도로 충분한지는 실제 초기 체험단(5~10명) 관찰 후 정하는
  게 안전하다.
- **초기 가져오기**(`PLACE_LOOKUP_IMPORT_DAILY_LIMIT`, 기본
  400/일): 300곳을 하루 안에 다 정리해도 안 막히도록 넉넉하게 잡은
  값 — 후쿠오카 160곳 사례를 실제로 통과시킬 수 있는지 기준으로
  골랐다.
- **재조회**(`PLACE_LOOKUP_REQUERY_DAILY_LIMIT`, 기본 60/일): 하루에
  같은 계정이 "위치 확인" 버튼을 60번 넘게 누르는 건 일반 사용
  패턴으로 보기 어렵다는 가정 — 이것도 실제 관찰 전 임의 가정이다.
- 모든 한도는 **막을 때 "한도 초과"라고 정직하게 알린다** — 조용히
  실패하거나 가짜 성공을 보여주지 않는다(이 세션 전체를 관통하는
  원칙과 동일, 이미 구현·테스트됨).
- 위 세 값 모두 `server/config.mjs`의 환경변수라 코드 수정 없이
  숫자만 바꿀 수 있다(가격과 같은 패턴).

### 3-6. 재방문 여행자(반복 구매) 지원 — 비용·가격 구조(5차 신규)

같은 도시·나라를 반복 방문하는 여행자(마케팅에서 "재방문 여행자"로
부르는 손님층)를 위해 여행 분리(trips)·방문 기록(visits)·다음 여행
코스 후보 제안 기능을 이번에 추가했다(서버는 완료, 화면 연결은
`docs/RELEASE_STATUS.md` 7절 참고). 이 기능이 **가격·비용 구조에
어떤 영향을 주는지** 정리한다.

- **새 가격 구간을 만들지 않는다** — "재방문 여행자용 요금제"
  같은 것은 없다. 기존 9,900원/30일 이용권 구조를 그대로 쓴다.
- **기록 자체(여행 만들기·방문 표시·과거 여행 열람)는 항상 무료다**
  — `server/routes/trips.mjs`, `visits.mjs`,
  `next-trip-suggestions.mjs` 어디에도 유료 외부 API(Places/Routes)
  호출이 없다(순수 DB 읽기/쓰기). 여행을 몇 개를 만들든, 도시를 몇 번
  바꾸든, 방문 표시를 몇 번을 하든 **비용이 전혀 안 든다** —
  `server/test/trips-and-visits.test.mjs` §4에서 실제로 확인했다.
- **새 코스를 실제로 생성/재계산할 때만 비용이 든다** — 이건 재방문
  여행자든 처음 오는 여행자든 완전히 동일한 기존 정책(무료체험 1회 +
  이후 유료)을 따른다. 재방문 여행자라고 코스 생성이 더 싸거나
  비싸지 않는다.
- **여행을 바꾸거나 도시를 전환해도 무료체험이 다시 채워지지
  않는다** — 계정당 무료체험은 여전히 평생 1회다(여러 여행을 만들 수
  있다고 해서 무료체험도 여행 개수만큼 늘어나는 게 아니다). 이는
  `server/test/trips-and-visits.test.mjs` §4로 확인됐다.
- **이용권이 만료된 뒤에도 여행·방문 기록은 계속 보인다** — 새 코스
  생성만 막히고, 과거에 만든 여행·방문 표시·메모는 전부 그대로
  남는다(만료 후에도 기존 코스 열람은 유지된다는 기존 원칙, 1절과
  동일한 원칙을 여행 단위로 확장한 것).
- **이 기능이 "재구매"를 늘리는 방식은 가격이 아니라 경험이다** —
  "저장해놓고 못 간 곳을 다음 여행에서 마저 가보기"가 재구매의
  동기가 되길 기대하는 것이지, 재방문 손님에게 별도 할인·구독을
  파는 구조가 아니다(마케팅 문서 참고,
  `docs/MARKETING_LAUNCH_PLAN.md`).
- **비용 예측에 주는 영향**: 이 기능 자체는 3-1~3-4절의 비용 계산에
  새 항목을 추가하지 않는다(유료 API 호출이 없으므로). 다만 "다음
  여행 코스 후보 제안"이 재구매를 실제로 늘린다면, 같은 계정이 한
  달 안에 여러 번 코스를 재계산하는 빈도가 늘 수 있다 — 이건 이미
  3-3절 "300곳·월 50회" 시나리오가 다루는 것과 같은 종류의 사용량
  증가이므로, 별도 표를 만들지 않고 기존 헤비유저 계산에 포함된
  것으로 본다.

## 4. 확인 안 된 가설 (숫자·주장을 만들지 않음)

- 9,900원/30일이라는 확정 가격에 **실제로 돈을 낼 사람이 얼마나
  있는지**는 여전히 검증 전이다. 검증 계획은
  `MARKETING_LAUNCH_PLAN.md` 4절(초기 체험단 5~10명) 참고.
- **Google Routes 실제 성공률** — 이 세션은 `developers.google.com`
  으로 나가는 네트워크가 막혀 있어 실제 키로 성공 응답을 한 번도
  못 받아봤다(2절 참고). 실제 인터넷 환경·실제 키로 다시 확인 필요.
- **3-2절의 재조회 비율(저장 장소의 10%/월) 가정** — 실제 관찰 전
  임의로 잡은 값이다. 체험단 운영 중 실제 재조회 횟수를 기록하면
  더 정확한 값으로 바꿀 수 있다.
- **3-3절의 API 단가·Google Maps Platform 무료 사용량 정책** — 이
  세션에서 공식 가격표를 열어보지 못해 학습 기억에만 의존했다.
  계약 전 재확인 전까지는 "확인 필요"로 취급해야 한다.
- **179건 실제 Google Takeout 데이터셋 재실행 — 이번 세션에서도
  여전히 차단됨**: 이 샌드박스에는 그 실제 파일이 없다. 필요하면
  사용자가 다시 올려줘야 한다.

## 5. 남은 결정 사항

### 5-0. 부분 환불 정책 미확정 — ChatGPT/사용자 결정 필요(별도 보고)

토스 결제가 부분 취소(예: 일부 금액만 환불)로 처리되면, 지금 코드는
**주문 상태만 `partially_cancelled`로 남기고 이용권을 자동으로
건드리지 않는다**(`server/adapters/payment-toss.mjs`의
`cancelPayment`/`handleTossWebhookEvent`). 이건 "정책이 없어서 아무
것도 안 하기로 한 것"이지, "부분 환불 시 이용권을 유지하기로 확정한
것"이 아니다 — 다음 질문이 아직 열려 있다:

- 부분 환불 비율만큼 이용권 기간을 비례해서 줄여야 하는가(예: 절반
  환불이면 남은 15일만 인정)?
- 아니면 부분 환불은 결제 금액 조정일 뿐 이용권과는 무관하게 둘 것인가
  (환불 사유가 "가격 조정" 같은 것이라면 이 편이 자연스러울 수 있다)?
- 부분 환불이 일정 비율(예: 50% 이상)을 넘으면 전액 취소와 동일하게
  취급할 것인가?

**이 질문에 대한 답이 나오기 전까지, 소비자 화면에는 부분 취소를
받을 수 있는 UI 자체를 만들지 않는다**(완성되지 않은 기능을 완성된
것처럼 제공하지 말라는 지시 반영 — 지금 부분 취소는 토스 쪽에서
발생한 이벤트를 서버가 안전하게 "기록만" 하는 수준이지, 사용자가
스스로 요청할 수 있는 기능이 아니다). 결정이 나면
`revokeEntitlementIfCurrentOrder` 호출 부분에 그 정책을 반영하면 된다
(코드 구조는 이미 분리해 뒀다).

### 5-1. 이메일·결제·경로 — 실제 키로 첫 연결 확인

공급자는 확정됐고(Toss/Resend/Google Places+Routes) 그 API를
호출하는 코드도 작성됐지만, **이 세션은 세 공급자의 공식 문서
사이트에 전부 접속이 막혀 있어(EGRESS_BLOCKED) 스펙을 다시 확인하거나
실제 키로 검증하지 못했다.** 실제 키를 발급받아 처음 넣어 볼 때
아래를 반드시 확인해야 한다:
- Resend: 로그인 코드 요청을 눌러 본인 이메일에 실제로 도착하는지.
- 토스페이먼츠: 결제창이 실제로 뜨고, 결제 완료 후 서버 로그에
  승인(confirm) 응답이 정상적으로 오는지, 웹훅이 실제로 우리 서버
  형식과 맞는지(코드 작성 시 기억에 의존했으므로 실제 스펙과 필드명이
  다를 가능성을 열어둔다).
- Google Routes: `computeRoutes` 응답이 우리가 기대한 필드 마스크로
  실제로 오는지, WALK 모드 요청이 거절되거나 다른 요금 등급으로
  청구되지 않는지.
이 셋 중 하나라도 실제 스펙과 다르면 어댑터 코드(`server/adapters/`)
만 고치면 되도록 이미 그 경계에서 분리해 뒀다(계약 테스트
`server/test/provider-contracts.test.mjs`가 그 경계를 지킨다).

### 5-2. 사용 한도 확정 + 헤비유저 원가 초과 문제 — 6차에서 (A)안으로 실행됨

**5차까지 남아 있던 결정**: 3-5절의 제안(코스 생성·초기 가져오기·
재조회 각각 상한)을 실제 숫자로 확정할지, 다른 방식(예: 초과분은
추가 결제로 풀기)으로 갈지 — (A) 저장·재계산 실사용량 자체를
9,900원으로 감당되는 범위로 제한 / (B) 구간별 요금 분리 / (C) 소수
헤비유저의 손실을 인수비용으로 감수, 셋 중 하나를 사업 쪽이 골라야
했다.

**6차 처리 — (A)안을 실제로 구현했다(개발 담당 임의 실행이 아니라,
이번 라운드 지시 "초기 테스트 상품의 사용 한도를 확정하라"에 따른
것)**: `server/entitlement-usage.mjs`가 유료 이용권 하나당 30일 동안
위치 확인 최대 50곳+코스 생성 최대 30회로 실사용량 자체를 막는다
(3-3-1절). 이 한도 안에서는 이론적 최댓값이 약 2,450원, 실패·재시도를
더해도 내부 안전상한(3,500원)을 구조적으로 못 넘는다 — "300곳·월
50회"급으로 원가가 매출을 넘던 5차 시나리오는 더 이상 발생할 수
없다. **(B)·(C)는 채택하지 않았다** — 가격 구간을 나누지도, 손실을
그냥 감수하지도 않고, 사용량 자체에 한도를 씌우는 방식으로 해결했다.

**단, 이 숫자들은 여전히 "확정된 시장가격"이 아니라 초기 테스트
상품 스펙이다** — 50곳/30회라는 한도가 실제 손님 만족도·재구매율에
비해 너무 박한지 넉넉한지는 아직 검증 전이고, Places/Routes 단가가
실제 계약가와 다르면 이 한도의 여유(약 1,050원, 3-3-1절)도 달라진다.
**출시 전 실사용 데이터로 다시 봐야 할 것**: (1) 유료 손님 대부분이
30회 한도 안에서 만족하는지, (2) 위 원가 계산이 실제 단가로도
유효한지, (3) 안전상한(3,500원)에 실제로 도달하는 계정이 얼마나
자주 나오는지(자주 나온다면 정상 사용도 막고 있다는 뜻이라 상한을
올려야 함).

### 5-3. 179건 실사용 데이터 재검증 여부

여전히 파일이 없어 재실행 못 함 — 필요하면 다시 받아야 한다.

### 5-4. 과거 커밋에 남은 개인정보 처리 여부

`docs/DESIGN_INTEGRATION_REPORT.md` 11절에 정리됨(현재 파일은 고쳤으나
git 히스토리에는 남아 있음) — 히스토리 재작성 여부는 아직 결정 안 됨.

---

## 6. 실제 전환에 필요한 계정·크리덴셜 — 어디서 설정하고 어떻게 확인하는지

**절대 채팅창에 실제 비밀키를 붙여넣지 말 것.** 실제 값은 서버가
실행될 환경(호스팅 서비스의 환경변수 설정 화면)에 직접 입력한다.

| 필요한 것 | 환경변수 | 어디서 설정 | 설정 후 확인 방법 |
|---|---|---|---|
| 운영 환경 표시 | `APP_ENV=production` | 서버 환경변수(운영 배포 시 반드시 설정 — 안 하면 development로 동작해 테스트 기능이 안 막힘) | `/api/health`의 `appEnv`가 `"production"`인지 확인 |
| 서버 호스팅 | — | Node.js를 지원하는 아무 호스팅(작은 VM, 컨테이너 서비스 등) 선택 후 `node server/index.mjs` 실행 | 배포 주소 + `/api/health`에 접속해 `{"ok":true,...}` 응답이 오는지 확인. `APP_ENV=production`인데 아래 필수값이 없으면 **서버가 아예 부팅을 거부하고 종료된다**(가짜로 뜨지 않음 — `assertBootReady`) |
| 장소 조회 API 키 | `GOOGLE_PLACES_API_KEY` | Google Cloud 콘솔 → API 및 서비스 → 사용자 인증 정보에서 발급, Places API(New)로 키 제한 설정 | 서버 환경변수에 넣고 재시작 → `/api/health`의 `services.placeLookup`이 `"real"`로 바뀌는지, `verified.placeLookup`에 실제 성공 시각이 찍히는지 확인 → 앱에서 위치 미확인 장소 상세의 "서버로 위치 후보 찾아보기"를 눌러 실제 후보가 오는지 확인 |
| 도보 경로 API 키 | `GOOGLE_ROUTES_API_KEY` | Google Cloud 콘솔에서 Routes API 활성화 후 발급(장소 조회와 같은 프로젝트여도 키는 분리 권장) | `/api/health`의 `services.routing`이 `"real"`로 바뀌는지 확인 → 실제로 코스를 만들어 "실제 도보 경로 기준"으로 표시되는지 확인(추정 대체로 뜨면 키·요청 형식을 다시 봐야 함 — 5-1절) |
| 결제대행사(PG) 가맹점 시크릿 | `PAYMENT_PG_SECRET` | 토스페이먼츠 가맹점 콘솔(개발자센터)에서 발급하는 시크릿 키 | `PAYMENT_PG_SECRET`+`TOSS_CLIENT_KEY` 둘 다 있어야 `/api/health`의 `services.payment`가 `"real"`로 바뀐다(하나만 있으면 반쪽 설정으로 보고 여전히 test로 남는다 — 사고 방지). **주의**: 5-1절 참고, 실제 스펙과 이번에 작성한 코드가 다를 수 있어 첫 결제는 반드시 소액/테스트 카드로 직접 확인 |
| 결제창(클라이언트) 키 | `TOSS_CLIENT_KEY` | 토스페이먼츠 가맹점 콘솔에서 발급하는 클라이언트 키(공개돼도 되는 값) | 이용권 화면에서 개발용 시뮬레이션이 아니라 실제 토스 결제창이 뜨는지 확인 |
| 결제 웹훅 서명 검증용 비밀값 | `PAYMENT_WEBHOOK_SECRET` | 토스페이먼츠 웹훅 설정 화면에서 발급하는 값 그대로 | **기본값 그대로 두면 운영 부팅 자체가 거부된다**(`assertBootReady`) — 반드시 실제 값으로 바꿔야 함. PG 콘솔에서 테스트 웹훅을 보내 보고 서버 로그에 401(서명/조회 불일치)이 안 뜨는지 확인 |
| 실제 이메일 발송 계정/키 | `EMAIL_API_KEY` | Resend 콘솔에서 발급, 발신 도메인(`EMAIL_FROM`에 쓸 주소) 인증까지 완료(기본값 `onboarding@resend.dev`는 Resend가 제공하는 테스트용 발신 주소라 실제 서비스에서는 자체 도메인으로 바꿔야 함) | `/api/health`의 `services.email`이 `"real"`로 바뀌는지 확인 → 실제로 로그인 코드 요청을 눌러 본인 이메일에 도착하는지 확인(도착 안 하면 발신 도메인 인증부터 재확인 — 5-1절) |
| 확정 가격 | `PRICE_AMOUNT_KRW=9900`, `PRICE_PERIOD_DAYS=30` | 서버 환경변수 | `/api/entitlement` 응답의 `price` 필드가 그대로 나오는지 확인(이미 기본값과 같아 안 넣어도 동작은 같다 — 나중에 가격을 바꿀 때만 필요) |
| 자동결제 여부 | `PRICE_AUTO_RENEW=false` | 서버 환경변수(기본값이 이미 false라 안 넣어도 됨) | 이용권 화면에 "자동결제: 꺼짐"이 표시되는지 확인 |
| 사용 한도(3-5절 제안값, 확정 후 조정) | `PLACE_LOOKUP_IMPORT_DAILY_LIMIT`, `PLACE_LOOKUP_REQUERY_DAILY_LIMIT`, `PLACE_LOOKUP_GLOBAL_DAILY_CAP`, `PLACE_LOOKUP_BATCH_MAX_ITEMS`, `PLACE_LOOKUP_BATCH_DAILY_LIMIT`, `GENERATION_RATE_LIMIT_PER_HOUR` | 서버 환경변수(전부 기본값 있음 — 안 넣어도 동작함) | 한도를 넘겨 실제로 "한도 초과" 응답(429)이 오는지 확인 |
| API 비용 SKU 단가 근거값(5차 신규 — 공식 등급·달러 단가·환율·안전여유 분리) | `COST_PLACES_TEXT_SEARCH_PRO_USD_PER_1000`(기본 32), `COST_ROUTES_COMPUTE_ESSENTIALS_USD_PER_1000`(기본 5), `COST_ROUTES_COMPUTE_PRO_USD_PER_1000`(기본 10), `COST_FX_KRW_PER_USD`(기본 1400 — 계산용 가정 환율, 실시간 아님), `COST_SAFETY_MARGIN_RATIO`(기본 0.15 — 예산 상한에만 적용, 원가 계산엔 안 씀) | 서버 환경변수 | 실제 계약·공식 확인 단가로 USD 값만 갱신하면 원화 단가(`server/config.mjs`의 `costEstimate`)가 자동으로 다시 계산됨. 이 값 또는 `COST_*_KRW_MICROS`(원화 직접 지정) 둘 중 하나만 바꾸면 된다 — 원화 값을 직접 넣으면 USD/환율 계산을 건너뛰고 그 값을 그대로 씀 |
| API 비용 한도(3-4절 제안값, 확정 후 조정) | `COST_PLACES_TEXT_SEARCH_KRW_MICROS`, `COST_ROUTES_COMPUTE_KRW_MICROS`, `COST_ROUTES_COMPUTE_HIGHVOLUME_KRW_MICROS`, `COST_PER_ACCOUNT_DAILY_KRW_MICROS`, `COST_PER_ACCOUNT_MONTHLY_KRW_MICROS`(5차 신규 — 계정별 월간 한도), `COST_GLOBAL_DAILY_KRW_MICROS`, `COST_GLOBAL_MONTHLY_KRW_MICROS`(기본 27,000원/월) | 서버 환경변수(단위는 마이크로원 — 1원 = 1,000,000) | 실제 계약 단가로 갱신한 뒤, 한도를 낮게 걸어 일부러 초과시켜 봤을 때 외부 호출 없이 503으로 막히는지 확인(`cost-budget-exceeded`) — 계정별 월간 한도는 5-2절의 헤비유저 원가 초과 문제를 다루는 실제 수단 |
| Google Routes 경유지 상한(7절, ChatGPT 확인값 — 이 세션은 공식 문서 재확인 못함) | `ROUTES_MAX_INTERMEDIATES_PER_CALL`(기본 25), `ROUTES_HIGH_VOLUME_THRESHOLD`(기본 11) | 서버 환경변수 | 실제 공식 문서로 재확인한 값과 다르면 이 값만 바꾸면 됨(코드 변경 불필요) |
| 외부 요청 타임아웃 | `EXTERNAL_REQUEST_TIMEOUT_MS`(기본 8000) | 서버 환경변수 | 응답이 느린 상황을 재현해 실제로 이 시간 안에 끊기는지 확인 |
| 결제·생성 잠금 만료(재시작·복구 안전장치) | `GENERATION_LOCK_TIMEOUT_SECONDS`(기본 120), `PAYMENT_LOCK_TIMEOUT_SECONDS`(기본 60) | 서버 환경변수 | 서버를 강제 종료했다가 다시 켰을 때 이전에 진행 중이던 계정이 영원히 막히지 않고 이 시간 뒤 정상화되는지 확인 |
| 코스 생성 입력 상한 | `MAX_PLACES_PER_GENERATION`(기본 60), `MAX_BUDGET_MINUTES`(기본 1440) | 서버 환경변수 | 실제 여행 코스 규모에 비해 너무 빠듯하면(예: 여러 날짜를 한 코스로 묶는 사용 패턴이 실제로 있다면) 값을 올려야 함 |

각 서비스는 **독립적으로** real/test가 판정된다(`docs/DESIGN_INTEGRATION_REPORT.md`
1-B-11) — 장소 조회 키만 먼저 넣어도 결제·이메일은 그대로 테스트
모드로 안전하게 남아 있다. 한 서비스씩 순서대로 실제로 켜고 확인해도
된다. 단, **운영(`APP_ENV=production`)으로 전환하는 순간에는** 결제·
이메일 필수값이 전부 갖춰지지 않으면 서버 자체가 시작되지 않는다 —
장소조회·경로 키만 없는 채로 운영을 시작하는 것은 허용된다(그
기능만 "개발 중"으로 정직하게 비활성 표시).

## 7. 최소 제휴 준비(9차 신규) — 구조만, 실제 계약·게시는 안 함

1-2절의 "추가 수익(보조)" 방향에 맞춰, 제휴처를 실제로 등록하지 않은
채로 나중에 등록만 하면 바로 켤 수 있는 최소 구조를 미리 만들어
뒀다. **지금은 승인된 실제 제휴 링크가 하나도 없다** — 그래서 이
기능은 소비자 화면 어디에도 아직 보이지 않는다(의도된 기본 상태).

- **데이터 구조**: `server/affiliates.mjs`의 `AFFILIATE_OFFERS`(빈
  배열로 배포) — 항목마다 `id·type(esim/transit/ticket/lodging)·
  city·label·url·active`를 갖는다.
- **표시 조건**: 그 도시에 `active:true`인 제휴가 있을 때만, 정해진
  진입점(출국 준비 안내·코스에 필요한 티켓/교통 안내)에 링크
  카드가 뜬다. 없으면 완전히 숨는다 — "곧 열립니다" 같은 자리
  채우기도 안 만든다.
- **안전장치**: 허용 도메인 화이트리스트(`ALLOWED_AFFILIATE_DOMAINS`)
  검증, `target="_blank" rel="noopener noreferrer"`로 새 창 오픈,
  "제휴 링크"라는 표시를 항상 붙인다. 클릭 이벤트(`affiliate_click`)
  만 기록하고, 이건 예약·매출이 아니라 "클릭이 있었다"는 사실만
  의미한다 — 예약/취소/정산 데이터는 실제로 그 제휴사에서 확인할 수
  있게 되는 시점에만 별도로 집계한다.
- **금지 사항(코드로 보장)**: 제휴 유무·수수료가 코스 생성 로직에
  전혀 영향을 주지 않는다(제휴 모듈은 코스 생성 코드를 아예 import
  하지 않음). 사용자의 저장 목록·좌표·이메일 등 개인 데이터를 제휴
  링크 URL에 실어 보내지 않는다(정적으로 등록된 URL을 그대로 열 뿐,
  쿼리 파라미터를 조합해 만들지 않음). 임의 URL로의 리다이렉트 기능
  자체가 없다(화이트리스트에 없는 도메인은 링크 생성 단계에서부터
  걸러진다).
- **아직 안 한 것**: 실제 제휴 계약 체결, 실제 제휴사 링크 등록,
  광고 게시. 전부 사용자·ChatGPT의 별도 승인이 있어야 진행되는
  일이다.

## 8. 한국에서 현지 위치인 것처럼 테스트하는 모드(9차 신규)

6-3절 지시대로, **OS의 실제 GPS를 흉내 내거나 가로채지 않는** 앱 안
"테스트 위치" 기능을 넣었다(`navigator.geolocation`을 전혀 건드리지
않음). 이 기능만으로 실제 현지 GPS·로밍·도보 조건을 검증했다고
**절대 주장할 수 없다** — 그냥 앱 로직이 참고하는 기준 좌표를 사람이
명시적으로 지정한 값으로 바꿔치기할 뿐이다.

- **허용 대상(중요한 한계)**: 지금 이 저장소에는 "승인된 테스트
  계정/스테이징"을 구분하는 서버 쪽 화이트리스트가 없다. 그래서
  최소 구현으로 URL에 `?testmode=1`을 한 번 붙이면 그 브라우저에
  계속 풀리는 방식(로컬스토리지 플래그)을 썼다 — **이건 진짜 계정
  기반 접근 제어가 아니다.** 실제로 특정 계정만 허용하려면 서버에
  계정 화이트리스트(예: `accounts.test_access` 플래그)와 이를
  확인하는 API가 새로 필요하며, 지금은 없다.
- **고를 수 있는 위치**: 하카타역·텐진 프리셋 2곳, 또는 위도·경도
  직접 입력. 숙소는 아직 좌표 자체가 없어(R9-9에서 확인 — `POST
  /api/trips`가 숙소 이름만 저장하고 좌표는 안 받음) 프리셋에서
  뺐다 — 나중에 숙소 지오코딩을 붙이면 추가할 수 있다.
- **일관되게 적용되는 곳**: 지금 실제로 구현된 위치 의존 기능은
  6-1절의 거리순 정렬뿐이다(코스 출발지·목적지 날씨는 좌표가 아니라
  선택한 도시 기준이라 이 테스트 위치와 무관하다 — "내 주변 날씨"
  같은 기능 자체가 아직 없다). 테스트 위치가 켜져 있으면 거리순
  정렬의 기준점이 평소의 "지정 위치"(보유 장소 평균 좌표) 대신 이
  좌표로 바뀌고, 화면 문구에 "테스트 위치(◯◯) · 실제 GPS 아님"이
  항상 같이 뜬다 — 실제 위치처럼 보이지 않게 하기 위해서다.
- **상단 배너**: 켜져 있는 동안 화면 맨 위에 "🧪 테스트 위치 사용
  중: ◯◯ · 실제 위치 아님(수동 지정)" 배너가 항상 보이고, "실제
  위치로 되돌리기"를 누르면 즉시 꺼진다.
- **실제/모의 API와의 분리**: 이 기능은 저장 목록·정렬 화면에만
  관여하고, 결제·이용권·장소 조회 같은 실제 과금·서버 판정 경로를
  전혀 건드리지 않는다(별도 저장 키, 별도 코드 경로 — 결제 시뮬레이션
  라우트가 production에는 아예 등록 안 되는 것과 같은 "실제와 모의를
  절대 안 섞는다"는 원칙을 그대로 따른다).
- **QA에 필요한 나머지 시나리오는 이미 있는 검증으로 커버됨(새로
  안 만듦 — 중복 방지)**: 저장 실패는
  `scripts/test-storage-migration.mjs`(저장소 가득 참 흉내), 응답
  지연·저장 중 충돌은 `scripts/test-sync-conflict-devices.mjs`,
  타임존은 `server/test/weather-timezone.test.mjs`, 결제
  실패·취소는 `scripts/test-purchase-flow.mjs`가 이미 실제로
  재현·검증한다. "GPS 거부/미확인" 자체는 앱에 실제 GPS 기능이
  아직 없어(`nearby()` 화면이 스스로 "GPS 연결 전"이라고 밝힘) 지금
  당장은 테스트할 대상이 없다 — 이 테스트 위치 기능은 그 실제 GPS
  기능이 나중에 생겼을 때 기기 GPS 없이도 개발 중 화면 동작을
  미리 확인해 볼 수 있게 준비해 둔 것이다.
- **아이폰 실기기·비공개 프리뷰**: 지금 이 저장소는 정적 파일
  (`src/design/index.html`)이라 어떤 URL로도 아직 공개 배포되지
  않았다(이번 승인 범위 밖). 아이폰에서 직접 열어 보려면 최소한
  다음 중 하나가 필요하고, 어느 쪽도 아직 설정돼 있지 않다 — 사용자가
  택할 사항이라 임의로 진행하지 않았다.
  1. 비밀번호로 보호된 스테이징 서브도메인에 이 정적 폴더를 올리고
     그 HTTPS 주소를 아이폰 사파리에서 직접 여는 방법(가장 단순).
  2. 로컬 개발 서버를 임시 터널(예: 사내 승인된 터널링 도구)로 잠깐
     공개해 그 세션 동안만 여는 방법(세션이 끝나면 주소가 사라짐).
  3. 위 6-3절 계정 화이트리스트를 실제로 만든 뒤, 그 계정으로 로그인해야만
     `?testmode=1`이 통하게 서버에서 한 번 더 확인하는 방법(가장 안전하지만
     구현이 더 필요함).
  어떤 방법을 쓰든 실제 배포 승인은 이번 라운드 범위가 아니므로, 사용자가
  방법을 고르면 그에 맞춰 다음 라운드에서 구현한다.

## 9. 소규모 베타·피드백 체계(9차 신규)

6-4절 지시를 실제로 구현했다. **실제 유료 모집은 이번에도 켜지 않았다**
(가격이 미정인 지금은 시작하지 않는다는 원칙 그대로) — 아래는 전부
"켜면 바로 쓸 수 있게 준비된 구조"다.

- **"불편함 보내기"**: 유형(가져오기/위치·동선/이용 방법/결제/기타) +
  한 줄 설명(최대 300자) + 선택 연락처. 로그인 여부와 무관하게 보낼 수
  있다(아직 로그인 못 한 상태에서 겪은 문제도 알려야 하므로). **화면
  스크린샷 첨부는 이번에 구현하지 않았다** — 개인정보(저장한 장소가
  화면에 그대로 보임)와 초기 운영비용을 고려한 의도적 최소 범위이며,
  그 사실을 화면 문구에서 숨기지 않는다. 최소 진단정보(appBuild·현재
  화면·오류 코드·기기 종류 4개 키만)를 서버가 화이트리스트로 강제해,
  실수로든 고의로든 전체 저장목록·정밀 GPS·결제정보가 같이 실리는 걸
  구조적으로 막는다(server/feedback.mjs). 오류 코드는 이번 라운드에서
  장소 위치 확인 실패 경로 하나만 부분적으로 채웠다 — 전체 실패
  경로를 다 연결하진 않은 의도된 최소 범위다.
- **코스 생성 직후 짧은 설문**: "실제로 이 코스로 여행하셨나요?" +
  선택 입력으로 "가장 큰 불편"을 묻는다. 카드 형태로 얹힐 뿐 코스
  화면의 다른 버튼(확인·새로 만들기)을 전혀 막지 않고, 같은 여행
  코스에는 한 번만 뜬다.
- **소규모 관리자 화면**(`src/design/admin.html`): ADMIN_TOKEN 하나로만
  보호된다(설정 안 하면 관리자 API 자체가 401이 아니라 501 — "아직
  준비 안 됨"으로 정직하게 막힌다, 빈 문자열끼리 비교해 아무 토큰
  없이 통과하는 사고 방지). 받음/처리 중/해결됨 상태 전이, 유형별
  개수 집계, 결제·"데이터 손실/사라짐" 키워드가 들어간 접수를
  화면에서 먼저 보여주는 우선순위 정렬(완벽한 분류가 아니라 운영자가
  먼저 보게 돕는 보조 장치일 뿐임을 화면에도 안내), 초대 코드
  발급·비활성화, 신규 모집 일시중지/재개 버튼을 담았다. 하루 한 번
  훑어보고 처리할 목록을 고르는 걸 전제로 만든 화면이다.
- **초대 코드**: 만료(기본 14일)·코드당 인원(기본 8명)·전체 모집
  상한(기본 30명)을 갖는다. **기본값은 꺼짐**
  (`REQUIRE_INVITE_CODE_FOR_SIGNUP=false`) — 켜지 않는 한 기존 열린
  가입 흐름이 그대로 유지된다(기존 회귀 테스트 전부가 이 기본값으로
  계속 통과함을 확인했다). 한 계정(=한 이메일)이 여러 번 "신규
  등록"으로 코드를 반복 소모할 수 없다 — 코드 소모는 그 이메일로
  계정이 "처음" 만들어지는 그 순간에만 일어나고, 이미 있는 계정의
  재로그인은 초대 코드와 완전히 무관하다(server/auth.mjs의
  loginOrCreateAccount). "5명→10명"은 예시였을 뿐 확정 값이 아니다 —
  아래에 실제 채택한 기본값과 근거를 남긴다.
  - 코드당 인원 8명: 예시보다 살짝 여유(초대 실수·한두 명 이탈 감안),
    여전히 "소규모".
  - 전체 모집 상한 30명: 초기 운영 여력(문의 응대 1인, 하루 1회
    검토)을 기준으로 "코드 하나(8명)의 3~4배" 정도로 잡은 제안값.
    확정 아님 — 응대가 밀리면 낮추고, 여유가 있으면 관리자 화면에서
    코드를 더 만들면 된다.
- **신규 모집 일시중지**: 결제·데이터손실급 심각 이슈가 생기면
  관리자 화면 버튼 하나로 즉시 멈출 수 있다(코드 재배포 불필요). 단,
  초대 코드 게이트 자체가 꺼져 있으면(기본 상태) 이 일시중지는 아무
  효과가 없다 — "모집"이라는 개념 자체가 게이트가 켜졌을 때만 의미가
  있기 때문에, 이 라운드에서는 일부러 범위를 그렇게 좁혔다(게이트가
  꺼진 상태에서까지 모든 개발용 회원가입을 막는 건 과잉 범위라고
  판단).
- **가입→가져오기→코스완성→실사용→피드백 퍼널**: 새 이벤트
  3종(`signup_completed`/`place_visited`/`feedback_submitted`/
  `survey_submitted`)만 추가했다 — 가져오기·코스완성 단계는 이미 있던
  이벤트(`import_result`/`course_generated`)로 충분해 중복으로 새로
  안 만들었다. 전부 기존 이벤트와 같은 화이트리스트 방식(허용된
  이벤트 이름·속성 키·값만 서버·클라이언트 양쪽에서 강제)이라 장소명
  등 개인 데이터가 섞일 방법이 없다.
- **아직 안 한 것**: 실제 유료 모집 시작, 화면 스크린샷 첨부, 결제형
  고객지원 SaaS·유료 AI 요약(6-4절 지시대로 시작부터 안 넣음).

### 9-1. 초대 모집 SNS 초안(게시 안 함 — 준비만)

실제 게시는 사용자·ChatGPT의 별도 승인이 있어야 한다. 가격·구체 기능을
확정된 것처럼 말하지 않고, "지금/곧 여행 가는 사람의 진짜 불편을
검증하고 싶다"는 목적을 앞세운 초안이다.

**Threads (짧은 글):**
> 여행 갈 때마다 구글맵에 저장만 해두고 정작 그 자리에서 뭘 어떻게
> 돌아야 할지 몰라서 헤맸던 적 있으신가요? 저장해둔 장소로 하루 동선을
> 짜주는 걸 만들고 있어요. 지금 여행 준비 중이거나 곧 떠나는 분들
> 소수만 먼저 초대해서 진짜 불편한 지점을 같이 찾고 싶습니다. 관심
> 있으면 댓글이나 DM 주세요 — 가격이나 정식 기능은 아직 정해지지
> 않았고, 이번엔 순수하게 사용해보고 솔직한 후기를 나눠주실 분을
> 찾는 거예요.

**Instagram (Threads 재활용, 캐러셀/스토리용 짧은 버전):**
> 구글맵에 저장만 해두고 못 써먹은 장소들 있으신가요? 🗺️
> 저장 목록 → 하루 동선으로 바꿔주는 걸 만드는 중이에요.
> 지금/곧 여행 가는 분 소수 초대해서 같이 다듬어보려고 합니다.
> 가격·기능은 아직 확정 전 — 진짜 불편함만 먼저 듣고 싶어요.
> 관심 있으면 댓글 남겨주세요.

두 초안 모두 "확정된 가격/기능"을 단언하지 않고, 검증이 목적임을
분명히 한다 — 실제로 발송하기 전에 반드시 다시 확인이 필요하다.

## 3. 사전 출시 마케팅 계획 (travel hub)

작성: 2026-09-10(1차) · 갱신: 2026-09-10(2차 — 확정 상품 반영) ·
갱신: 2026-09-10(3차 — 결제/이메일/경로 공급자 확정 반영, "구독"·
"서버가 있어 복제 문제가 사라졌다" 식 표현 정정) ·
갱신: 2026-09-10(5차 — **재방문 여행자 지원**을 반영한 스레드 초안 +
인스타 재사용 소재 추가. 단, 이 기능은 서버만 완료되고 화면은 아직
미연결이라(`docs/RELEASE_STATUS.md` 7절) **지금 게시할 수 있는 콘텐츠가
아니라 화면이 연결된 뒤 쓸 초안**임을 5절·아래에 명시) ·
담당: Claude(초안 작성) → ChatGPT(최종 문구 검수) → 사용자(집행)

**2차 갱신 요약**: 가격·상품 구조가 확정됐다 — **9,900원·30일 이용권,
결제 승인 시 시작, 자동결제 없음**. 무료는 샘플·가져오기·보관·검색·
수동 정리·개인화 코스 생성 성공 1회까지고, 추가 코스 생성·조건 변경
후 재계산·여러 날짜 일정 정리는 유료다. 이 갱신판은 그 확정 내용에
맞춰 1차 초안의 "가격은 아직 말하지 않는다" 원칙을 걷어내고, 실제
Threads 게시물 3개·Instagram 소재 1개 전문을 채워 넣었다.

**3차 갱신 요약**: 결제(토스페이먼츠)·이메일(Resend)·경로(Google
Places+Routes) 공급자가 정해지고 실제로 그 API를 호출하는 코드까지
작성됐다(단, 실제 키로 검증하지는 못했다 — 5절 표 참고). 이 갱신에서
두 가지 부정확한 표현을 고쳤다: ① 이 상품은 "구독"이 아니라
자동갱신 없는 **30일 기간제 이용권**이므로 "구독 가설"이라는 말을
더 이상 쓰지 않는다. ② "서버가 있으니 복제 문제가 사라졌다"는
서술은 그 서버 쪽 통제(무료체험 실패시 미차감, 동시 생성 잠금,
레이트리밋 등)가 **실제로 코드로 갖춰져 검증됐을 때만** 성립하는
말이라, 이번에 그 구현·검증이 끝난 상태를 `docs/RELEASE_STATUS.md`
기준으로 확인한 뒤 0절에 조건을 명시했다.

**4차 갱신 요약**: ChatGPT가 실제로 재현한 문제들(운영에서 가짜 실제
경로가 나오던 버그, 결제 취소 권한 미대조 등)을 코드로 고쳤고, API
비용 통제(계정별·전체 일일/월간 예산, Google Routes 경유지 상한)를
실제로 구현했다. 콘텐츠·카피 자체는 이미 "검증 안 된 속도·시간절약·
최단경로·자동동기화 주장 금지" 원칙을 지키고 있어 이번 검토에서 새로
고칠 표현은 없었다(전부 재확인만 함 — 6절 참고). 4-5절에 체험단
정량 측정 지표(서버 이벤트)를 추가했다.

---

## 0. 이 문서와 기존 문서의 관계 — 먼저 밝힐 것

`docs/` 안에 이미 `가격_판매전략.md`·`판매_SNS전략.md`·`판매채널.md`·
`홍보전략.md`·`복제방지_법적보호.md`가 있다. 열어 보니 전부 **다른
제품**을 전제로 쓴 문서다 — 브랜드 "별표털기", 서버 없는 단일 HTML
파일, 9,900원 **한 번** 결제, "복제를 막을 방법이 없으니 싸게 많이
판다"는 전략, 후쿠오카 항공권 200만원(9,900원×약 200개)이라는 구체
목표. 이번에 만들고 있는 제품 "travel hub"는 실제 서버·로그인이 있고,
상품 자체는 **자동갱신 없는 30일 기간제 이용권**이지 구독(정기결제)이
아니다 — 이 문서에서도 "구독"이라는 말을 쓰지 않는다(6절 참고).
파일을 복사해도 남의 이용권까지 복사되지 않는 것은 맞지만(이용권
여부를 서버가 매 요청마다 확인하는 구조), 그 서버 쪽 통제가 **이번
세션에서 실제로 코드로 다 갖춰졌는지**가 이 문서가 기댈 수 있는
전제다 — 무료체험 소비를 서버가 실패 결과에는 차감하지 않고 실제
경로 성공에만 정확히 한 번 차감하는 로직, 계정당 동시 생성 잠금,
로그인·장소조회 레이트리밋까지 실제로 구현·테스트된 상태를
`docs/RELEASE_STATUS.md`에서 확인한 뒤에만 "서버가 지킨다"는 표현을
쓴다. 그래서 옛 문서의 "싸게, 많이" 전략을 그대로 가져오면 안 맞을 수
있다.

**이 문서는 옛 문서를 대체하지 않는다.** 옛 문서를 지우거나 고치지
않았다. 가격(9,900원/30일, 자동결제 없음)은 이번에 ChatGPT가 확정해
전달한 값을 그대로 반영했고, 이 문서에서 새로 정한 게 아니다(옛
문서의 "싸게 많이" 한 번 결제 전략과는 다른 전제라는 점만 다시
밝혀 둔다 — `docs/BUSINESS_DECISIONS.md` 4-1 참고).

---

## 1. 고객 가설 (아직 검증 안 됨 — 가설로 표시)

**가설 대상**: 해외여행(특히 일본처럼 짧게 다녀오는 여행) 전에 SNS·
블로그에서 본 장소를 구글맵 "저장됨" 목록에 습관적으로 쌓아두지만,
실제 출발 전에 그걸 도시별·하루 코스별로 정리할 시간이 없어서 결국
현지에서 검색하며 다니는 사람.

**근거로 삼을 수 있는 것 / 아직 없는 것**을 구분해서 적는다:
- 근거로 삼을 수 있는 것: 이 앱 자체가 "만든 사람의 실제 사용 습관"에서
  출발했다는 점(제작자 본인이 이 문제를 겪음). 구글맵 저장 목록 내보내기
  (Google Takeout) 기능이 실제로 존재하고 이 앱이 그 파일을 실제로
  읽어낸다는 점은 이번 세션에서 직접 검증했다.
- **아직 없는 것(가설일 뿐)**: 이 문제를 겪는 사람이 실제로 몇 명이나
  있는지, 그중 9,900원/30일에 돈을 낼 의향이 있는 비율이 얼마인지 —
  가격은 확정됐지만 "이 가격에 실제로 사는 사람이 있는지"는 여전히
  검증 전이다. 아래 3절 체험단 운영의 목적이 바로 이 가설을 실제
  반응으로 확인하는 것이다.

**하지 않을 것**: 가상의 사용자 수·후기·시간 절약 수치를 만들어
메시지에 넣지 않는다. 검증 전까지 모든 카피는 "우리가 만든 것"에 대한
설명이지 "이미 검증된 효과"에 대한 주장이 아니다.

---

## 2. 핵심 메시지 — 세 기둥

1. **"이미 모아둔 걸 그대로 가져온다"** — 장소를 새로 입력하지 않는다.
   구글맵에 저장해 둔 것을 그대로 가져온다(Google Takeout ZIP 또는
   CSV/JSON). *과장 금지*: "실시간 자동 동기화"라고 말하지 않는다 —
   실제로는 사용자가 내보내기 파일을 받아서 넣는 방식이다.
2. **"도시별로 저절로 정리된다"** — 여러 나라·도시에 흩어져 쌓인 저장
   목록이 도시별로 나뉘어 보인다. 유형(카페·맛집 등)도 자동으로
   짐작하되, 사람이 직접 고치면 그 선택을 기억한다.
3. **"고른 곳들로 걸어서 도는 하루 코스가 만들어진다"** — 시간 예산과
   출발 시각을 반영해 순서를 짠다. *과장 금지*: 실제 도보 경로 계산이
   모든 인터넷 환경에서 되는 건 아니다(막히면 방문 순서만으로 정직하게
   대체한다는 걸 화면에서도 밝힌다 — `landing.html`과 동일한 원칙).

**가격 언급 원칙(갱신)**: 상품이 확정됐으므로("9,900원·30일 이용권,
결제 승인 시 시작, 자동결제 없음") 이제는 콘텐츠에서 가격을 말해도
된다 — 단, 항상 **"첫 코스는 무료"**를 먼저 말하고 그다음에 유료
범위를 붙인다("추가 코스·여러 날짜 정리부터 9,900원/30일"). 자동결제가
없다는 것도 매번 같이 말한다(오해로 인한 불만을 미리 막는다). "무제한
사용"이라는 표현은 쓰지 않는다 — 사용 한도는 아직 확정되지 않았다
(`docs/BUSINESS_DECISIONS.md` "이용권 원가·사용 한도" 참고).

**제작자 실제 경험 언급 원칙**: 제작자 본인의 "후쿠오카에만 160곳
이상 저장해 놓았다"는 경험은 실제 사실이라 그대로 써도 된다(가짜
후기·가짜 사용자 수 금지 원칙과 다른 층위 — 이건 검증 안 된 타인의
후기가 아니라 제작자 본인의 실제 데이터다). **여러 도시 합계로
부풀리지 않는다** — "후쿠오카에만" 이라는 단서를 항상 같이 붙인다.

---

## 3. 2주 사전 콘텐츠 캘린더 (스레드 중심 · 인스타 보조)

원칙:
- 스레드는 글 위주로 매번 올리고, 그중 화면이 필요한 3~4개만 골라
  인스타에도 같은 소재로 올린다(스레드=본진, 인스타=보조 — 새 문서
  요구사항 그대로).
- 화면·자산은 **실제로 존재하는 것만** 쓴다. 이번 세션에서 실제로
  캡처한 두 장(`src/design/assets/marketing/1-collection.png`,
  `2-course.png`)만 지금 쓸 수 있다 — `2-course.png`는 실제 걷기 경로
  계산 성공 장면을 예시 데이터로 만든 것이라는 걸 게시물에도 캡션으로
  밝힌다(랜딩 페이지와 같은 원칙 — 안 밝히면 실제로 확인 안 된 걸
  확인된 것처럼 보이는 과장이 된다).
- 로그인·결제·이용권 화면은 **아직 실제로 열리지 않은 기능**이라
  이번 2주 콘텐츠에서는 다루지 않는다(사전 신청 CTA만 반복).
- 이 표는 시안이다 — 실제 게시 전 문구·톤은 ChatGPT가 다듬고,
  실제 게시 자체는 사용자 승인 없이 하지 않는다(지시사항: SNS에
  실제로 올리거나 광고비를 쓰는 건 별도 승인 필요).

| 일차 | 플랫폼 | 내용 | 필요한 화면/자산 | CTA |
|---|---|---|---|---|
| D1 | 스레드 | 문제 제기(제품 언급 없음) — 전문 3-1 | 없음(글만) | 없음(공감 유도) |
| D3 | 스레드 | 제품 공개, 메시지 ① — 전문 3-2 | `1-collection.png` | 사전 신청 랜딩 예고 |
| D4 | 인스타 | D3 재구성 — 전문 3-4 | `1-collection.png` | 프로필 링크(랜딩) |
| D6 | 스레드 | 메시지 ② 자동 정리, 짐작/직접수정 정직하게 언급 | `1-collection.png`(크롭) | 사전 신청 랜딩 링크 |
| D8 | 스레드 | 메시지 ③ 코스 생성 + 가격 최초 공개 — 전문 3-3 | `2-course.png`(예시 데이터 캡션 필수) | 사전 신청 랜딩 링크 |
| D9 | 인스타 | D8 재구성, 같은 단서 캡션 | `2-course.png` | 프로필 링크 |
| D11 | 스레드 | 제작자 서사 — "후쿠오카에만 160곳 이상" 언급 | 없음(글만) | 사전 신청 랜딩 링크 |
| D13 | 스레드 | 체험단 모집 공지(4-1 모집 문구) | `1-collection.png` | 랜딩 링크(체험단 지원 겸용) |
| D14 | 인스타 | D13 재구성 | `1-collection.png` | 프로필 링크 |

D2·D5·D7·D10·D12는 의도적으로 비웠다 — 매일 올리면 "올릴 내용"이
바닥나 억지 콘텐츠가 나온다는 게 기존 `판매_SNS전략.md`의 지적이었고,
지금도 유효한 판단이라 그대로 따랐다.

### 3-1. D1 — 스레드 전문(문제 제기)

> 구글맵 켜서 저장 목록 보다가 소름 돋았어요. 후쿠오카 하나에만 저장한
> 곳이 160개가 넘더라고요. 근데 실제로 가본 곳은 그중 반도 안 돼요.
> 저장은 부지런히 하는데 여행 갈 때 그걸 다시 정리할 시간이 없어서,
> 결국 현지에서 아무거나 검색해서 다니고 옵니다. 이거 저만 이런가요?

### 3-2. D3 — 스레드 전문(제품 공개, 메시지 ①)

> 그래서 이거 만들고 있어요. 구글맵에 저장해둔 곳들, 새로 입력할 필요
> 없이 그대로 가져와서 한곳에 모아 보여주는 화면이에요(사진). 도시별로
> 자동으로 나뉘어서 정리돼요 — 여러 나라에 흩어져 저장해 둔 곳들도
> 섞이지 않고요. 다음 여행 준비할 때 저장 목록부터 다시 뒤지는 일,
> 이제 안 해도 되게 만드는 중입니다. 어떤 화면인지 더 궁금하시면
> 댓글 남겨주세요 — 먼저 써보실 수 있는 링크 준비 중이에요.

### 3-3. D8 — 스레드 전문(코스 생성 + 가격 최초 공개)

> 오늘은 코스 만들기 화면 자랑할게요. 담아둔 장소 중 고른 곳들로
> 걸어서 도는 순서를 자동으로 짜줘요(사진 — 실제 걷기 경로 계산 성공
> 장면을 예시 데이터로 보여준 겁니다. 인터넷 상황에 따라 실제 경로
> 계산이 안 될 때도 있는데, 그럴 땐 방문 순서만으로 정직하게 코스를
> 보여줘요). 첫 코스는 무료로 바로 만들어볼 수 있게 할 거고, 그 다음
> 코스부터·여러 날짜 일정을 정리하려면 9,900원에 30일 이용권이 필요할
> 예정이에요(자동결제는 없어요 — 30일 지나면 그냥 끝, 다시 결제 안
> 하면 추가로 안 빠져나갑니다).

### 3-4. D4 — 인스타그램 소재 전문

이미지: `src/design/assets/marketing/1-collection.png`

> 구글맵에 저장만 해두고 못 가본 곳들, 있으신가요?
> 저장해둔 장소를 그대로 가져와서 도시별로 모아 보여주는 화면을
> 만들고 있어요. 새로 입력할 필요 없이, 있는 그대로 가져오기만
> 하면 돼요.
> 먼저 써보고 싶으신 분은 프로필 링크로 와주세요 🧭
>
> #여행준비 #구글맵 #여행앱 #travelhub

### 3-5. 재방문 여행자 콘텐츠(6차 갱신 — 화면 연결 완료, 게시는 여전히 보류)

**게시 시점 조건(6차에서 바뀐 점)**: 5차까지는 재방문 여행자 지원의
서버 API만 있고 화면이 안 열려 있어 이 초안 자체를 아예 게시할 수
없었다. **6차에서 `src/design/spots.js`에 화면 연결이 실제로
끝났으므로(`docs/RELEASE_STATUS.md` 7절), 아래 초안은 이제 "화면이
없어서 게시 불가"는 아니다.** 그렇다고 지금 바로 게시하는 것도 아니다
— 이번 지시("마케팅은 갱신하되 게시하지 말 것")에 따라 **초안만
실제 화면 기준으로 갱신해 두고, 실제 게시는 여전히 하지 않는다.**
게시 여부·시점은 3절 캘린더와 함께 사업 쪽이 별도로 결정할 사항이다.

**핵심 메시지**: "저장해놓고 못 간 곳을 다음 여행에서 마저 가보기" —
같은 도시·나라를 다시 가는 여행자를 위한 이야기다. **주의해야 할
과장**: "저장한 곳을 전부/모두 재방문한다"고 말하지 않는다(그런
보장은 없다 — 이 기능은 "못 가본 곳을 다음 여행 후보로 쉽게 다시
꺼내 볼 수 있게" 도와줄 뿐, 실제로 다 가게 만들어주는 게 아니다).
속도·최단경로·실시간 자동 동기화 관련 미검증 주장도 이 초안에는
없다(6절 금지 목록과 동일하게 적용). **새로 추가**: 이용권에 실제
사용 횟수 한도가 생겼다는 사실(무료체험 위치확인 10곳+코스 1회,
유료 위치확인 50곳+코스 30회, 3-3-1절 `BUSINESS_DECISIONS.md`
참고)은 이 콘텐츠에서 굳이 먼저 꺼내지 않는다 — 개발 용어(엔타이틀먼트
등)를 노출하지 않는다는 원칙과 같은 이유로, 소비자에게는 화면에서
"남은 횟수"로만 자연스럽게 보이면 충분하다. 다만 **"무제한"이라고
암시하는 표현은 절대 쓰지 않는다**(실제로 한도가 있으므로).

**스레드 초안**(6차: 실제 화면 기준으로 문구를 다듬었다 — "만들고
있어요"가 아니라 "만들었어요"로, 실제로 눌러본 흐름 그대로):

> 후쿠오카에만 저장한 장소가 160곳이 넘는데, 첫 여행에서 다 못
> 가봤어요. 그래서 여행을 분리해서 저장할 수 있게 만들었어요 —
> 같은 도시를 또 가더라도 이번 여행, 저번 여행 기록이 따로 남고,
> 장소마다 "방문 완료"·"다시 가고 싶어요"를 눌러 표시해 둘 수
> 있어요. 다음에 후쿠오카를 또 갈 때는 저번에 못 가본 곳들을 먼저
> 후보로 보여주는 "이어가기" 버튼이 있어요. 재방문 여행자를 위한
> 기능이라 다른 도시로 여러 번 다니는 분들께 특히 도움이 될 것
> 같아요.

**인스타그램 재사용 소재**(6차: `docs/screenshots/after_여행선택.png`,
`after_방문표시_장소상세.png`, `after_이월후보.png`가 실제로
찍혔다 — 전부 지어낸 이름만 쓴 합성 데이터라 그대로 공개용으로
쓰려면 브랜드 톤에 맞는 실제 캡처를 다시 떠야 한다. 지금 있는
`1-collection.png`/`2-course.png`는 이 기능 화면이 아니므로 계속
별도로 취급):

> 저장해놓고 못 간 곳, 다음 여행에서 마저 가보세요.
> 같은 도시를 다시 가도 여행마다 기록이 따로 남고, 못 가본 곳은
> "이어가기"로 다음 여행 후보에 다시 꺼내 볼 수 있어요.
> 재방문 여행자를 위해 만들었습니다 🧭
>
> #여행준비 #재방문여행 #구글맵 #여행앱 #travelhub

---

## 4. 초기 체험단 운영 계획 (5~10명)

**전제**: 결제·로그인은 아직 실제 서비스로 열려 있지 않다(테스트
모드로만 검증됨 — `docs/DESIGN_INTEGRATION_REPORT.md` 1-B-9 참고).
그래서 이번 체험단은 **무료로 전체 기능(가져오기·코스 생성)을 써보게
하고, 결제 의향은 실제 결제 없이 설문으로만 물어본다.** 실제 돈을
받거나 실제 결제 화면을 체험단에게 태우지 않는다.

### 4-1. 모집 문구 (초안 — 실제 게시는 승인 후)

> 여행 갈 때 구글맵에 저장만 해두고 못 가본 곳들 있으신가요?
> 그 저장 목록을 가져와서 하루 코스로 짜주는 걸 만들고 있어요.
> 정식으로는 9,900원/30일 이용권으로 열릴 예정인데, 지금 체험단으로
> 참여하시면 완전히 무료로 먼저 써보실 수 있어요. 5~10분이면
> 되는데, 아직 다듬는 중이라 여러분의 솔직한 반응이 필요합니다.
> 3~4개 질문에만 답해 주시면 됩니다. 관심 있으시면 댓글이나 DM
> 주세요.

### 4-2. 테스트 진행 순서

1. 참가자에게 사전 신청 랜딩 링크로 이메일 받기(이미 구현된
   `/api/waitlist` 흐름 재사용 — 새 수집 창구를 따로 안 만든다).
2. 참가자 본인의 구글맵 저장 목록을 Google Takeout으로 내보내게
   안내(방법 링크 필요 — 이 문서에는 안 넣는다, 실제 발송 문구에서
   구글 공식 안내 링크를 그대로 인용).
3. 내보낸 파일을 앱에 직접 넣어보게 함(가져오기 → 컬렉션 확인).
4. 원하는 장소 몇 곳을 골라 코스 생성까지 직접 눌러보게 함.
5. 4-3의 질문지를 보내고, 답변을 4-4 양식에 기록.

### 4-3. 질문지 (그대로 사용 가능한 문항)

1. 구글맵에 저장한 장소를 실제 여행에서 코스로 정리해 본 적이
   있나요? 있다면 어떻게 하셨나요? (기존 습관 파악)
2. 가져오기 과정에서 막히거나 헷갈렸던 부분이 있었나요?
3. 자동으로 나온 코스가 실제로 그 순서로 다닐 만했나요? 아니라면
   왜 그런가요?
4. 추가 코스·여러 날짜 일정 정리가 9,900원/30일(자동결제 없음)이면
   실제로 결제하실 것 같나요? 그렇다면/아니라면 왜 그런가요? (가격은
   이미 확정됐지만, "그 가격에 실제로 살 사람이 있는지"는 이 답변으로
   가늠하는 것이지 이 답을 "검증된 수요"라고 포장하지 않는다 — 5~10명
   응답은 표본이 너무 작다)
5. 없어서 아쉬웠던 기능이 있나요?

### 4-4. 불편 사항 기록 양식 (문서/스프레드시트에 그대로 옮겨 쓸 표)

| 참가자(익명 코드) | 날짜 | 막힌 단계(가져오기/정리/코스생성/기타) | 구체적 증상 | 심각도(사용 포기 수준 / 불편하지만 계속함 / 사소함) | 조치 여부 |
|---|---|---|---|---|---|
| P1 | | | | | |
| P2 | | | | | |

**주의**: 참가자 이름·이메일 등 개인정보는 이 로그 표에 직접 적지
않는다 — 익명 코드(P1, P2…)로만 관리하고, 실제 연락처는 사전 신청
DB(`waitlist` 테이블) 쪽에만 둔다(불필요하게 개인정보 사본을 여러 곳에
만들지 않는다).

### 4-5. 정량 측정(서버 이벤트로 이미 자동 수집됨 — 4차 갱신 신규)

4-3의 질문지(정성)와 별개로, 서버가 이미 기록하는 이벤트로 "몇 명이
첫 사용→코스 완성→결제까지 갔는지"를 실제로 셀 수 있다(장소명·좌표
같은 개인 데이터는 이 이벤트들에 전혀 안 담긴다 — `server/routes/events.mjs`
의 화이트리스트가 막는다):

| 측정 지점 | 이벤트 이름 | 무엇을 세는지 |
|---|---|---|
| 유입 | `channel_inflow` | 랜딩 페이지 도달(채널 구분) |
| 사전 신청 | `waitlist_signup` | 이메일 접수 완료 |
| 첫 사용(가져오기 이후 코스 완성) | `course_generated` | 실제로 코스가 만들어진 횟수(성공 여부·개수만, 장소명 없음) |
| 결제 유도 화면 노출 | `paywall_viewed` | 이용권 화면이 뜬 횟수(트리거 종류 구분 — 새 코스/새 날짜) |
| 결제 시도·결과 | `payment_started`, `payment_result` | 결제 시작·성공/실패 횟수 |

체험단 운영 중에는 이 표의 이벤트 수를 그대로 세어 "가져오기까지 온
사람 중 몇 명이 코스 생성까지 갔는지", "이용권 화면을 본 사람 중 몇
명이 결제 의향(4-3 문항 4)에 긍정 답을 했는지"를 대조할 수 있다 —
단, 5~10명 규모에서는 이 숫자도 4-3과 마찬가지로 "검증된 전환율"이
아니라 참고 지표일 뿐이다(표본이 너무 작다는 원칙은 정량 지표에도
그대로 적용된다).

---

## 5. 기능 상태 고지 — 모든 콘텐츠·페이지 공통 원칙

콘텐츠·랜딩 페이지 어디든 기능을 언급할 때는 아래 기준을 따른다.

| 기능 | 상태 | 콘텐츠에서 부르는 이름 |
|---|---|---|
| 구글맵 내보내기 가져오기 | 동작 확인됨 | "지금 됩니다" |
| 도시·유형 자동 정리 | 동작 확인됨 | "지금 됩니다" |
| 걸어서 도는 코스 생성(1회 무료) | 동작 확인됨(단, 실제 도보 경로 계산은 환경에 따라 대체됨) | "지금 됩니다" + 단서 문구 필수 |
| 여러 날짜 일정 정리(유료) | 화면·게이트까지 동작 확인됨 | "지금 됩니다" — 단, 유료라는 것도 같이 말한다 |
| 이메일 로그인(Resend), 결제(토스페이먼츠, 9,900원/30일) | 공급자가 확정되고 실제로 그 API를 호출하는 코드까지 작성·계약서 형태 테스트(키 없이 통과하는 요청 형태 검증)까지 마쳤다 — 하지만 **실제 발급받은 키로 진짜 이메일이 도착하는지, 진짜 결제창이 뜨는지는 이 세션에서 검증하지 못했다**(`docs/RELEASE_STATUS.md` 3절 — ④ 실제 키 연결 검증 필요) | "개발 중" — 날짜·시기 약속 금지, "이미 살 수 있다"는 인상을 주는 CTA 금지 |
| 재방문 여행자 지원(여행 분리·방문 기록·다음 여행 후보 제안, 5차 신규) | 서버 API·DB·마이그레이션·동기화까지 전부 완료·재현 테스트 통과했지만, **`src/design/spots.js` 화면이 아직 그 API를 안 부른다**(`docs/RELEASE_STATUS.md` 7절 — ⑤ 화면 미연결) | **"개발 중"조차 아직 이르다 — 3-5절의 초안은 화면이 실제로 열릴 때까지 게시 금지.** "저장한 곳을 전부/모두 재방문한다"는 과장 금지 |

---

## 6. 절대 하지 않을 것 (재확인)

- 가짜 후기, 가짜 사용자 수, 검증 안 된 시간 절약 수치를 만들어
  콘텐츠에 넣지 않는다(제작자 본인의 "후쿠오카 160곳+" 경험은 예외 —
  실제 사실이고, 항상 "후쿠오카에만"이라는 단서를 붙인다).
- 승인 없이 실제로 SNS에 게시하거나 광고비를 쓰지 않는다 — 이 문서는
  계획이지 집행 승인이 아니다.
- 가격은 확정됐으니 말해도 되지만, "이미 결제해서 쓸 수 있다"는
  인상은 주지 않는다 — 실제 키로 이메일 로그인·결제가 검증된 적이
  아직 없다(`docs/RELEASE_STATUS.md` 3절, ④ 항목). "곧 열립니다",
  "사전 신청하면 가장 먼저 알려드립니다"까지만 말한다.
- "무제한 사용"이라는 표현을 쓰지 않는다 — 사용 한도는 아직 확정
  안 됐다(`docs/BUSINESS_DECISIONS.md` 3-3절 제안 참고).
- "구독"이라는 표현을 쓰지 않는다 — 확정 상품은 자동갱신 없는 30일
  기간제 이용권이다.
- 체험단에게 실제 결제를 시키지 않는다(실제 키로 검증된 결제가
  아직 없다).
- **재방문 여행자 콘텐츠(3-5절)를 화면 연결 전에 게시하지 않는다**
  — 서버만 완료된 기능을 화면이 있는 것처럼 보여주지 않는다.
- 재방문 여행자 콘텐츠에서 "저장한 곳을 전부/모두 재방문한다"고
  말하지 않는다 — 이 기능은 "못 가본 곳을 다시 후보로 보여주는" 것
  이지 재방문을 보장하지 않는다. 제작자 경험 문구는 항상 "후쿠오카에
  **만** 저장한 장소 160곳 이상"으로 쓴다(여러 도시 합계로 부풀리지
  않는다 — 2절 원칙과 동일).

## 4. 운영 서버 설정 순서 (OPERATIONS_SETUP)

작성: 2026-09-10(6차 재검토) · 갱신: 2026-09-11(10차 — 테스트 위치
허용 절차 변경, AI 보조 분류 항목 추가) · 브랜치 `design-integration`

이 문서는 **실제 서비스를 켜기 전에** 사람이 직접 해야 하는 일을
순서대로 정리한다. 개발 담당(이 세션)은 outbound 네트워크가 막혀 있어
(`docs/RELEASE_STATUS.md` 2절) 아래 순서를 실제로 밟아본 적이
없다 — 이 문서는 "코드가 무엇을 기대하는지"를 정확히 옮긴 것이지,
실제로 이 순서대로 켰을 때 문제가 없다고 보장하는 게 아니다.

**핵심 원칙 하나**: `/api/health`가 "키가 설정돼 있다"고 보여주는 것과
"그 키로 실제 연결에 성공했다"는 것은 다른 이야기다. 이 문서의 순서를
전부 따라도, **각 서비스로 실제 요청을 한 번 이상 보내서 성공 응답을
받아보기 전까지는 "연결됐다"고 말할 수 없다.** 아래 3단계에 그
확인 방법을 서비스별로 따로 적어 뒀다.

---

## 0. 시작 전에 준비할 것

아래 네 공급자의 계정을 먼저 만들어 둔다(계정 생성 자체는 무료):

1. **토스페이먼츠**(결제) — https://www.tosspayments.com 가맹점 가입
2. **Resend**(이메일 발송) — https://resend.com 가입
3. **Google Cloud / Google Maps Platform**(장소 조회 + 경로 계산) —
   https://console.cloud.google.com 에서 프로젝트 생성 후
   Places API(New)·Routes API 활성화, **결제 수단 등록 필요**
   (Google Maps Platform은 무료 크레딧이 있어도 결제 수단 등록을
   요구한다)

이 중 하나라도 계정 생성 단계에서 막히면, 그 서비스는 나중에 "④
사용자 설정 필요" 상태로 남겨 두고(2절 참고) 나머지부터 먼저 켤 수
있다 — 서비스별로 독립적으로 켜지는 구조다(`server/config.mjs`).

---

## 1. 환경변수 입력 순서

**서버(`node server/index.mjs`)는 시작될 때 한 번만 환경변수를 읽는다
— 값을 바꾸면 서버를 재시작해야 반영된다.** 아래 순서로 하나씩
채운다(위에서부터 순서대로 해도 되고, 이미 갖고 있는 키부터 먼저
넣어도 된다 — 순서 자체가 서로를 막지는 않는다. 단, **6번(운영
모드 전환)은 항상 맨 마지막에 한다** — 그 전까지는 테스트 기능이
실수로 운영에 노출되는 걸 막아 주는 안전판 역할을 한다).

### 1-1. 필수 — 서버가 아예 못 뜨는 값

| 환경변수 | 값 | 확인 방법 |
|---|---|---|
| `DB_PATH` | 실제 서버가 쓸 SQLite 파일 경로(예: `/data/travelhub.db`) | 이 경로가 있는 디스크에 **지속 저장소**가 붙어 있어야 한다(컨테이너 재시작 시 파일이 사라지면 전체 계정 데이터가 날아간다) |
| `PORT` | 서버가 들을 포트(예: `3000`) | 리버스 프록시(nginx 등)가 이 포트로 연결하는지 확인 |

### 1-2. 이메일 로그인(Resend)

| 환경변수 | 값 |
|---|---|
| `EMAIL_ADAPTER` | `resend` |
| `EMAIL_API_KEY` | Resend 대시보드 → API Keys에서 발급한 키(`re_`로 시작) |
| `EMAIL_FROM` | 실제 보낼 발신 주소(Resend에 도메인 인증을 마친 주소여야 함 — 인증 안 된 도메인은 발송 자체가 거부될 수 있다) |

### 1-3. 결제(토스페이먼츠)

| 환경변수 | 값 |
|---|---|
| `PAYMENT_ADAPTER` | `toss` |
| `TOSS_CLIENT_KEY` | 토스페이먼츠 개발자센터의 **클라이언트 키**(결제창에 쓰임, `live_ck_`로 시작) |
| `PAYMENT_PG_SECRET` | 토스페이먼츠의 **시크릿 키**(서버 승인 API 호출용, `live_sk_`로 시작 — **절대 클라이언트에 노출되면 안 됨**, 이 값은 서버 프로세스 환경에만 넣는다) |
| `PAYMENT_WEBHOOK_SECRET` | 토스페이먼츠 웹훅 설정 화면에서 발급한 서명 검증용 시크릿 |
| `PAYMENT_EXPECTED_CURRENCY` | `KRW`(기본값, 보통 안 바꿔도 됨) |

### 1-4. 장소 조회(Google Places API New)

| 환경변수 | 값 |
|---|---|
| `PLACE_LOOKUP_ADAPTER` | `google` |
| `GOOGLE_PLACES_API_KEY` | Google Cloud Console에서 발급한 키 — **반드시 Places API(New)가 활성화된 프로젝트의 키**(Legacy Places API 전용 키는 이 코드가 요구하는 필드마스크를 못 준다) |

### 1-5. 경로 계산(Google Routes)

| 환경변수 | 값 |
|---|---|
| `ROUTING_ADAPTER` | `google` |
| `GOOGLE_ROUTES_API_KEY` | Google Cloud Console에서 발급한 키(Places 키와 같은 프로젝트여도 되고 달라도 됨 — Routes API가 활성화돼 있어야 함) |

### 1-5b. 착장 판단용 날씨(WeatherAPI.com) — 7차 신규

| 환경변수 | 값 |
|---|---|
| `WEATHER_API_KEY` | WeatherAPI.com에서 발급한 키(무료 등급으로 충분 — 공식 가격표
  https://www.weatherapi.com/pricing.aspx 확인: 상업적 사용 허용, 월
  100,000회, 현재 날씨+3일 예보 포함, https://www.weatherapi.com/docs/) |

이 키가 없어도 서버는 정상 시작된다(결제·이메일과 달리 **부팅을 막는
필수값이 아니다** — 날씨는 코스·동선 기능에 얹는 부가 기능이라, 키가
없으면 그냥 날씨 카드만 "일시적으로 이용할 수 없어요"로 뜬다). 다만
소비자에게 날씨 카드를 실제로 보여주려면 이 키를 넣어야 한다.

**소비자에게 절대 이 키를 입력하라고 요구하지 않는다** — 서버가 키를
들고 있고, 클라이언트(`src/design/weather-card.js`)는 좌표만 서버에
보낸다(Google Places/Routes 키와 같은 원칙). 이 공급자와 유료 계약을
새로 맺지 않는다 — 무료 등급 그대로 쓴다.

### 1-6. 운영 모드 전환(항상 맨 마지막)

| 환경변수 | 값 | 주의 |
|---|---|---|
| `APP_ENV` | `production` | **이 값을 넣기 전까지는 개발용 결제 시뮬레이션(`/api/dev/simulate-payment`) 같은 테스트 전용 경로가 그대로 열려 있다.** `production`으로 두면 그 경로들이 서버 자체에 아예 등록되지 않는다(`server/test/production-boot.test.mjs`로 확인된 동작) — 실제 손님을 받기 전 반드시 이 값을 넣는다 |
| `FORCE_TEST_MODE` | (설정 안 함 — 비워 둠) | 이 값이 `true`이면 장소 조회·경로 계산이 전부 가짜 테스트 어댑터로 동작한다. **운영 서버에는 이 변수 자체를 아예 넣지 않는다** |

### 1-6b. 특정 계정에 테스트 위치 허용(10차 변경 — 반드시 새로 읽을 것)

**9차까지는 브라우저 주소창에 `?testmode=1`만 붙이면 아무나 영구적으로
테스트 위치 기능을 켤 수 있었다 — 이건 보안 결함이었고 10차에서
막았다.** 이제는 서버 DB의 `accounts.test_access` 값이 `1`인 계정만
로그인마다 서버 재확인을 거쳐 테스트 위치 기능을 쓸 수 있다. 이 값은
운영자가 아래처럼 관리자 전용 API로만 켜고 끌 수 있다(환경변수 아님,
DB 값):

```bash
curl -X POST https://your-domain/api/admin/test-access \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"email":"tester@example.com","enabled":true}'
```

`ADMIN_TOKEN`은 기존 관리자 인증 방식을 그대로 쓴다(별도 신규 환경변수
없음). QA·데모용 계정에만 켜 두고, 실제 손님 계정에는 절대 켜지
않는다. 끌 때는 `"enabled":false`로 같은 요청을 다시 보낸다 —
로그아웃·재로그인 없이도 다음 화면 전환부터 바로 반영된다(클라이언트가
매 로그인/화면마다 서버에 재확인하기 때문).

### 1-6c. AI 보조 자동분류(10차 신규) — 운영자가 할 일 없음

이 기능은 실제 AI 공급자가 아직 선정되지 않아 **운영 환경에서는
항상 비활성이고, 이를 켜는 환경변수 자체가 없다**(`server/config.mjs`
— `APP_ENV=production`이면 무조건 `disabled`). 개발 환경에서만
`AI_CLASSIFY_ADAPTER=mock`으로 가짜 어댑터를 켤 수 있는데, 이건
운영자가 신경 쓸 값이 아니다(운영 서버에 이 변수를 넣어도 코드가
무시한다). 실제 AI 공급자를 선정하면 이 절을 다시 채워야 한다 —
아직은 "구조만 있고 실제 연결은 없다"는 상태다(`docs/BUSINESS_DECISIONS.md`
3-3-6절 참고).

### 1-7. 선택 — 값 조정용(기본값 그대로 써도 된다)

가격·이용권 한도·비용 한도는 전부 기본값이 코드에 들어 있고, 아래
변수로만 바꿀 수 있다(코드 수정 불필요):

- `PRICE_AMOUNT_KRW`(기본 9900), `PRICE_PERIOD_DAYS`(기본 30),
  `PRICE_AUTO_RENEW`(기본 false)
- `ENTITLEMENT_FREE_PLACE_LOOKUP_LIMIT`(기본 10),
  `ENTITLEMENT_FREE_COURSE_LIMIT`(기본 1),
  `ENTITLEMENT_PAID_PLACE_LOOKUP_LIMIT`(기본 50),
  `ENTITLEMENT_PAID_COURSE_LIMIT`(기본 30) — **6차 신규, 이 문서
  작성 시점 기준 "초기 테스트 상품" 스펙이다(`docs/BUSINESS_DECISIONS.md`
  1절·3-3-1절) — 시장 검증된 확정값이 아니므로, 실제 손님 반응을 보고
  바꿀 수 있다는 걸 전제로 둔 값이다.**
- `COST_SAFETY_CAP_FREE_KRW_MICROS`(기본 700원 단위 micros),
  `COST_SAFETY_CAP_PAID_KRW_MICROS`(기본 3,500원 단위 micros) —
  6차 신규 내부 안전상한. **고객에게 보여주는 숫자가 아니다** — 회사가
  실제로 감당할 실비용의 절대 상한이다.
- `COST_GLOBAL_MONTHLY_KRW_MICROS` 등 나머지 비용 한도는
  `docs/BUSINESS_DECISIONS.md` 3-4절 참고.

---

## 2. 서버 시작

환경변수를 전부 넣은 뒤에만 서버를 시작한다:

```bash
DB_PATH=/data/travelhub.db PORT=3000 APP_ENV=production \
EMAIL_ADAPTER=resend EMAIL_API_KEY=re_xxx EMAIL_FROM=noreply@yourdomain.com \
PAYMENT_ADAPTER=toss TOSS_CLIENT_KEY=live_ck_xxx PAYMENT_PG_SECRET=live_sk_xxx PAYMENT_WEBHOOK_SECRET=whsec_xxx \
PLACE_LOOKUP_ADAPTER=google GOOGLE_PLACES_API_KEY=xxx \
ROUTING_ADAPTER=google GOOGLE_ROUTES_API_KEY=xxx \
WEATHER_API_KEY=xxx \
node server/index.mjs
```

(실제 운영에서는 위 값들을 커맨드라인에 직접 안 쓰고, 컨테이너
환경변수나 시크릿 매니저로 주입하는 걸 권장한다 — 커맨드라인 인자는
`ps` 등으로 다른 프로세스에서 보일 수 있다.)

---

## 3. 시작 후 반드시 할 것 — "키가 있다"와 "연결됐다"는 다르다

서버가 뜨면 `GET /api/health`를 열어 본다. 이 응답은 **"각 서비스에
키가 설정돼 있는지"만 보여준다** — `services.places: "configured"`가
"실제로 Google에 요청을 보내 성공 응답을 받았다"는 뜻이 아니다.
`verified` 필드가 있다면 그건 **이 세션이 코드로 미리 짐작한 값**이지
사람이 실제로 확인한 기록이 아니다. **아래 네 가지를 실제 화면에서
직접 눌러 봐야 "연결됐다"고 말할 수 있다**:

1. **이메일 로그인** — 실제 화면에서 이메일 주소를 넣고 "코드 받기"를
   누른 뒤, 그 이메일함에 실제로 6자리 코드가 도착하는지 확인한다.
2. **장소 조회** — 좌표가 없는 장소 하나를 실제로 가져온 뒤, 상세
   화면에서 "서버로 위치 후보 찾아보기"를 눌러 실제 후보(위도·경도·
   주소)가 나오는지 확인한다.
3. **경로 계산** — 좌표가 있는 장소 2곳 이상을 오늘 동선에 담고
   "코스 만들기"를 실제로 눌러, 결과 화면에 "실제 도보 경로 기준으로
   계산했습니다"라고 뜨는지 확인한다(직선거리 추정으로 뜨면 경로 키
   연결에 문제가 있다는 뜻).
4. **결제** — 실제로 소액(9,900원) 결제를 한 번 진행해, 토스페이먼츠
   대시보드에 그 거래가 실제로 찍히는지, 이 계정이 실제로
   `paid`(유료)로 바뀌는지 확인한다. 그 뒤 **테스트 결제라면 반드시
   토스페이먼츠 쪽에서 취소 처리**한다(실제 돈이 오갔다면).
5. **날씨(7차 신규)** — "오늘 동선" 화면을 열어 맨 위 날씨 카드가
   "테스트 데이터(예시)"가 아니라 "WeatherAPI.com 제공"으로 뜨는지,
   실제 그 도시의 실제 기온·날씨로 보이는지 확인한다. 카드 하단의
   "몇 시 기준" 표시가 실제 현재 시각과 크게 어긋나지 않는지도 함께
   본다(너무 오래된 값이면 `stale` 문구가 같이 뜬다).

이 다섯 가지를 실제 아이폰(Safari)에서도 한 번씩 해 보는 걸 권장한다 —
이 세션은 실제 iPhone 기기에서 확인한 적이 없다(`docs/RELEASE_STATUS.md`
2-3절).

---

## 4. 이미 마이그레이션된 계정이 있다면

서버가 처음 켜질 때 기존 `account_courses`(여행 분리 이전 데이터)를
`trips`/`trip_courses`로 자동 옮기는 마이그레이션이 **딱 한 번만**
실행된다(`server/db.mjs`의 `migrateLegacyCoursesIntoTrips`,
`schema_migrations` 테이블로 중복 실행을 막는다). 운영 DB에 처음
배포할 때 서버 로그에서 이 마이그레이션이 실제로 한 번 실행됐는지
확인한다 — 실행 안 됐다면 기존 사용자가 재방문 여행자 화면에서
"여행이 하나도 없다"고 보일 수 있다(데이터 자체는 안전하게 남아 있고
서버 재시작 시 다시 시도된다).

---

## 5. 하지 말아야 할 것

- `FORCE_TEST_MODE=true`를 운영 서버에 넣지 않는다.
- `APP_ENV=production` 없이 실제 손님에게 서버를 공개하지 않는다.
- `PAYMENT_PG_SECRET`을 클라이언트 코드·저장소·로그에 남기지 않는다.
- `/api/health`의 `services` 값만 보고 "전부 연결 확인 완료"라고
  보고하지 않는다(위 3절을 실제로 다 해봐야 한다).
