# 개인 데이터 — 절대 커밋하지 않는다
private/
*.backup.json
*_백업*.json
전체데이터*.json
후쿠오카_개인지도*.json

# 의존성·부산물
node_modules/
dist/
.DS_Store
*.log

# 서버 런타임 DB(테스트는 :memory:를 쓴다 — 이 파일은 로컬 실행 산출물일 뿐)
server/data/

# 편집기
.vscode/
.idea/
